---
title:
  en: HTML encoder examples
order: 20
status: draft
---

The text `<strong>Hello</strong>` can be encoded so it displays as text instead of becoming markup.

Attribute examples should also encode quotes to avoid breaking HTML attributes.
