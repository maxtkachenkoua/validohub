---
title:
  en: JSON validator examples
order: 20
status: draft
---

Valid JSON includes objects, arrays, strings, numbers, booleans, and `null`.

Invalid examples should show why `{name: "Ada"}` is not strict JSON: the property name must be quoted.
