---
title:
  en: URL encoder examples
order: 20
status: draft
---

The text `hello world` becomes `hello%20world` when spaces are percent-encoded.

Reserved characters such as `?`, `&`, and `=` should be encoded when they are data rather than URL syntax.
