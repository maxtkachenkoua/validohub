schemaVersion: 1
id: afghanistan-accessibility-locale-copy-checker
kind: tool
module: validohub
country: AF
category: localization
status: active
name:
  en: "Afghan Locale Copy Checker"
summary:
  en: "Check UI labels for local identifier names, date/currency wording, and support-safe explanations."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.afghanistan-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "COPY input"
        required: true
        placeholder:
          en: "Tazkira boundary input, postal code input, amount 1,234.56 AFN"
    actions: [validate, parse, format, explain]
related:
  explicit: [afghanistan-support-ticket-scrubber, afghanistan-integration-smoke-test-builder]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Afghan Locale Copy Checker | ValidoHub"
  description:
    en: "Check UI labels for local identifier names, date/currency wording, and support-safe explanations."
