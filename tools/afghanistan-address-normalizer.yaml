schemaVersion: 1
id: afghanistan-address-normalizer
kind: tool
module: validohub
country: AF
category: address
status: active
name:
  en: "Afghan Address Normalizer"
summary:
  en: "Normalize street, postal code, locality, region, and country lines for local address forms."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.afghanistan-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ADDR input"
        required: true
        placeholder:
          en: "Shahr-e Naw 1, Kabul 1001"
    actions: [validate, parse, format, explain]
related:
  explicit: [afghanistan-address-transliteration-normalizer, afghanistan-region-code-mapper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Afghan Address Normalizer | ValidoHub"
  description:
    en: "Normalize street, postal code, locality, region, and country lines for local address forms."
