schemaVersion: 1
id: afghanistan-address-transliteration-normalizer
kind: tool
module: validohub
country: AF
category: address
status: active
name:
  en: "Afghan Address Transliteration Normalizer"
summary:
  en: "Prepare ASCII-safe address keys while preserving local display text and search/debug evidence."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.afghanistan-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ASCII input"
        required: true
        placeholder:
          en: "Shahr-e Naw 1, Kabul 1001"
    actions: [validate, parse, format, explain]
related:
  explicit: [afghanistan-region-code-mapper, afghanistan-municipality-code-inspector]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Afghan Address Transliteration Normalizer | ValidoHub"
  description:
    en: "Prepare ASCII-safe address keys while preserving local display text and search/debug evidence."
