schemaVersion: 1
id: afghanistan-bank-account-validator
kind: tool
module: validohub
country: AF
category: finance
status: active
name:
  en: "Afghanistan Bank Account Validator"
summary:
  en: "Validate local bank-account or routing shape, split bank/account blocks, and keep ownership lookup outside the browser."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.afghanistan-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "BANK input"
        required: true
        placeholder:
          en: "001 1234567890"
    actions: [validate, parse, format, explain]
related:
  explicit: [afghanistan-bank-account-fixture-generator, afghanistan-bank-account-inspector]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Afghanistan Bank Account Validator | ValidoHub"
  description:
    en: "Validate local bank-account or routing shape, split bank/account blocks, and keep ownership lookup outside the browser."
