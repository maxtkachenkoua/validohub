schemaVersion: 1
id: afghanistan-currency-decimal-formatter
kind: tool
module: validohub
country: AF
category: finance
status: active
name:
  en: "Afghan AFN Decimal Currency Formatter"
summary:
  en: "Normalize AFN amount strings, decimal separators, grouping, and API-safe numeric previews."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.afghanistan-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "CUR input"
        required: true
        placeholder:
          en: "1,234.56 AFN"
    actions: [validate, parse, format, explain]
related:
  explicit: [afghanistan-tax-rate-sanity-helper, afghanistan-tax-return-field-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Afghan AFN Decimal Currency Formatter | ValidoHub"
  description:
    en: "Normalize AFN amount strings, decimal separators, grouping, and API-safe numeric previews."
