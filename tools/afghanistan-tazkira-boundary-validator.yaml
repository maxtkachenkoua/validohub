schemaVersion: 1
id: afghanistan-tazkira-boundary-validator
kind: tool
module: validohub
country: AF
category: national-identifiers
status: active
name:
  en: "Afghan Tazkira boundary Validator"
summary:
  en: "Validate Tazkira boundary shape, split date/control/body evidence, and prepare privacy-safe debugging output."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.afghanistan-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ID input"
        required: true
        placeholder:
          en: "1234-567890"
    actions: [validate, parse, format, explain]
related:
  explicit: [afghanistan-company-registration-number-validator, afghanistan-tax-id-validator]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Afghan Tazkira boundary Validator | ValidoHub"
  description:
    en: "Validate Tazkira boundary shape, split date/control/body evidence, and prepare privacy-safe debugging output."
