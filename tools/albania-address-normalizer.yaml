schemaVersion: 1
id: albania-address-normalizer
kind: tool
module: validohub
country: AL
category: address
status: active
name:
  en: "Albanian Address Normalizer"
summary:
  en: "Normalize street, postal code, locality, region, and country lines for local address forms."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.albania-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ADDR input"
        required: true
        placeholder:
          en: "Bulevardi Deshmoret e Kombit 1, 1001 Tirana"
    actions: [validate, parse, format, explain]
related:
  explicit: [albania-address-transliteration-normalizer, albania-region-code-mapper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Albanian Address Normalizer | ValidoHub"
  description:
    en: "Normalize street, postal code, locality, region, and country lines for local address forms."
