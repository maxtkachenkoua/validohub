schemaVersion: 1
id: albania-iban-validator
kind: tool
module: validohub
country: AL
category: finance
status: active
name:
  en: "Albania IBAN Validator"
summary:
  en: "Validate AL IBAN shape, replay MOD-97 evidence, and split bank/account blocks for debugging."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.albania-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "IBAN input"
        required: true
        placeholder:
          en: "AL47212110090000000235698741"
    actions: [validate, parse, format, explain]
related:
  explicit: [albania-iban-generator, albania-bank-account-inspector]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Albania IBAN Validator | ValidoHub"
  description:
    en: "Validate AL IBAN shape, replay MOD-97 evidence, and split bank/account blocks for debugging."
