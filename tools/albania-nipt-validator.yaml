schemaVersion: 1
id: albania-nipt-validator
kind: tool
module: validohub
country: AL
category: national-identifiers
status: active
name:
  en: "Albanian NIPT Validator"
summary:
  en: "Inspect NIPT structure, registry-style prefixes, control digits, and official lookup boundaries."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.albania-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ORG input"
        required: true
        placeholder:
          en: "L12345678A"
    actions: [validate, parse, format, explain]
related:
  explicit: [albania-vat-id-validator, albania-eori-validator]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Albanian NIPT Validator | ValidoHub"
  description:
    en: "Inspect NIPT structure, registry-style prefixes, control digits, and official lookup boundaries."
