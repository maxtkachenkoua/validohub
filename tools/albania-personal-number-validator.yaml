schemaVersion: 1
id: albania-personal-number-validator
kind: tool
module: validohub
country: AL
category: national-identifiers
status: active
name:
  en: "Albanian Personal number Validator"
summary:
  en: "Validate Personal number shape, split date/control/body evidence, and prepare privacy-safe debugging output."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.albania-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ID input"
        required: true
        placeholder:
          en: "J12345678K"
    actions: [validate, parse, format, explain]
related:
  explicit: [albania-nipt-validator, albania-vat-id-validator]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Albanian Personal number Validator | ValidoHub"
  description:
    en: "Validate Personal number shape, split date/control/body evidence, and prepare privacy-safe debugging output."
