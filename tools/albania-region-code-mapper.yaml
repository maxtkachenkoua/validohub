schemaVersion: 1
id: albania-region-code-mapper
kind: tool
module: validohub
country: AL
category: address
status: active
name:
  en: "Albanian Region / Province Code Mapper"
summary:
  en: "Inspect regional abbreviations, locality evidence, postal zones, and official geography lookup boundaries."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.albania-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "REGION input"
        required: true
        placeholder:
          en: "1001 Tirana"
    actions: [validate, parse, format, explain]
related:
  explicit: [albania-municipality-code-inspector, albania-phone-number-validator]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Albanian Region / Province Code Mapper | ValidoHub"
  description:
    en: "Inspect regional abbreviations, locality evidence, postal zones, and official geography lookup boundaries."
