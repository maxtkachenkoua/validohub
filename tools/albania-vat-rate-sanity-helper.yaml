schemaVersion: 1
id: albania-vat-rate-sanity-helper
kind: tool
module: validohub
country: AL
category: tax
status: active
name:
  en: "Albanian VAT Rate Sanity Helper"
summary:
  en: "Inspect VAT-rate snippets, local tax labels, amount bases, and official rate lookup boundaries."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.albania-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "RATE input"
        required: true
        placeholder:
          en: "TVSH / VAT 20% base 1.234,56 ALL"
    actions: [validate, parse, format, explain]
related:
  explicit: [albania-vat-return-field-helper, albania-invoice-number-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Albanian VAT Rate Sanity Helper | ValidoHub"
  description:
    en: "Inspect VAT-rate snippets, local tax labels, amount bases, and official rate lookup boundaries."
