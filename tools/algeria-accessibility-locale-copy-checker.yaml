schemaVersion: 1
id: algeria-accessibility-locale-copy-checker
kind: tool
module: validohub
country: DZ
category: localization
status: active
name:
  en: "Algerian Locale Copy Checker"
summary:
  en: "Check UI labels for local identifier names, date/currency wording, and support-safe explanations."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.algeria-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "COPY input"
        required: true
        placeholder:
          en: "national ID boundary input, postal/address code input, amount 1,234.56 DZD"
    actions: [validate, parse, format, explain]
related:
  explicit: [algeria-support-ticket-scrubber, algeria-integration-smoke-test-builder]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Algerian Locale Copy Checker | ValidoHub"
  description:
    en: "Check UI labels for local identifier names, date/currency wording, and support-safe explanations."
