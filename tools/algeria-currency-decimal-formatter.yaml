schemaVersion: 1
id: algeria-currency-decimal-formatter
kind: tool
module: validohub
country: DZ
category: finance
status: active
name:
  en: "Algerian DZD Decimal Currency Formatter"
summary:
  en: "Normalize DZD amount strings, decimal separators, grouping, and API-safe numeric previews."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.algeria-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "CUR input"
        required: true
        placeholder:
          en: "1,234.56 DZD"
    actions: [validate, parse, format, explain]
related:
  explicit: [algeria-tax-rate-sanity-helper, algeria-tax-return-field-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Algerian DZD Decimal Currency Formatter | ValidoHub"
  description:
    en: "Normalize DZD amount strings, decimal separators, grouping, and API-safe numeric previews."
