schemaVersion: 1
id: angola-business-register-readiness-helper
kind: tool
module: validohub
country: AO
category: government
status: active
name:
  en: "Angolan business registry Readiness Helper"
summary:
  en: "Prepare browser-only evidence before a regulated business registry lookup or company registry workflow."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.angola-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "REG input"
        required: true
        placeholder:
          en: "AO-REG-123456 AO123456789 Central business district, Luanda 1000"
    actions: [validate, parse, format, explain]
related:
  explicit: [angola-id-card-format-helper, angola-passport-number-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Angolan business registry Readiness Helper | ValidoHub"
  description:
    en: "Prepare browser-only evidence before a regulated business registry lookup or company registry workflow."
