schemaVersion: 1
id: angola-currency-decimal-formatter
kind: tool
module: validohub
country: AO
category: finance
status: active
name:
  en: "Angolan AOA Decimal Currency Formatter"
summary:
  en: "Normalize AOA amount strings, decimal separators, grouping, and API-safe numeric previews."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.angola-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "CUR input"
        required: true
        placeholder:
          en: "1,234.56 AOA"
    actions: [validate, parse, format, explain]
related:
  explicit: [angola-tax-rate-sanity-helper, angola-tax-return-field-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Angolan AOA Decimal Currency Formatter | ValidoHub"
  description:
    en: "Normalize AOA amount strings, decimal separators, grouping, and API-safe numeric previews."
