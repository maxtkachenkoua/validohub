schemaVersion: 1
id: argentina-address-transliteration-normalizer
kind: tool
module: validohub
country: AR
category: address
status: active
name:
  en: "Argentine Address Transliteration Normalizer"
summary:
  en: "Prepare ASCII-safe address keys while preserving local display text and search/debug evidence."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.argentina-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ASCII input"
        required: true
        placeholder:
          en: "Av. Corrientes 1234, C1043 Buenos Aires"
    actions: [validate, parse, format, explain]
related:
  explicit: [argentina-region-code-mapper, argentina-municipality-code-inspector]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Argentine Address Transliteration Normalizer | ValidoHub"
  description:
    en: "Prepare ASCII-safe address keys while preserving local display text and search/debug evidence."
