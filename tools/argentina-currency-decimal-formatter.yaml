schemaVersion: 1
id: argentina-currency-decimal-formatter
kind: tool
module: validohub
country: AR
category: finance
status: active
name:
  en: "Argentine ARS Decimal Currency Formatter"
summary:
  en: "Normalize ARS amount strings, decimal separators, grouping, and API-safe numeric previews."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.argentina-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "CUR input"
        required: true
        placeholder:
          en: "1.234,56 ARS"
    actions: [validate, parse, format, explain]
related:
  explicit: [argentina-vat-rate-sanity-helper, argentina-vat-return-field-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Argentine ARS Decimal Currency Formatter | ValidoHub"
  description:
    en: "Normalize ARS amount strings, decimal separators, grouping, and API-safe numeric previews."
