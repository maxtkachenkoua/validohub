schemaVersion: 1
id: argentina-payment-reference-helper
kind: tool
module: validohub
country: AR
category: finance
status: active
name:
  en: "Argentine CBU / CVU / Alias Reference Helper"
summary:
  en: "Inspect local payment references, invoice links, amount evidence, and reconciliation-safe formatting."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.argentina-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "PAY input"
        required: true
        placeholder:
          en: "CBU / CVU / Alias REF 2026-001 1.234,56 ARS"
    actions: [validate, parse, format, explain]
related:
  explicit: [argentina-remittance-text-builder, argentina-payment-reconciliation-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Argentine CBU / CVU / Alias Reference Helper | ValidoHub"
  description:
    en: "Inspect local payment references, invoice links, amount evidence, and reconciliation-safe formatting."
