schemaVersion: 1
id: argentina-vat-id-validator
kind: tool
module: validohub
country: AR
category: national-identifiers
status: active
name:
  en: "Argentine IVA / CUIT Tax ID Validator"
summary:
  en: "Normalize IVA / CUIT identifiers, inspect local tax body evidence, and prepare tax-authority handoff diagnostics."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.argentina-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "TAX input"
        required: true
        placeholder:
          en: "AR 30-12345678-9"
    actions: [validate, parse, format, explain]
related:
  explicit: [argentina-customs-tax-identifier-helper, argentina-cuil-social-insurance-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Argentine IVA / CUIT Tax ID Validator | ValidoHub"
  description:
    en: "Normalize IVA / CUIT identifiers, inspect local tax body evidence, and prepare tax-authority handoff diagnostics."
