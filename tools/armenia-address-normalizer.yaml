schemaVersion: 1
id: armenia-address-normalizer
kind: tool
module: validohub
country: AM
category: address
status: active
name:
  en: "Armenian Address Normalizer"
summary:
  en: "Normalize street, postal code, locality, region, and country lines for local address forms."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.armenia-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ADDR input"
        required: true
        placeholder:
          en: "Mashtots Avenue 1, Yerevan 0010"
    actions: [validate, parse, format, explain]
related:
  explicit: [armenia-address-transliteration-normalizer, armenia-region-code-mapper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Armenian Address Normalizer | ValidoHub"
  description:
    en: "Normalize street, postal code, locality, region, and country lines for local address forms."
