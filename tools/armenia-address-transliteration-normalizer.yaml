schemaVersion: 1
id: armenia-address-transliteration-normalizer
kind: tool
module: validohub
country: AM
category: address
status: active
name:
  en: "Armenian Address Transliteration Normalizer"
summary:
  en: "Prepare ASCII-safe address keys while preserving local display text and search/debug evidence."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.armenia-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ASCII input"
        required: true
        placeholder:
          en: "Mashtots Avenue 1, Yerevan 0010"
    actions: [validate, parse, format, explain]
related:
  explicit: [armenia-region-code-mapper, armenia-municipality-code-inspector]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Armenian Address Transliteration Normalizer | ValidoHub"
  description:
    en: "Prepare ASCII-safe address keys while preserving local display text and search/debug evidence."
