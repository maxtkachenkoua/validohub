schemaVersion: 1
id: armenia-currency-decimal-formatter
kind: tool
module: validohub
country: AM
category: finance
status: active
name:
  en: "Armenian AMD Decimal Currency Formatter"
summary:
  en: "Normalize AMD amount strings, decimal separators, grouping, and API-safe numeric previews."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.armenia-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "CUR input"
        required: true
        placeholder:
          en: "1 234.56 AMD"
    actions: [validate, parse, format, explain]
related:
  explicit: [armenia-tax-rate-sanity-helper, armenia-tax-return-field-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Armenian AMD Decimal Currency Formatter | ValidoHub"
  description:
    en: "Normalize AMD amount strings, decimal separators, grouping, and API-safe numeric previews."
