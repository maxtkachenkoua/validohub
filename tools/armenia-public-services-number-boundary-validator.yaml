schemaVersion: 1
id: armenia-public-services-number-boundary-validator
kind: tool
module: validohub
country: AM
category: national-identifiers
status: active
name:
  en: "Armenian public services number boundary Validator"
summary:
  en: "Validate public services number boundary shape, split date/control/body evidence, and prepare privacy-safe debugging output."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.armenia-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ID input"
        required: true
        placeholder:
          en: "1234567890"
    actions: [validate, parse, format, explain]
related:
  explicit: [armenia-state-register-number-validator, armenia-tax-id-validator]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Armenian public services number boundary Validator | ValidoHub"
  description:
    en: "Validate public services number boundary shape, split date/control/body evidence, and prepare privacy-safe debugging output."
