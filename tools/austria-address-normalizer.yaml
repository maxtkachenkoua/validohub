schemaVersion: 1
id: austria-address-normalizer
kind: tool
module: validohub
country: AT
category: address
status: active
name:
  en: "Austrian Address Normalizer"
summary:
  en: "Normalize street, postal code, locality, region, and country lines for local address forms."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.austria-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ADDR input"
        required: true
        placeholder:
          en: "Kaerntner Strasse 10, 1010 Wien"
    actions: [validate, parse, format, explain]
related:
  explicit: [austria-address-transliteration-normalizer, austria-region-code-mapper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Austrian Address Normalizer | ValidoHub"
  description:
    en: "Normalize street, postal code, locality, region, and country lines for local address forms."
