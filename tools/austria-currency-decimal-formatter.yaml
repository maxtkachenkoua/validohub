schemaVersion: 1
id: austria-currency-decimal-formatter
kind: tool
module: validohub
country: AT
category: finance
status: active
name:
  en: "Austrian EUR Decimal Currency Formatter"
summary:
  en: "Normalize EUR amount strings, decimal separators, grouping, and API-safe numeric previews."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.austria-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "CUR input"
        required: true
        placeholder:
          en: "1.234,56 EUR"
    actions: [validate, parse, format, explain]
related:
  explicit: [austria-vat-rate-sanity-helper, austria-vat-return-field-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Austrian EUR Decimal Currency Formatter | ValidoHub"
  description:
    en: "Normalize EUR amount strings, decimal separators, grouping, and API-safe numeric previews."
