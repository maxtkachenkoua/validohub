schemaVersion: 1
id: austria-svnr-validator
kind: tool
module: validohub
country: AT
category: national-identifiers
status: active
name:
  en: "Austrian SVNR Validator"
summary:
  en: "Validate SVNR shape, split date/control/body evidence, and prepare privacy-safe debugging output."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.austria-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ID input"
        required: true
        placeholder:
          en: "1234010180"
    actions: [validate, parse, format, explain]
related:
  explicit: [austria-firmenbuchnummer-validator, austria-vat-id-validator]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Austrian SVNR Validator | ValidoHub"
  description:
    en: "Validate SVNR shape, split date/control/body evidence, and prepare privacy-safe debugging output."
