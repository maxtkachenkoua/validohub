schemaVersion: 1
id: azerbaijan-address-transliteration-normalizer
kind: tool
module: validohub
country: AZ
category: address
status: active
name:
  en: "Azerbaijani Address Transliteration Normalizer"
summary:
  en: "Prepare ASCII-safe address keys while preserving local display text and search/debug evidence."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.azerbaijan-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ASCII input"
        required: true
        placeholder:
          en: "Neftchilar Avenue 1, Baku AZ1000"
    actions: [validate, parse, format, explain]
related:
  explicit: [azerbaijan-region-code-mapper, azerbaijan-municipality-code-inspector]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Azerbaijani Address Transliteration Normalizer | ValidoHub"
  description:
    en: "Prepare ASCII-safe address keys while preserving local display text and search/debug evidence."
