schemaVersion: 1
id: azerbaijan-bank-account-fixture-generator
kind: tool
module: validohub
country: AZ
category: finance
status: active
name:
  en: "Azerbaijan Bank Account Fixture Generator"
summary:
  en: "Generate local bank-account fixture payloads, split routing/account blocks, and prepare payment QA data."
capabilities:
  - generate
  - validate
  - parse
  - explain
algorithm:
  algorithmId: validohub.azerbaijan-suite
forms:
  generate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "BGEN input"
        required: true
        placeholder:
          en: "NABZ 137010001944"
    actions: [generate, validate, explain]
related:
  explicit: [azerbaijan-bank-account-inspector, azerbaijan-bic-swift-inspector]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Azerbaijan Bank Account Fixture Generator | ValidoHub"
  description:
    en: "Generate local bank-account fixture payloads, split routing/account blocks, and prepare payment QA data."
