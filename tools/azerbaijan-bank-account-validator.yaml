schemaVersion: 1
id: azerbaijan-bank-account-validator
kind: tool
module: validohub
country: AZ
category: finance
status: active
name:
  en: "Azerbaijan Bank Account Validator"
summary:
  en: "Validate local bank-account or routing shape, split bank/account blocks, and keep ownership lookup outside the browser."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.azerbaijan-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "BANK input"
        required: true
        placeholder:
          en: "NABZ 137010001944"
    actions: [validate, parse, format, explain]
related:
  explicit: [azerbaijan-bank-account-fixture-generator, azerbaijan-bank-account-inspector]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Azerbaijan Bank Account Validator | ValidoHub"
  description:
    en: "Validate local bank-account or routing shape, split bank/account blocks, and keep ownership lookup outside the browser."
