schemaVersion: 1
id: azerbaijan-business-register-readiness-helper
kind: tool
module: validohub
country: AZ
category: government
status: active
name:
  en: "Azerbaijani State Tax Service registry Readiness Helper"
summary:
  en: "Prepare browser-only evidence before a regulated State Tax Service registry lookup or company registry workflow."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.azerbaijan-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "REG input"
        required: true
        placeholder:
          en: "AZ1234567 9900003611 Neftchilar Avenue 1, Baku AZ1000"
    actions: [validate, parse, format, explain]
related:
  explicit: [azerbaijan-id-card-format-helper, azerbaijan-passport-number-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Azerbaijani State Tax Service registry Readiness Helper | ValidoHub"
  description:
    en: "Prepare browser-only evidence before a regulated State Tax Service registry lookup or company registry workflow."
