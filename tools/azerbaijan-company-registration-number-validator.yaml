schemaVersion: 1
id: azerbaijan-company-registration-number-validator
kind: tool
module: validohub
country: AZ
category: national-identifiers
status: active
name:
  en: "Azerbaijani company registration number Validator"
summary:
  en: "Inspect company registration number structure, registry-style prefixes, control digits, and official lookup boundaries."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.azerbaijan-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ORG input"
        required: true
        placeholder:
          en: "AZ1234567"
    actions: [validate, parse, format, explain]
related:
  explicit: [azerbaijan-tax-id-validator, azerbaijan-customs-importer-code-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Azerbaijani company registration number Validator | ValidoHub"
  description:
    en: "Inspect company registration number structure, registry-style prefixes, control digits, and official lookup boundaries."
