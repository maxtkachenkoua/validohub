schemaVersion: 1
id: azerbaijan-currency-decimal-formatter
kind: tool
module: validohub
country: AZ
category: finance
status: active
name:
  en: "Azerbaijani AZN Decimal Currency Formatter"
summary:
  en: "Normalize AZN amount strings, decimal separators, grouping, and API-safe numeric previews."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.azerbaijan-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "CUR input"
        required: true
        placeholder:
          en: "1 234,56 AZN"
    actions: [validate, parse, format, explain]
related:
  explicit: [azerbaijan-tax-rate-sanity-helper, azerbaijan-tax-return-field-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Azerbaijani AZN Decimal Currency Formatter | ValidoHub"
  description:
    en: "Normalize AZN amount strings, decimal separators, grouping, and API-safe numeric previews."
