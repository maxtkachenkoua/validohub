schemaVersion: 1
id: azerbaijan-phone-number-validator
kind: tool
module: validohub
country: AZ
category: address
status: active
name:
  en: "Azerbaijani Phone Number Validator"
summary:
  en: "Validate local phone shape, country prefix, national number blocks, and contact-form safety notes."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.azerbaijan-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "PHONE input"
        required: true
        placeholder:
          en: "+994 50 123 45 67"
    actions: [validate, parse, format, explain]
related:
  explicit: [azerbaijan-phone-e164-formatter, azerbaijan-date-locale-formatter]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Azerbaijani Phone Number Validator | ValidoHub"
  description:
    en: "Validate local phone shape, country prefix, national number blocks, and contact-form safety notes."
