schemaVersion: 1
id: azerbaijan-region-code-mapper
kind: tool
module: validohub
country: AZ
category: address
status: active
name:
  en: "Azerbaijani Region / Province Code Mapper"
summary:
  en: "Inspect regional abbreviations, locality evidence, postal zones, and official geography lookup boundaries."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.azerbaijan-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "REGION input"
        required: true
        placeholder:
          en: "AZ1000 Baku"
    actions: [validate, parse, format, explain]
related:
  explicit: [azerbaijan-municipality-code-inspector, azerbaijan-phone-number-validator]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Azerbaijani Region / Province Code Mapper | ValidoHub"
  description:
    en: "Inspect regional abbreviations, locality evidence, postal zones, and official geography lookup boundaries."
