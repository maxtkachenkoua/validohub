schemaVersion: 1
id: azerbaijan-tax-id-validator
kind: tool
module: validohub
country: AZ
category: national-identifiers
status: active
name:
  en: "Azerbaijani VOEN / TIN Validator"
summary:
  en: "Normalize VOEN / TIN identifiers, inspect local tax body evidence, and prepare authority handoff diagnostics."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.azerbaijan-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "TAX input"
        required: true
        placeholder:
          en: "9900003611"
    actions: [validate, parse, format, explain]
related:
  explicit: [azerbaijan-customs-importer-code-helper, azerbaijan-social-insurance-boundary-social-insurance-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Azerbaijani VOEN / TIN Validator | ValidoHub"
  description:
    en: "Normalize VOEN / TIN identifiers, inspect local tax body evidence, and prepare authority handoff diagnostics."
