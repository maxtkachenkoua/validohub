schemaVersion: 1
id: azerbaijan-tax-rate-sanity-helper
kind: tool
module: validohub
country: AZ
category: tax
status: active
name:
  en: "Azerbaijani Tax Rate Sanity Helper"
summary:
  en: "Inspect tax-rate snippets, local tax labels, amount bases, and official rate lookup boundaries."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.azerbaijan-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "RATE input"
        required: true
        placeholder:
          en: "VOEN / TIN 20% base 1 234,56 AZN"
    actions: [validate, parse, format, explain]
related:
  explicit: [azerbaijan-tax-return-field-helper, azerbaijan-invoice-number-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Azerbaijani Tax Rate Sanity Helper | ValidoHub"
  description:
    en: "Inspect tax-rate snippets, local tax labels, amount bases, and official rate lookup boundaries."
