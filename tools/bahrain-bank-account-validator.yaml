schemaVersion: 1
id: bahrain-bank-account-validator
kind: tool
module: validohub
country: BH
category: finance
status: active
name:
  en: "Bahrain Bank Account Validator"
summary:
  en: "Validate local bank-account or routing shape, split bank/account blocks, and keep ownership lookup outside the browser."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.bahrain-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "BANK input"
        required: true
        placeholder:
          en: "BMAG 1299123456"
    actions: [validate, parse, format, explain]
related:
  explicit: [bahrain-bank-account-fixture-generator, bahrain-bank-account-inspector]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Bahrain Bank Account Validator | ValidoHub"
  description:
    en: "Validate local bank-account or routing shape, split bank/account blocks, and keep ownership lookup outside the browser."
