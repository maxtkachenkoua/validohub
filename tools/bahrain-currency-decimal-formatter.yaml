schemaVersion: 1
id: bahrain-currency-decimal-formatter
kind: tool
module: validohub
country: BH
category: finance
status: active
name:
  en: "Bahraini BHD Decimal Currency Formatter"
summary:
  en: "Normalize BHD amount strings, decimal separators, grouping, and API-safe numeric previews."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.bahrain-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "CUR input"
        required: true
        placeholder:
          en: "1,234.560 BHD"
    actions: [validate, parse, format, explain]
related:
  explicit: [bahrain-tax-rate-sanity-helper, bahrain-tax-return-field-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Bahraini BHD Decimal Currency Formatter | ValidoHub"
  description:
    en: "Normalize BHD amount strings, decimal separators, grouping, and API-safe numeric previews."
