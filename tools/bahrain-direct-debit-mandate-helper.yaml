schemaVersion: 1
id: bahrain-direct-debit-mandate-helper
kind: tool
module: validohub
country: BH
category: finance
status: active
name:
  en: "Bahraini Direct Debit / Auto-Debit Mandate Helper"
summary:
  en: "Inspect mandate references, creditor data, debtor account evidence, and browser-only debit readiness."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.bahrain-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "DD input"
        required: true
        placeholder:
          en: "MANDATE-2026-001 BMAG 1299123456"
    actions: [validate, parse, format, explain]
related:
  explicit: [bahrain-payment-reference-helper, bahrain-remittance-text-builder]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Bahraini Direct Debit / Auto-Debit Mandate Helper | ValidoHub"
  description:
    en: "Inspect mandate references, creditor data, debtor account evidence, and browser-only debit readiness."
