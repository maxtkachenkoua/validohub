schemaVersion: 1
id: bangladesh-currency-decimal-formatter
kind: tool
module: validohub
country: BD
category: finance
status: active
name:
  en: "Bangladeshi BDT Decimal Currency Formatter"
summary:
  en: "Normalize BDT amount strings, decimal separators, grouping, and API-safe numeric previews."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.bangladesh-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "CUR input"
        required: true
        placeholder:
          en: "1,234.56 BDT"
    actions: [validate, parse, format, explain]
related:
  explicit: [bangladesh-tax-rate-sanity-helper, bangladesh-tax-return-field-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Bangladeshi BDT Decimal Currency Formatter | ValidoHub"
  description:
    en: "Normalize BDT amount strings, decimal separators, grouping, and API-safe numeric previews."
