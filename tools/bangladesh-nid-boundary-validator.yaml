schemaVersion: 1
id: bangladesh-nid-boundary-validator
kind: tool
module: validohub
country: BD
category: national-identifiers
status: active
name:
  en: "Bangladeshi NID boundary Validator"
summary:
  en: "Validate NID boundary shape, split date/control/body evidence, and prepare privacy-safe debugging output."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.bangladesh-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ID input"
        required: true
        placeholder:
          en: "1990123456789"
    actions: [validate, parse, format, explain]
related:
  explicit: [bangladesh-rjsc-registration-number-validator, bangladesh-tax-id-validator]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Bangladeshi NID boundary Validator | ValidoHub"
  description:
    en: "Validate NID boundary shape, split date/control/body evidence, and prepare privacy-safe debugging output."
