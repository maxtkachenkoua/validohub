schemaVersion: 1
id: belgium-rrn-niss-validator
kind: tool
module: validohub
country: BE
category: national-identifiers
status: active
name:
  en: "Belgian RRN / NISS Validator"
summary:
  en: "Validate RRN / NISS shape, split date/control/body evidence, and prepare privacy-safe debugging output."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.belgium-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ID input"
        required: true
        placeholder:
          en: "85.07.30-033.28"
    actions: [validate, parse, format, explain]
related:
  explicit: [belgium-kbo-bce-validator, belgium-vat-id-validator]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Belgian RRN / NISS Validator | ValidoHub"
  description:
    en: "Validate RRN / NISS shape, split date/control/body evidence, and prepare privacy-safe debugging output."
