schemaVersion: 1
id: belgium-vat-id-validator
kind: tool
module: validohub
country: BE
category: national-identifiers
status: active
name:
  en: "Belgian VAT ID / BE Prefix Validator"
summary:
  en: "Normalize BE VAT identifiers, inspect local tax body evidence, and prepare VIES handoff diagnostics."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.belgium-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "VAT input"
        required: true
        placeholder:
          en: "BE0123456749"
    actions: [validate, parse, format, explain]
related:
  explicit: [belgium-eori-validator, belgium-bis-number-social-insurance-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Belgian VAT ID / BE Prefix Validator | ValidoHub"
  description:
    en: "Normalize BE VAT identifiers, inspect local tax body evidence, and prepare VIES handoff diagnostics."
