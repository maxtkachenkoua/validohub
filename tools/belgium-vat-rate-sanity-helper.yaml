schemaVersion: 1
id: belgium-vat-rate-sanity-helper
kind: tool
module: validohub
country: BE
category: tax
status: active
name:
  en: "Belgian VAT Rate Sanity Helper"
summary:
  en: "Inspect VAT-rate snippets, local tax labels, amount bases, and official rate lookup boundaries."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.belgium-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "RATE input"
        required: true
        placeholder:
          en: "BTW / TVA 20% base 1.234,56 EUR"
    actions: [validate, parse, format, explain]
related:
  explicit: [belgium-vat-return-field-helper, belgium-invoice-number-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Belgian VAT Rate Sanity Helper | ValidoHub"
  description:
    en: "Inspect VAT-rate snippets, local tax labels, amount bases, and official rate lookup boundaries."
