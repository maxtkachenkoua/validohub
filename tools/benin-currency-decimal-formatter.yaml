schemaVersion: 1
id: benin-currency-decimal-formatter
kind: tool
module: validohub
country: BJ
category: finance
status: active
name:
  en: "Beninese XOF Decimal Currency Formatter"
summary:
  en: "Normalize XOF amount strings, decimal separators, grouping, and API-safe numeric previews."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.benin-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "CUR input"
        required: true
        placeholder:
          en: "1,234.56 XOF"
    actions: [validate, parse, format, explain]
related:
  explicit: [benin-tax-rate-sanity-helper, benin-tax-return-field-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Beninese XOF Decimal Currency Formatter | ValidoHub"
  description:
    en: "Normalize XOF amount strings, decimal separators, grouping, and API-safe numeric previews."
