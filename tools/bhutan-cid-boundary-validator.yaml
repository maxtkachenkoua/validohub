schemaVersion: 1
id: bhutan-cid-boundary-validator
kind: tool
module: validohub
country: BT
category: national-identifiers
status: active
name:
  en: "Bhutanese CID boundary Validator"
summary:
  en: "Validate CID boundary shape, split date/control/body evidence, and prepare privacy-safe debugging output."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.bhutan-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ID input"
        required: true
        placeholder:
          en: "10101001234"
    actions: [validate, parse, format, explain]
related:
  explicit: [bhutan-company-registration-number-validator, bhutan-tax-id-validator]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Bhutanese CID boundary Validator | ValidoHub"
  description:
    en: "Validate CID boundary shape, split date/control/body evidence, and prepare privacy-safe debugging output."
