schemaVersion: 1
id: bolivia-address-normalizer
kind: tool
module: validohub
country: BO
category: address
status: active
name:
  en: "Bolivian Address Normalizer"
summary:
  en: "Normalize street, postal code, locality, region, and country lines for local address forms."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.bolivia-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ADDR input"
        required: true
        placeholder:
          en: "Av. Arce 123, La Paz"
    actions: [validate, parse, format, explain]
related:
  explicit: [bolivia-address-transliteration-normalizer, bolivia-region-code-mapper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Bolivian Address Normalizer | ValidoHub"
  description:
    en: "Normalize street, postal code, locality, region, and country lines for local address forms."
