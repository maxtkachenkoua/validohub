schemaVersion: 1
id: bolivia-ci-validator
kind: tool
module: validohub
country: BO
category: national-identifiers
status: active
name:
  en: "Bolivian CI Validator"
summary:
  en: "Validate CI shape, split date/control/body evidence, and prepare privacy-safe debugging output."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.bolivia-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ID input"
        required: true
        placeholder:
          en: "1234567 LP"
    actions: [validate, parse, format, explain]
related:
  explicit: [bolivia-nit-validator, bolivia-vat-id-validator]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Bolivian CI Validator | ValidoHub"
  description:
    en: "Validate CI shape, split date/control/body evidence, and prepare privacy-safe debugging output."
