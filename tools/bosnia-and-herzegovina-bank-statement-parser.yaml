schemaVersion: 1
id: bosnia-and-herzegovina-bank-statement-parser
kind: tool
module: validohub
country: BA
category: finance
status: active
name:
  en: "Bosnian Bank Statement Parser"
summary:
  en: "Parse statement rows for date, amount, IBAN, counterparty, reference, and local decimal conventions."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.bosnia-and-herzegovina-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "STMT input"
        required: true
        placeholder:
          en: "21.07.2026; 1.234,56 BAM; BA391290079401028494; sample counterparty"
    actions: [validate, parse, format, explain]
related:
  explicit: [bosnia-and-herzegovina-masked-iban-formatter, bosnia-and-herzegovina-currency-decimal-formatter]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Bosnian Bank Statement Parser | ValidoHub"
  description:
    en: "Parse statement rows for date, amount, IBAN, counterparty, reference, and local decimal conventions."
