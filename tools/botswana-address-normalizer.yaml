schemaVersion: 1
id: botswana-address-normalizer
kind: tool
module: validohub
country: BW
category: address
status: active
name:
  en: "Botswana Address Normalizer"
summary:
  en: "Normalize street, postal code, locality, region, and country lines for local address forms."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.botswana-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ADDR input"
        required: true
        placeholder:
          en: "Central business district, Gaborone 1000"
    actions: [validate, parse, format, explain]
related:
  explicit: [botswana-address-transliteration-normalizer, botswana-region-code-mapper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Botswana Address Normalizer | ValidoHub"
  description:
    en: "Normalize street, postal code, locality, region, and country lines for local address forms."
