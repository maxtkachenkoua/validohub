schemaVersion: 1
id: brazil-boleto-barcode-validator
kind: tool
module: validohub
country: BR
category: finance
status: active
name:
  en: "Boleto Barcode Validator"
summary:
  en: "Validate boleto barcode shape, inspect bank code, currency, due-date factor, amount fields, and check digit boundaries."
capabilities:
  - validate
  - parse
  - generate
  - format
  - calculate
  - explain
algorithm:
  algorithmId: validohub.brazil-suite
forms:
  validate:
    inputs:
      - name: input
        type: text
        required: true
    actions:
      - validate
      - parse
      - generate
      - format
      - calculate
      - explain
related:
  explicit: [brazil-pix-validator, brazil-cpf-validator, brazil-cnpj-validator, brazil-cep-validator]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
    sameModule: true
seo:
  title:
    en: "Boleto Barcode Validator | ValidoHub"
  description:
    en: "Validate boleto barcode shape, inspect bank code, currency, due-date factor, amount fields, and check digit boundaries."
