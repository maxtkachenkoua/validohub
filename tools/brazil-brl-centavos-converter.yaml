schemaVersion: 1
id: brazil-brl-centavos-converter
kind: tool
module: validohub
country: BR
category: finance
status: active
name:
  en: "BRL Centavos Converter"
summary:
  en: "Parse Brazilian real amounts, normalize comma decimals, convert to integer centavos, and generate storage-safe payment fields."
capabilities:
  - validate
  - parse
  - generate
  - format
  - calculate
  - explain
algorithm:
  algorithmId: validohub.brazil-suite
forms:
  validate:
    inputs:
      - name: input
        type: text
        required: true
    actions:
      - validate
      - parse
      - generate
      - format
      - calculate
      - explain
related:
  explicit: [brazil-pix-validator, brazil-cpf-validator, brazil-cnpj-validator, brazil-cep-validator]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
    sameModule: true
seo:
  title:
    en: "BRL Centavos Converter | ValidoHub"
  description:
    en: "Parse Brazilian real amounts, normalize comma decimals, convert to integer centavos, and generate storage-safe payment fields."
