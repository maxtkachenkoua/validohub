schemaVersion: 1
id: brazil-ibge-municipality-code-inspector
kind: tool
module: validohub
country: BR
category: national-identifiers
status: active
name:
  en: "IBGE Municipality Code Inspector"
summary:
  en: "Inspect seven-digit IBGE municipality codes, split UF and locality hints, and prepare geography keys for data imports."
capabilities:
  - validate
  - parse
  - generate
  - format
  - calculate
  - explain
algorithm:
  algorithmId: validohub.brazil-suite
forms:
  validate:
    inputs:
      - name: input
        type: text
        required: true
    actions:
      - validate
      - parse
      - generate
      - format
      - calculate
      - explain
related:
  explicit: [brazil-pix-validator, brazil-cpf-validator, brazil-cnpj-validator, brazil-cep-validator]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
    sameModule: true
seo:
  title:
    en: "IBGE Municipality Code Inspector | ValidoHub"
  description:
    en: "Inspect seven-digit IBGE municipality codes, split UF and locality hints, and prepare geography keys for data imports."
