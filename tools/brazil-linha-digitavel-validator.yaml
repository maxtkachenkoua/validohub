schemaVersion: 1
id: brazil-linha-digitavel-validator
kind: tool
module: validohub
country: BR
category: finance
status: active
name:
  en: "Linha Digitável Validator"
summary:
  en: "Validate boleto linha digitável fields, normalize punctuation, replay field-level check digits, and mask payment references."
capabilities:
  - validate
  - parse
  - generate
  - format
  - calculate
  - explain
algorithm:
  algorithmId: validohub.brazil-suite
forms:
  validate:
    inputs:
      - name: input
        type: text
        required: true
    actions:
      - validate
      - parse
      - generate
      - format
      - calculate
      - explain
related:
  explicit: [brazil-pix-validator, brazil-cpf-validator, brazil-cnpj-validator, brazil-cep-validator]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
    sameModule: true
seo:
  title:
    en: "Linha Digitável Validator | ValidoHub"
  description:
    en: "Validate boleto linha digitável fields, normalize punctuation, replay field-level check digits, and mask payment references."
