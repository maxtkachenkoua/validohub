schemaVersion: 1
id: brazil-municipal-registration-im-helper
kind: tool
module: validohub
country: BR
category: national-identifiers
status: active
name:
  en: "Inscrição Municipal Helper"
summary:
  en: "Normalize municipal-registration references, identify city context fields, and prepare invoice-safe examples without claiming city status lookup."
capabilities:
  - validate
  - parse
  - generate
  - format
  - calculate
  - explain
algorithm:
  algorithmId: validohub.brazil-suite
forms:
  validate:
    inputs:
      - name: input
        type: text
        required: true
    actions:
      - validate
      - parse
      - generate
      - format
      - calculate
      - explain
related:
  explicit: [brazil-pix-validator, brazil-cpf-validator, brazil-cnpj-validator, brazil-cep-validator]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
    sameModule: true
seo:
  title:
    en: "Inscrição Municipal Helper | ValidoHub"
  description:
    en: "Normalize municipal-registration references, identify city context fields, and prepare invoice-safe examples without claiming city status lookup."
