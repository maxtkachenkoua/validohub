schemaVersion: 1
id: brazil-passport-number-helper
kind: tool
module: validohub
country: BR
category: national-identifiers
status: active
name:
  en: "Brazil Passport Number Helper"
summary:
  en: "Normalize Brazilian passport-like document input, mask travel references, and prepare fictional identity document fixtures."
capabilities:
  - validate
  - parse
  - generate
  - format
  - calculate
  - explain
algorithm:
  algorithmId: validohub.brazil-suite
forms:
  validate:
    inputs:
      - name: input
        type: text
        required: true
    actions:
      - validate
      - parse
      - generate
      - format
      - calculate
      - explain
related:
  explicit: [brazil-pix-validator, brazil-cpf-validator, brazil-cnpj-validator, brazil-cep-validator]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
    sameModule: true
seo:
  title:
    en: "Brazil Passport Number Helper | ValidoHub"
  description:
    en: "Normalize Brazilian passport-like document input, mask travel references, and prepare fictional identity document fixtures."
