schemaVersion: 1
id: brazil-pix-qr-payload-generator
kind: tool
module: validohub
country: BR
category: finance
status: active
name:
  en: "Pix QR Payload Generator"
summary:
  en: "Build Pix QR payload test strings from key, recipient, amount, city, and transaction ID fields while staying fully browser-only."
capabilities:
  - validate
  - parse
  - generate
  - format
  - calculate
  - explain
algorithm:
  algorithmId: validohub.brazil-suite
forms:
  validate:
    inputs:
      - name: input
        type: text
        required: true
    actions:
      - validate
      - parse
      - generate
      - format
      - calculate
      - explain
related:
  explicit: [brazil-pix-validator, brazil-cpf-validator, brazil-cnpj-validator, brazil-cep-validator]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
    sameModule: true
seo:
  title:
    en: "Pix QR Payload Generator | ValidoHub"
  description:
    en: "Build Pix QR payload test strings from key, recipient, amount, city, and transaction ID fields while staying fully browser-only."
