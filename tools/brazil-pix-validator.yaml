schemaVersion: 1
id: brazil-pix-validator
kind: tool
module: validohub
country: BR
category: finance
status: active
name:
  en: "Brazil Pix Validator"
summary:
  en: "Validate Pix keys and BR Code payloads, inspect EMV fields, generate QR-friendly payment data, and explain offline payment boundaries."
capabilities:
  - validate
  - parse
  - generate
  - explain
algorithm:
  algorithmId: validohub.brazil-pix
forms:
  validate:
    inputs:
      - name: input
        type: text
        required: true
    actions:
      - validate
      - parse
      - generate
      - explain
related:
  explicit: [brazil-cpf-validator, brazil-cnpj-validator, brazil-cep-validator, brazil-boleto-barcode-validator]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
    sameModule: true
seo:
  title:
    en: "Brazil Pix Validator | ValidoHub"
  description:
    en: "Validate Pix keys and BR Code payloads, inspect EMV fields, generate QR-friendly payment data, and explain offline payment boundaries."
