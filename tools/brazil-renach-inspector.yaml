schemaVersion: 1
id: brazil-renach-inspector
kind: tool
module: validohub
country: BR
category: national-identifiers
status: active
name:
  en: "RENACH Number Inspector"
summary:
  en: "Normalize RENACH driver-record references, check common document shape, mask values, and prepare transport onboarding fixtures."
capabilities:
  - validate
  - parse
  - generate
  - format
  - calculate
  - explain
algorithm:
  algorithmId: validohub.brazil-suite
forms:
  validate:
    inputs:
      - name: input
        type: text
        required: true
    actions:
      - validate
      - parse
      - generate
      - format
      - calculate
      - explain
related:
  explicit: [brazil-pix-validator, brazil-cpf-validator, brazil-cnpj-validator, brazil-cep-validator]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
    sameModule: true
seo:
  title:
    en: "RENACH Number Inspector | ValidoHub"
  description:
    en: "Normalize RENACH driver-record references, check common document shape, mask values, and prepare transport onboarding fixtures."
