schemaVersion: 1
id: brazil-renavam-validator
kind: tool
module: validohub
country: BR
category: national-identifiers
status: active
name:
  en: "RENAVAM Vehicle Registry Validator"
summary:
  en: "Validate RENAVAM-shaped vehicle registry numbers, inspect length and check-digit behavior where possible, and create safe vehicle fixtures."
capabilities:
  - validate
  - parse
  - generate
  - format
  - calculate
  - explain
algorithm:
  algorithmId: validohub.brazil-suite
forms:
  validate:
    inputs:
      - name: input
        type: text
        required: true
    actions:
      - validate
      - parse
      - generate
      - format
      - calculate
      - explain
related:
  explicit: [brazil-pix-validator, brazil-cpf-validator, brazil-cnpj-validator, brazil-cep-validator]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
    sameModule: true
seo:
  title:
    en: "RENAVAM Vehicle Registry Validator | ValidoHub"
  description:
    en: "Validate RENAVAM-shaped vehicle registry numbers, inspect length and check-digit behavior where possible, and create safe vehicle fixtures."
