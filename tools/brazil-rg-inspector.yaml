schemaVersion: 1
id: brazil-rg-inspector
kind: tool
module: validohub
country: BR
category: national-identifiers
status: active
name:
  en: "RG Number Inspector"
summary:
  en: "Inspect RG-shaped identity numbers, normalize issuer notation, mask document values, and document state-level offline limits."
capabilities:
  - validate
  - parse
  - generate
  - format
  - calculate
  - explain
algorithm:
  algorithmId: validohub.brazil-suite
forms:
  validate:
    inputs:
      - name: input
        type: text
        required: true
    actions:
      - validate
      - parse
      - generate
      - format
      - calculate
      - explain
related:
  explicit: [brazil-pix-validator, brazil-cpf-validator, brazil-cnpj-validator, brazil-cep-validator]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
    sameModule: true
seo:
  title:
    en: "RG Number Inspector | ValidoHub"
  description:
    en: "Inspect RG-shaped identity numbers, normalize issuer notation, mask document values, and document state-level offline limits."
