schemaVersion: 1
id: brazil-simples-nacional-das-helper
kind: tool
module: validohub
country: BR
category: finance
status: active
name:
  en: "Simples Nacional DAS Helper"
summary:
  en: "Prepare Simples Nacional DAS references, normalize period and CNPJ inputs, and explain what browser-only checks cannot confirm."
capabilities:
  - validate
  - parse
  - generate
  - format
  - calculate
  - explain
algorithm:
  algorithmId: validohub.brazil-suite
forms:
  validate:
    inputs:
      - name: input
        type: text
        required: true
    actions:
      - validate
      - parse
      - generate
      - format
      - calculate
      - explain
related:
  explicit: [brazil-pix-validator, brazil-cpf-validator, brazil-cnpj-validator, brazil-cep-validator]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
    sameModule: true
seo:
  title:
    en: "Simples Nacional DAS Helper | ValidoHub"
  description:
    en: "Prepare Simples Nacional DAS references, normalize period and CNPJ inputs, and explain what browser-only checks cannot confirm."
