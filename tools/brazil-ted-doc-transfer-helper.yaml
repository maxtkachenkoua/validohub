schemaVersion: 1
id: brazil-ted-doc-transfer-helper
kind: tool
module: validohub
country: BR
category: finance
status: active
name:
  en: "TED / DOC Transfer Helper"
summary:
  en: "Check Brazilian transfer payload fields for bank code, agency, account, CPF/CNPJ, amount, and recipient consistency before handoff."
capabilities:
  - validate
  - parse
  - generate
  - format
  - calculate
  - explain
algorithm:
  algorithmId: validohub.brazil-suite
forms:
  validate:
    inputs:
      - name: input
        type: text
        required: true
    actions:
      - validate
      - parse
      - generate
      - format
      - calculate
      - explain
related:
  explicit: [brazil-pix-validator, brazil-cpf-validator, brazil-cnpj-validator, brazil-cep-validator]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
    sameModule: true
seo:
  title:
    en: "TED / DOC Transfer Helper | ValidoHub"
  description:
    en: "Check Brazilian transfer payload fields for bank code, agency, account, CPF/CNPJ, amount, and recipient consistency before handoff."
