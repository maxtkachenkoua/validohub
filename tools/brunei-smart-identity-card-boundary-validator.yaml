schemaVersion: 1
id: brunei-smart-identity-card-boundary-validator
kind: tool
module: validohub
country: BN
category: national-identifiers
status: active
name:
  en: "Bruneian smart identity card boundary Validator"
summary:
  en: "Validate smart identity card boundary shape, split date/control/body evidence, and prepare privacy-safe debugging output."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.brunei-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ID input"
        required: true
        placeholder:
          en: "00-123456"
    actions: [validate, parse, format, explain]
related:
  explicit: [brunei-roc-number-validator, brunei-tax-id-validator]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Bruneian smart identity card boundary Validator | ValidoHub"
  description:
    en: "Validate smart identity card boundary shape, split date/control/body evidence, and prepare privacy-safe debugging output."
