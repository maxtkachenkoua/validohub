schemaVersion: 1
id: bulgaria-address-transliteration-normalizer
kind: tool
module: validohub
country: BG
category: address
status: active
name:
  en: "Bulgarian Address Transliteration Normalizer"
summary:
  en: "Prepare ASCII-safe address keys while preserving local display text and search/debug evidence."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.bulgaria-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ASCII input"
        required: true
        placeholder:
          en: "Vitosha Blvd 1, 1000 Sofia"
    actions: [validate, parse, format, explain]
related:
  explicit: [bulgaria-region-code-mapper, bulgaria-municipality-code-inspector]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Bulgarian Address Transliteration Normalizer | ValidoHub"
  description:
    en: "Prepare ASCII-safe address keys while preserving local display text and search/debug evidence."
