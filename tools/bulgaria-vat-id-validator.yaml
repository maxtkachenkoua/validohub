schemaVersion: 1
id: bulgaria-vat-id-validator
kind: tool
module: validohub
country: BG
category: national-identifiers
status: active
name:
  en: "Bulgarian VAT ID / BG Prefix Validator"
summary:
  en: "Normalize BG VAT identifiers, inspect local tax body evidence, and prepare VIES handoff diagnostics."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.bulgaria-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "VAT input"
        required: true
        placeholder:
          en: "BG123456789"
    actions: [validate, parse, format, explain]
related:
  explicit: [bulgaria-eori-validator, bulgaria-lnch-social-insurance-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Bulgarian VAT ID / BG Prefix Validator | ValidoHub"
  description:
    en: "Normalize BG VAT identifiers, inspect local tax body evidence, and prepare VIES handoff diagnostics."
