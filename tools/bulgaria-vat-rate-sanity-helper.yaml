schemaVersion: 1
id: bulgaria-vat-rate-sanity-helper
kind: tool
module: validohub
country: BG
category: tax
status: active
name:
  en: "Bulgarian VAT Rate Sanity Helper"
summary:
  en: "Inspect VAT-rate snippets, local tax labels, amount bases, and official rate lookup boundaries."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.bulgaria-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "RATE input"
        required: true
        placeholder:
          en: "DDS / VAT 20% base 1 234,56 BGN"
    actions: [validate, parse, format, explain]
related:
  explicit: [bulgaria-vat-return-field-helper, bulgaria-invoice-number-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Bulgarian VAT Rate Sanity Helper | ValidoHub"
  description:
    en: "Inspect VAT-rate snippets, local tax labels, amount bases, and official rate lookup boundaries."
