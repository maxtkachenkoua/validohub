schemaVersion: 1
id: burkina-faso-address-transliteration-normalizer
kind: tool
module: validohub
country: BF
category: address
status: active
name:
  en: "Burkinabe Address Transliteration Normalizer"
summary:
  en: "Prepare ASCII-safe address keys while preserving local display text and search/debug evidence."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.burkina-faso-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ASCII input"
        required: true
        placeholder:
          en: "Central business district, Ouagadougou 1000"
    actions: [validate, parse, format, explain]
related:
  explicit: [burkina-faso-region-code-mapper, burkina-faso-municipality-code-inspector]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Burkinabe Address Transliteration Normalizer | ValidoHub"
  description:
    en: "Prepare ASCII-safe address keys while preserving local display text and search/debug evidence."
