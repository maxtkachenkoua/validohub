schemaVersion: 1
id: burundi-currency-decimal-formatter
kind: tool
module: validohub
country: BI
category: finance
status: active
name:
  en: "Burundian BIF Decimal Currency Formatter"
summary:
  en: "Normalize BIF amount strings, decimal separators, grouping, and API-safe numeric previews."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.burundi-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "CUR input"
        required: true
        placeholder:
          en: "1,234.56 BIF"
    actions: [validate, parse, format, explain]
related:
  explicit: [burundi-tax-rate-sanity-helper, burundi-tax-return-field-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Burundian BIF Decimal Currency Formatter | ValidoHub"
  description:
    en: "Normalize BIF amount strings, decimal separators, grouping, and API-safe numeric previews."
