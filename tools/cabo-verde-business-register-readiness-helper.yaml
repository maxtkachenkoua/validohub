schemaVersion: 1
id: cabo-verde-business-register-readiness-helper
kind: tool
module: validohub
country: CV
category: government
status: active
name:
  en: "Cabo Verdean business registry Readiness Helper"
summary:
  en: "Prepare browser-only evidence before a regulated business registry lookup or company registry workflow."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.cabo-verde-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "REG input"
        required: true
        placeholder:
          en: "CV-REG-123456 CV123456789 Central business district, Praia 1000"
    actions: [validate, parse, format, explain]
related:
  explicit: [cabo-verde-id-card-format-helper, cabo-verde-passport-number-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Cabo Verdean business registry Readiness Helper | ValidoHub"
  description:
    en: "Prepare browser-only evidence before a regulated business registry lookup or company registry workflow."
