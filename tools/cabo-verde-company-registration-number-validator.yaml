schemaVersion: 1
id: cabo-verde-company-registration-number-validator
kind: tool
module: validohub
country: CV
category: national-identifiers
status: active
name:
  en: "Cabo Verdean company registration number Validator"
summary:
  en: "Inspect company registration number structure, registry-style prefixes, control digits, and official lookup boundaries."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.cabo-verde-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ORG input"
        required: true
        placeholder:
          en: "CV-REG-123456"
    actions: [validate, parse, format, explain]
related:
  explicit: [cabo-verde-tax-id-validator, cabo-verde-customs-importer-code-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Cabo Verdean company registration number Validator | ValidoHub"
  description:
    en: "Inspect company registration number structure, registry-style prefixes, control digits, and official lookup boundaries."
