schemaVersion: 1
id: cabo-verde-tax-id-validator
kind: tool
module: validohub
country: CV
category: national-identifiers
status: active
name:
  en: "Cabo Verdean tax number / VAT boundary Validator"
summary:
  en: "Normalize tax number / VAT boundary identifiers, inspect local tax body evidence, and prepare authority handoff diagnostics."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.cabo-verde-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "TAX input"
        required: true
        placeholder:
          en: "CV123456789"
    actions: [validate, parse, format, explain]
related:
  explicit: [cabo-verde-customs-importer-code-helper, cabo-verde-social-security-boundary-social-insurance-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Cabo Verdean tax number / VAT boundary Validator | ValidoHub"
  description:
    en: "Normalize tax number / VAT boundary identifiers, inspect local tax body evidence, and prepare authority handoff diagnostics."
