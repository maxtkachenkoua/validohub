schemaVersion: 1
id: cameroon-currency-decimal-formatter
kind: tool
module: validohub
country: CM
category: finance
status: active
name:
  en: "Cameroonian XAF Decimal Currency Formatter"
summary:
  en: "Normalize XAF amount strings, decimal separators, grouping, and API-safe numeric previews."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.cameroon-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "CUR input"
        required: true
        placeholder:
          en: "1,234.56 XAF"
    actions: [validate, parse, format, explain]
related:
  explicit: [cameroon-tax-rate-sanity-helper, cameroon-tax-return-field-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Cameroonian XAF Decimal Currency Formatter | ValidoHub"
  description:
    en: "Normalize XAF amount strings, decimal separators, grouping, and API-safe numeric previews."
