schemaVersion: 1
id: chile-currency-decimal-formatter
kind: tool
module: validohub
country: CL
category: finance
status: active
name:
  en: "Chilean CLP Decimal Currency Formatter"
summary:
  en: "Normalize CLP amount strings, decimal separators, grouping, and API-safe numeric previews."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.chile-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "CUR input"
        required: true
        placeholder:
          en: "1.234 CLP"
    actions: [validate, parse, format, explain]
related:
  explicit: [chile-vat-rate-sanity-helper, chile-vat-return-field-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Chilean CLP Decimal Currency Formatter | ValidoHub"
  description:
    en: "Normalize CLP amount strings, decimal separators, grouping, and API-safe numeric previews."
