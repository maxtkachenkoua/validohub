schemaVersion: 1
id: chile-rut-validator
kind: tool
module: validohub
country: CL
category: national-identifiers
status: active
name:
  en: "Chilean RUT Validator"
summary:
  en: "Inspect RUT structure, registry-style prefixes, control digits, and official lookup boundaries."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.chile-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ORG input"
        required: true
        placeholder:
          en: "76.123.456-7"
    actions: [validate, parse, format, explain]
related:
  explicit: [chile-vat-id-validator, chile-customs-tax-identifier-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Chilean RUT Validator | ValidoHub"
  description:
    en: "Inspect RUT structure, registry-style prefixes, control digits, and official lookup boundaries."
