schemaVersion: 1
id: chile-vat-id-validator
kind: tool
module: validohub
country: CL
category: national-identifiers
status: active
name:
  en: "Chilean IVA / RUT Tax ID Validator"
summary:
  en: "Normalize IVA / RUT identifiers, inspect local tax body evidence, and prepare tax-authority handoff diagnostics."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.chile-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "TAX input"
        required: true
        placeholder:
          en: "CL 76.123.456-7"
    actions: [validate, parse, format, explain]
related:
  explicit: [chile-customs-tax-identifier-helper, chile-run-social-insurance-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Chilean IVA / RUT Tax ID Validator | ValidoHub"
  description:
    en: "Normalize IVA / RUT identifiers, inspect local tax body evidence, and prepare tax-authority handoff diagnostics."
