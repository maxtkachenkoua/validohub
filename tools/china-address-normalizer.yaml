schemaVersion: 1
id: china-address-normalizer
kind: tool
module: validohub
country: CN
category: address
status: active
name:
  en: "Chinese Address Normalizer"
summary:
  en: "Normalize street, postal code, locality, region, and country lines for local address forms."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.china-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ADDR input"
        required: true
        placeholder:
          en: "1 Chang An Avenue, Beijing 100000"
    actions: [validate, parse, format, explain]
related:
  explicit: [china-address-transliteration-normalizer, china-region-code-mapper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Chinese Address Normalizer | ValidoHub"
  description:
    en: "Normalize street, postal code, locality, region, and country lines for local address forms."
