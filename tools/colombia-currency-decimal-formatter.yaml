schemaVersion: 1
id: colombia-currency-decimal-formatter
kind: tool
module: validohub
country: CO
category: finance
status: active
name:
  en: "Colombian COP Decimal Currency Formatter"
summary:
  en: "Normalize COP amount strings, decimal separators, grouping, and API-safe numeric previews."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.colombia-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "CUR input"
        required: true
        placeholder:
          en: "1.234,56 COP"
    actions: [validate, parse, format, explain]
related:
  explicit: [colombia-vat-rate-sanity-helper, colombia-vat-return-field-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Colombian COP Decimal Currency Formatter | ValidoHub"
  description:
    en: "Normalize COP amount strings, decimal separators, grouping, and API-safe numeric previews."
