schemaVersion: 1
id: colombia-nit-validator
kind: tool
module: validohub
country: CO
category: national-identifiers
status: active
name:
  en: "Colombian NIT Validator"
summary:
  en: "Inspect NIT structure, registry-style prefixes, control digits, and official lookup boundaries."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.colombia-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ORG input"
        required: true
        placeholder:
          en: "900.123.456-7"
    actions: [validate, parse, format, explain]
related:
  explicit: [colombia-vat-id-validator, colombia-customs-tax-identifier-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Colombian NIT Validator | ValidoHub"
  description:
    en: "Inspect NIT structure, registry-style prefixes, control digits, and official lookup boundaries."
