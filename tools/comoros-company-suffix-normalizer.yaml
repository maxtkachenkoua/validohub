schemaVersion: 1
id: comoros-company-suffix-normalizer
kind: tool
module: validohub
country: KM
category: developer-tools
status: active
name:
  en: "Comorian Company Suffix Normalizer"
summary:
  en: "Normalize local company suffixes, registry labels, and search-key variants for onboarding forms."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.comoros-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "SUFFIX input"
        required: true
        placeholder:
          en: "Comoros Sample Holding Ltd KM-REG-123456"
    actions: [validate, parse, format, explain]
related:
  explicit: [comoros-procurement-identifier-helper, comoros-accessibility-locale-copy-checker]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Comorian Company Suffix Normalizer | ValidoHub"
  description:
    en: "Normalize local company suffixes, registry labels, and search-key variants for onboarding forms."
