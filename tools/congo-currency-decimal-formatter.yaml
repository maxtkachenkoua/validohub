schemaVersion: 1
id: congo-currency-decimal-formatter
kind: tool
module: validohub
country: CG
category: finance
status: active
name:
  en: "Congolese XAF Decimal Currency Formatter"
summary:
  en: "Normalize XAF amount strings, decimal separators, grouping, and API-safe numeric previews."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.congo-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "CUR input"
        required: true
        placeholder:
          en: "1,234.56 XAF"
    actions: [validate, parse, format, explain]
related:
  explicit: [congo-tax-rate-sanity-helper, congo-tax-return-field-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Congolese XAF Decimal Currency Formatter | ValidoHub"
  description:
    en: "Normalize XAF amount strings, decimal separators, grouping, and API-safe numeric previews."
