schemaVersion: 1
id: croatia-address-normalizer
kind: tool
module: validohub
country: HR
category: address
status: active
name:
  en: "Croatian Address Normalizer"
summary:
  en: "Normalize street, postal code, locality, region, and country lines for local address forms."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.croatia-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ADDR input"
        required: true
        placeholder:
          en: "Ilica 1, 10000 Zagreb"
    actions: [validate, parse, format, explain]
related:
  explicit: [croatia-address-transliteration-normalizer, croatia-region-code-mapper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Croatian Address Normalizer | ValidoHub"
  description:
    en: "Normalize street, postal code, locality, region, and country lines for local address forms."
