schemaVersion: 1
id: croatia-mbs-oib-validator
kind: tool
module: validohub
country: HR
category: national-identifiers
status: active
name:
  en: "Croatian MBS / OIB Validator"
summary:
  en: "Inspect MBS / OIB structure, registry-style prefixes, control digits, and official lookup boundaries."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.croatia-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ORG input"
        required: true
        placeholder:
          en: "080012345"
    actions: [validate, parse, format, explain]
related:
  explicit: [croatia-vat-id-validator, croatia-eori-validator]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Croatian MBS / OIB Validator | ValidoHub"
  description:
    en: "Inspect MBS / OIB structure, registry-style prefixes, control digits, and official lookup boundaries."
