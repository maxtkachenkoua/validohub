schemaVersion: 1
id: croatia-regex-pack-helper
kind: tool
module: validohub
country: HR
category: developer-tools
status: active
name:
  en: "Croatian Regex Pack Helper"
summary:
  en: "Prepare regex snippets for local identifiers, VAT, IBAN, postal, phone, dates, and debug labels."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.croatia-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "REGEX input"
        required: true
        placeholder:
          en: "OIB MBS / OIB postanski broj  HR1210010051863000160"
    actions: [validate, parse, format, explain]
related:
  explicit: [croatia-api-payload-auditor, croatia-form-field-auditor]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Croatian Regex Pack Helper | ValidoHub"
  description:
    en: "Prepare regex snippets for local identifiers, VAT, IBAN, postal, phone, dates, and debug labels."
