schemaVersion: 1
id: currency-minor-units-checker
kind: tool
module: validohub
category: developer-tools
status: active
name:
  en: Currency Minor Units Checker
summary:
  en: Inspect currency minor units, decimal precision, integer amount conversion, zero-decimal currencies, and API-safe money payload shape.
capabilities: [validate, generate, explain, parse]
algorithm:
  algorithmId: validohub.currency-minor-units
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: Money payload or amount list
      - type: select
        name: profile
        label:
          en: Profile
        options:
          - payments-fixtures
          - strict
          - review
        default: "payments-fixtures"
    actions: [validate, generate, explain, parse]
related:
  explicit: [sepa-pain001-fixture-helper, payment-reference-generator, password-policy-tester, json-schema-workbench]
  auto:
    sameCountry: false
    sameCategory: true
    sameCapability: true
    sameModule: true
seo:
  title:
    en: Currency Minor Units Checker
  description:
    en: Inspect currency minor units, decimal precision, integer amount conversion, zero-decimal currencies, and API-safe money payload shape.
