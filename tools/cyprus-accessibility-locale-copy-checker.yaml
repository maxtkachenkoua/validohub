schemaVersion: 1
id: cyprus-accessibility-locale-copy-checker
kind: tool
module: validohub
country: CY
category: localization
status: active
name:
  en: "Cypriot Locale Copy Checker"
summary:
  en: "Check UI labels for local identifier names, date/currency wording, and support-safe explanations."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.cyprus-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "COPY input"
        required: true
        placeholder:
          en: "Civil ID input, postal code input, amount 1.234,56 EUR"
    actions: [validate, parse, format, explain]
related:
  explicit: [cyprus-support-ticket-scrubber, cyprus-integration-smoke-test-builder]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Cypriot Locale Copy Checker | ValidoHub"
  description:
    en: "Check UI labels for local identifier names, date/currency wording, and support-safe explanations."
