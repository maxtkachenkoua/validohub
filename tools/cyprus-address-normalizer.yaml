schemaVersion: 1
id: cyprus-address-normalizer
kind: tool
module: validohub
country: CY
category: address
status: active
name:
  en: "Cypriot Address Normalizer"
summary:
  en: "Normalize street, postal code, locality, region, and country lines for local address forms."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.cyprus-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ADDR input"
        required: true
        placeholder:
          en: "Ledras 1, 1010 Nicosia"
    actions: [validate, parse, format, explain]
related:
  explicit: [cyprus-address-transliteration-normalizer, cyprus-region-code-mapper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Cypriot Address Normalizer | ValidoHub"
  description:
    en: "Normalize street, postal code, locality, region, and country lines for local address forms."
