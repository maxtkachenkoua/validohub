schemaVersion: 1
id: cyprus-address-transliteration-normalizer
kind: tool
module: validohub
country: CY
category: address
status: active
name:
  en: "Cypriot Address Transliteration Normalizer"
summary:
  en: "Prepare ASCII-safe address keys while preserving local display text and search/debug evidence."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.cyprus-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ASCII input"
        required: true
        placeholder:
          en: "Ledras 1, 1010 Nicosia"
    actions: [validate, parse, format, explain]
related:
  explicit: [cyprus-region-code-mapper, cyprus-municipality-code-inspector]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Cypriot Address Transliteration Normalizer | ValidoHub"
  description:
    en: "Prepare ASCII-safe address keys while preserving local display text and search/debug evidence."
