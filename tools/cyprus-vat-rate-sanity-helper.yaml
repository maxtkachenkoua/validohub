schemaVersion: 1
id: cyprus-vat-rate-sanity-helper
kind: tool
module: validohub
country: CY
category: tax
status: active
name:
  en: "Cypriot VAT Rate Sanity Helper"
summary:
  en: "Inspect VAT-rate snippets, local tax labels, amount bases, and official rate lookup boundaries."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.cyprus-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "RATE input"
        required: true
        placeholder:
          en: "VAT 20% base 1.234,56 EUR"
    actions: [validate, parse, format, explain]
related:
  explicit: [cyprus-vat-return-field-helper, cyprus-invoice-number-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Cypriot VAT Rate Sanity Helper | ValidoHub"
  description:
    en: "Inspect VAT-rate snippets, local tax labels, amount bases, and official rate lookup boundaries."
