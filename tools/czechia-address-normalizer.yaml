schemaVersion: 1
id: czechia-address-normalizer
kind: tool
module: validohub
country: CZ
category: address
status: active
name:
  en: "Czech Address Normalizer"
summary:
  en: "Normalize street, postal code, locality, region, and country lines for local address forms."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.czechia-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ADDR input"
        required: true
        placeholder:
          en: "Vaclavske namesti 1, 110 00 Praha 1"
    actions: [validate, parse, format, explain]
related:
  explicit: [czechia-address-transliteration-normalizer, czechia-region-code-mapper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Czech Address Normalizer | ValidoHub"
  description:
    en: "Normalize street, postal code, locality, region, and country lines for local address forms."
