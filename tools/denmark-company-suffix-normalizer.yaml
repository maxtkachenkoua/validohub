schemaVersion: 1
id: denmark-company-suffix-normalizer
kind: tool
module: validohub
country: DK
category: developer-tools
status: active
name:
  en: "Danish Company Suffix Normalizer"
summary:
  en: "Normalize local company suffixes, registry labels, and search-key variants for onboarding forms."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.denmark-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "SUFFIX input"
        required: true
        placeholder:
          en: "Denmark Sample Holding Ltd 12345674"
    actions: [validate, parse, format, explain]
related:
  explicit: [denmark-procurement-identifier-helper, denmark-accessibility-locale-copy-checker]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Danish Company Suffix Normalizer | ValidoHub"
  description:
    en: "Normalize local company suffixes, registry labels, and search-key variants for onboarding forms."
