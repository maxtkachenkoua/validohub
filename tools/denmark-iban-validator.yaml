schemaVersion: 1
id: denmark-iban-validator
kind: tool
module: validohub
country: DK
category: finance
status: active
name:
  en: "Denmark IBAN Validator"
summary:
  en: "Validate DK IBAN shape, replay MOD-97 evidence, and split bank/account blocks for debugging."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.denmark-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "IBAN input"
        required: true
        placeholder:
          en: "DK5000400440116243"
    actions: [validate, parse, format, explain]
related:
  explicit: [denmark-iban-generator, denmark-bank-account-inspector]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Denmark IBAN Validator | ValidoHub"
  description:
    en: "Validate DK IBAN shape, replay MOD-97 evidence, and split bank/account blocks for debugging."
