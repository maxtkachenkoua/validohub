schemaVersion: 1
id: denmark-vat-id-validator
kind: tool
module: validohub
country: DK
category: national-identifiers
status: active
name:
  en: "Danish VAT ID / DK Prefix Validator"
summary:
  en: "Normalize DK VAT identifiers, inspect local tax body evidence, and prepare VIES handoff diagnostics."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.denmark-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "VAT input"
        required: true
        placeholder:
          en: "DK12345674"
    actions: [validate, parse, format, explain]
related:
  explicit: [denmark-eori-validator, denmark-cpr-social-insurance-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Danish VAT ID / DK Prefix Validator | ValidoHub"
  description:
    en: "Normalize DK VAT identifiers, inspect local tax body evidence, and prepare VIES handoff diagnostics."
