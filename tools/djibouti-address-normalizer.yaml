schemaVersion: 1
id: djibouti-address-normalizer
kind: tool
module: validohub
country: DJ
category: address
status: active
name:
  en: "Djiboutian Address Normalizer"
summary:
  en: "Normalize street, postal code, locality, region, and country lines for local address forms."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.djibouti-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ADDR input"
        required: true
        placeholder:
          en: "Central business district, Djibouti 1000"
    actions: [validate, parse, format, explain]
related:
  explicit: [djibouti-address-transliteration-normalizer, djibouti-region-code-mapper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Djiboutian Address Normalizer | ValidoHub"
  description:
    en: "Normalize street, postal code, locality, region, and country lines for local address forms."
