schemaVersion: 1
id: ecuador-debit-authorization-helper
kind: tool
module: validohub
country: EC
category: finance
status: active
name:
  en: "Ecuadorian Debit Authorization Helper"
summary:
  en: "Inspect debit authorization references, creditor data, payer account evidence, and browser-only readiness."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.ecuador-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "DEBIT input"
        required: true
        placeholder:
          en: "MANDATE-2026-001 EC-BANK-010-1234567890"
    actions: [validate, parse, format, explain]
related:
  explicit: [ecuador-payment-reference-helper, ecuador-remittance-text-builder]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Ecuadorian Debit Authorization Helper | ValidoHub"
  description:
    en: "Inspect debit authorization references, creditor data, payer account evidence, and browser-only readiness."
