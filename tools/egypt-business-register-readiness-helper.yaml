schemaVersion: 1
id: egypt-business-register-readiness-helper
kind: tool
module: validohub
country: EG
category: government
status: active
name:
  en: "Egyptian business registry Readiness Helper"
summary:
  en: "Prepare browser-only evidence before a regulated business registry lookup or company registry workflow."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.egypt-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "REG input"
        required: true
        placeholder:
          en: "EG-REG-123456 EG123456789 Central business district, Cairo 1000"
    actions: [validate, parse, format, explain]
related:
  explicit: [egypt-id-card-format-helper, egypt-passport-number-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Egyptian business registry Readiness Helper | ValidoHub"
  description:
    en: "Prepare browser-only evidence before a regulated business registry lookup or company registry workflow."
