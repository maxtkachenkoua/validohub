schemaVersion: 1
id: egypt-currency-decimal-formatter
kind: tool
module: validohub
country: EG
category: finance
status: active
name:
  en: "Egyptian EGP Decimal Currency Formatter"
summary:
  en: "Normalize EGP amount strings, decimal separators, grouping, and API-safe numeric previews."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.egypt-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "CUR input"
        required: true
        placeholder:
          en: "1,234.56 EGP"
    actions: [validate, parse, format, explain]
related:
  explicit: [egypt-tax-rate-sanity-helper, egypt-tax-return-field-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Egyptian EGP Decimal Currency Formatter | ValidoHub"
  description:
    en: "Normalize EGP amount strings, decimal separators, grouping, and API-safe numeric previews."
