schemaVersion: 1
id: egypt-masked-bank-account-formatter
kind: tool
module: validohub
country: EG
category: finance
status: active
name:
  en: "Egyptian Masked Bank Account Formatter"
summary:
  en: "Create log-safe bank-account previews while preserving routing and account-tail evidence."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.egypt-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "MASK input"
        required: true
        placeholder:
          en: "001 1234567890"
    actions: [validate, parse, format, explain]
related:
  explicit: [egypt-currency-decimal-formatter, egypt-tax-rate-sanity-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Egyptian Masked Bank Account Formatter | ValidoHub"
  description:
    en: "Create log-safe bank-account previews while preserving routing and account-tail evidence."
