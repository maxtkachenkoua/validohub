schemaVersion: 1
id: egypt-personal-data-fixture-generator
kind: tool
module: validohub
country: EG
category: privacy
status: active
name:
  en: "Egyptian Personal Data Fixture Helper"
summary:
  en: "Generate fixture-safe local identity/address/payment snippets with clear non-official boundaries."
capabilities:
  - generate
  - validate
  - parse
  - explain
algorithm:
  algorithmId: validohub.egypt-suite
forms:
  generate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "FIX input"
        required: true
        placeholder:
          en: "EG-ID-123456\\nCentral business district, Cairo 1000\\n+20 123 456 789"
    actions: [generate, validate, explain]
related:
  explicit: [egypt-driving-licence-format-helper, egypt-residence-permit-format-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Egyptian Personal Data Fixture Helper | ValidoHub"
  description:
    en: "Generate fixture-safe local identity/address/payment snippets with clear non-official boundaries."
