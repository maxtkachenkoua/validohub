schemaVersion: 1
id: equatorial-guinea-address-transliteration-normalizer
kind: tool
module: validohub
country: GQ
category: address
status: active
name:
  en: "Equatoguinean Address Transliteration Normalizer"
summary:
  en: "Prepare ASCII-safe address keys while preserving local display text and search/debug evidence."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.equatorial-guinea-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ASCII input"
        required: true
        placeholder:
          en: "Central business district, Malabo 1000"
    actions: [validate, parse, format, explain]
related:
  explicit: [equatorial-guinea-region-code-mapper, equatorial-guinea-municipality-code-inspector]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Equatoguinean Address Transliteration Normalizer | ValidoHub"
  description:
    en: "Prepare ASCII-safe address keys while preserving local display text and search/debug evidence."
