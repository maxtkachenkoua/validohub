schemaVersion: 1
id: eritrea-address-normalizer
kind: tool
module: validohub
country: ER
category: address
status: active
name:
  en: "Eritrean Address Normalizer"
summary:
  en: "Normalize street, postal code, locality, region, and country lines for local address forms."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.eritrea-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ADDR input"
        required: true
        placeholder:
          en: "Central business district, Asmara 1000"
    actions: [validate, parse, format, explain]
related:
  explicit: [eritrea-address-transliteration-normalizer, eritrea-region-code-mapper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Eritrean Address Normalizer | ValidoHub"
  description:
    en: "Normalize street, postal code, locality, region, and country lines for local address forms."
