schemaVersion: 1
id: estonia-accessibility-locale-copy-checker
kind: tool
module: validohub
country: EE
category: localization
status: active
name:
  en: "Estonian Locale Copy Checker"
summary:
  en: "Check UI labels for local identifier names, date/currency wording, and support-safe explanations."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.estonia-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "COPY input"
        required: true
        placeholder:
          en: "Isikukood input, postal code input, amount 1 234,56 EUR"
    actions: [validate, parse, format, explain]
related:
  explicit: [estonia-support-ticket-scrubber, estonia-integration-smoke-test-builder]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Estonian Locale Copy Checker | ValidoHub"
  description:
    en: "Check UI labels for local identifier names, date/currency wording, and support-safe explanations."
