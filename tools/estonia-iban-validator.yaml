schemaVersion: 1
id: estonia-iban-validator
kind: tool
module: validohub
country: EE
category: finance
status: active
name:
  en: "Estonia IBAN Validator"
summary:
  en: "Validate EE IBAN shape, replay MOD-97 evidence, and split bank/account blocks for debugging."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.estonia-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "IBAN input"
        required: true
        placeholder:
          en: "EE382200221020145685"
    actions: [validate, parse, format, explain]
related:
  explicit: [estonia-iban-generator, estonia-bank-account-inspector]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Estonia IBAN Validator | ValidoHub"
  description:
    en: "Validate EE IBAN shape, replay MOD-97 evidence, and split bank/account blocks for debugging."
