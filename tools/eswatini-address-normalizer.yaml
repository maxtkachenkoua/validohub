schemaVersion: 1
id: eswatini-address-normalizer
kind: tool
module: validohub
country: SZ
category: address
status: active
name:
  en: "Swazi Address Normalizer"
summary:
  en: "Normalize street, postal code, locality, region, and country lines for local address forms."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.eswatini-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ADDR input"
        required: true
        placeholder:
          en: "Central business district, Mbabane 1000"
    actions: [validate, parse, format, explain]
related:
  explicit: [eswatini-address-transliteration-normalizer, eswatini-region-code-mapper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Swazi Address Normalizer | ValidoHub"
  description:
    en: "Normalize street, postal code, locality, region, and country lines for local address forms."
