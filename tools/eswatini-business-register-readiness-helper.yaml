schemaVersion: 1
id: eswatini-business-register-readiness-helper
kind: tool
module: validohub
country: SZ
category: government
status: active
name:
  en: "Swazi business registry Readiness Helper"
summary:
  en: "Prepare browser-only evidence before a regulated business registry lookup or company registry workflow."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.eswatini-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "REG input"
        required: true
        placeholder:
          en: "SZ-REG-123456 SZ123456789 Central business district, Mbabane 1000"
    actions: [validate, parse, format, explain]
related:
  explicit: [eswatini-id-card-format-helper, eswatini-passport-number-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Swazi business registry Readiness Helper | ValidoHub"
  description:
    en: "Prepare browser-only evidence before a regulated business registry lookup or company registry workflow."
