schemaVersion: 1
id: eswatini-company-registration-number-validator
kind: tool
module: validohub
country: SZ
category: national-identifiers
status: active
name:
  en: "Swazi company registration number Validator"
summary:
  en: "Inspect company registration number structure, registry-style prefixes, control digits, and official lookup boundaries."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.eswatini-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ORG input"
        required: true
        placeholder:
          en: "SZ-REG-123456"
    actions: [validate, parse, format, explain]
related:
  explicit: [eswatini-tax-id-validator, eswatini-customs-importer-code-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Swazi company registration number Validator | ValidoHub"
  description:
    en: "Inspect company registration number structure, registry-style prefixes, control digits, and official lookup boundaries."
