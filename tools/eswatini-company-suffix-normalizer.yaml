schemaVersion: 1
id: eswatini-company-suffix-normalizer
kind: tool
module: validohub
country: SZ
category: developer-tools
status: active
name:
  en: "Swazi Company Suffix Normalizer"
summary:
  en: "Normalize local company suffixes, registry labels, and search-key variants for onboarding forms."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.eswatini-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "SUFFIX input"
        required: true
        placeholder:
          en: "Eswatini Sample Holding Ltd SZ-REG-123456"
    actions: [validate, parse, format, explain]
related:
  explicit: [eswatini-procurement-identifier-helper, eswatini-accessibility-locale-copy-checker]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Swazi Company Suffix Normalizer | ValidoHub"
  description:
    en: "Normalize local company suffixes, registry labels, and search-key variants for onboarding forms."
