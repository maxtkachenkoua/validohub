schemaVersion: 1
id: eswatini-id-card-format-helper
kind: tool
module: validohub
country: SZ
category: national-identifiers
status: active
name:
  en: "Swazi ID Card Format Helper"
summary:
  en: "Inspect local identity-card snippets, document numbers, dates, and privacy boundaries without proving identity."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.eswatini-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "CARD input"
        required: true
        placeholder:
          en: "SZ-ID-123456"
    actions: [validate, parse, format, explain]
related:
  explicit: [eswatini-passport-number-helper, eswatini-mrz-passport-parser]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Swazi ID Card Format Helper | ValidoHub"
  description:
    en: "Inspect local identity-card snippets, document numbers, dates, and privacy boundaries without proving identity."
