schemaVersion: 1
id: eswatini-tax-return-field-helper
kind: tool
module: validohub
country: SZ
category: tax
status: active
name:
  en: "Swazi Tax Return Field Helper"
summary:
  en: "Map tax-return field labels, tax evidence, period dates, and export-safe developer payloads."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.eswatini-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "RET input"
        required: true
        placeholder:
          en: "tax number / VAT boundary; SZ123456789; period 2026-07; 1,234.56 SZL"
    actions: [validate, parse, format, explain]
related:
  explicit: [eswatini-invoice-number-helper, eswatini-e-invoicing-readiness-checker]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Swazi Tax Return Field Helper | ValidoHub"
  description:
    en: "Map tax-return field labels, tax evidence, period dates, and export-safe developer payloads."
