schemaVersion: 1
id: ethiopia-date-locale-formatter
kind: tool
module: validohub
country: ET
category: localization
status: active
name:
  en: "Ethiopian Date Locale Formatter"
summary:
  en: "Normalize local dates, ISO previews, fiscal-period hints, and locale parsing diagnostics."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.ethiopia-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "DATE input"
        required: true
        placeholder:
          en: "24/07/2026"
    actions: [validate, parse, format, explain]
related:
  explicit: [ethiopia-csv-locale-normalizer, ethiopia-slug-normalizer]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Ethiopian Date Locale Formatter | ValidoHub"
  description:
    en: "Normalize local dates, ISO previews, fiscal-period hints, and locale parsing diagnostics."
