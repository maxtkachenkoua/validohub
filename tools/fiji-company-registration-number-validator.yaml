schemaVersion: 1
id: fiji-company-registration-number-validator
kind: tool
module: validohub
country: FJ
category: national-identifiers
status: active
name:
  en: "Fijian company registration number Validator"
summary:
  en: "Inspect company registration number structure, registry-style prefixes, control digits, and official lookup boundaries."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.fiji-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ORG input"
        required: true
        placeholder:
          en: "FJ-REG-123456"
    actions: [validate, parse, format, explain]
related:
  explicit: [fiji-tax-id-validator, fiji-customs-importer-code-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Fijian company registration number Validator | ValidoHub"
  description:
    en: "Inspect company registration number structure, registry-style prefixes, control digits, and official lookup boundaries."
