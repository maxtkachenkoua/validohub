schemaVersion: 1
id: fiji-company-suffix-normalizer
kind: tool
module: validohub
country: FJ
category: developer-tools
status: active
name:
  en: "Fijian Company Suffix Normalizer"
summary:
  en: "Normalize local company suffixes, registry labels, and search-key variants for onboarding forms."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.fiji-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "SUFFIX input"
        required: true
        placeholder:
          en: "Fiji Sample Holding Ltd FJ-REG-123456"
    actions: [validate, parse, format, explain]
related:
  explicit: [fiji-procurement-identifier-helper, fiji-accessibility-locale-copy-checker]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Fijian Company Suffix Normalizer | ValidoHub"
  description:
    en: "Normalize local company suffixes, registry labels, and search-key variants for onboarding forms."
