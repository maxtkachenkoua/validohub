schemaVersion: 1
id: finland-vat-id-validator
kind: tool
module: validohub
country: FI
category: national-identifiers
status: active
name:
  en: "Finnish VAT ID / FI Prefix Validator"
summary:
  en: "Normalize FI VAT identifiers, inspect local tax body evidence, and prepare VIES handoff diagnostics."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.finland-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "VAT input"
        required: true
        placeholder:
          en: "FI12345671"
    actions: [validate, parse, format, explain]
related:
  explicit: [finland-eori-validator, finland-hetu-social-insurance-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Finnish VAT ID / FI Prefix Validator | ValidoHub"
  description:
    en: "Normalize FI VAT identifiers, inspect local tax body evidence, and prepare VIES handoff diagnostics."
