schemaVersion: 1
id: finland-y-tunnus-validator
kind: tool
module: validohub
country: FI
category: national-identifiers
status: active
name:
  en: "Finnish Y-tunnus Validator"
summary:
  en: "Inspect Y-tunnus structure, registry-style prefixes, control digits, and official lookup boundaries."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.finland-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ORG input"
        required: true
        placeholder:
          en: "1234567-1"
    actions: [validate, parse, format, explain]
related:
  explicit: [finland-vat-id-validator, finland-eori-validator]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Finnish Y-tunnus Validator | ValidoHub"
  description:
    en: "Inspect Y-tunnus structure, registry-style prefixes, control digits, and official lookup boundaries."
