schemaVersion: 1
id: france-accent-normalizer
kind: tool
module: validohub
country: FR
category: developer-tools
status: active
name:
  en: "French Accent Normalizer"
summary:
  en: "Normalize French accented text for search keys, ASCII fallbacks, slugs, and original-display preservation."
capabilities:
  - validate
  - parse
  - generate
  - format
  - explain
algorithm:
  algorithmId: validohub.france-suite
forms:
  validate:
    inputs:
      - type: text
        name: input
        label:
          en: "ACCENT"
        required: true
        placeholder:
          en: "École supérieure d’ingénieurs à Lyon"
    actions: [validate, parse, generate, format, explain]
related:
  explicit: [france-address-transliteration-normalizer, france-date-locale-formatter, france-csv-locale-normalizer, france-decimal-currency-formatter]
seo:
  title:
    en: "French Accent Normalizer | ValidoHub"
  description:
    en: "Normalize French accented text for search keys, ASCII fallbacks, slugs, and original-display preservation."
