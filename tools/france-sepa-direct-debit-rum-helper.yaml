schemaVersion: 1
id: france-sepa-direct-debit-rum-helper
kind: tool
module: validohub
country: FR
category: finance
status: active
name:
  en: "SEPA Direct Debit RUM Helper"
summary:
  en: "Normalize French SEPA mandate references and inspect length, character set, and logging-safe masked values."
capabilities:
  - validate
  - parse
  - generate
  - format
  - explain
algorithm:
  algorithmId: validohub.france-suite
forms:
  validate:
    inputs:
      - type: text
        name: input
        label:
          en: "RUM"
        required: true
        placeholder:
          en: "RUM-2026-CLIENT-00042"
    actions: [validate, parse, generate, format, explain]
related:
  explicit: [france-sepa-transfer-helper, france-remittance-text-builder, france-payment-reconciliation-helper]
seo:
  title:
    en: "SEPA Direct Debit RUM Helper | ValidoHub"
  description:
    en: "Normalize French SEPA mandate references and inspect length, character set, and logging-safe masked values."
