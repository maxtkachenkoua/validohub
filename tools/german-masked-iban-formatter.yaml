schemaVersion: 1
id: german-masked-iban-formatter
kind: tool
module: validohub
country: DE
category: finance
status: active
name:
  en: "German Masked IBAN Formatter"
summary:
  en: "Mask German IBANs for logs, keep country/check/BLZ hints, and prepare privacy-safe banking previews."
capabilities:
  - validate
  - parse
  - format
  - explain
  - calculate
algorithm:
  algorithmId: validohub.germany-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "MIBAN input"
        required: true
        placeholder:
          en: "DE89370400440532013000"
    actions: [validate, parse, format, explain]
related:
  explicit: [german-eur-decimal-currency-formatter]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "German Masked IBAN Formatter | ValidoHub"
  description:
    en: "Mask German IBANs for logs, keep country/check/BLZ hints, and prepare privacy-safe banking previews."
