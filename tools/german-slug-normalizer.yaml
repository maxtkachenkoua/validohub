schemaVersion: 1
id: german-slug-normalizer
kind: tool
module: validohub
country: DE
category: developer-tools
status: active
name:
  en: "German Slug Normalizer"
summary:
  en: "Normalize German umlauts, ß, punctuation, and whitespace into stable ASCII slug keys."
capabilities:
  - validate
  - parse
  - format
  - explain
  - calculate
algorithm:
  algorithmId: validohub.germany-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "SLUG input"
        required: true
        placeholder:
          en: "Müllerstraße GmbH & Co. KG Berlin"
    actions: [validate, parse, format, explain]
related:
  explicit: [german-document-ocr-fixer]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "German Slug Normalizer | ValidoHub"
  description:
    en: "Normalize German umlauts, ß, punctuation, and whitespace into stable ASCII slug keys."
