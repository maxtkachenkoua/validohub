schemaVersion: 1
id: german-vat-ust-idnr-validator
kind: tool
module: validohub
country: DE
category: developer-tools
status: active
name:
  en: "German USt-IdNr / VAT Validator"
summary:
  en: "Validate German VAT display syntax, normalize DE prefixes, inspect numeric body length, and prepare VIES handoff notes."
capabilities:
  - validate
  - parse
  - format
  - explain
  - calculate
algorithm:
  algorithmId: validohub.germany-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "USt input"
        required: true
        placeholder:
          en: "DE123456789"
    actions: [validate, parse, format, explain]
related:
  explicit: [german-eori-validator]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "German USt-IdNr / VAT Validator | ValidoHub"
  description:
    en: "Validate German VAT display syntax, normalize DE prefixes, inspect numeric body length, and prepare VIES handoff notes."
