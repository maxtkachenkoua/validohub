schemaVersion: 1
id: greece-accessibility-locale-copy-checker
kind: tool
module: validohub
country: GR
category: localization
status: active
name:
  en: "Greek Locale Copy Checker"
summary:
  en: "Check UI labels for local identifier names, date/currency wording, and support-safe explanations."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.greece-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "COPY input"
        required: true
        placeholder:
          en: "AMKA input, TK postal code input, amount 1.234,56 EUR"
    actions: [validate, parse, format, explain]
related:
  explicit: [greece-support-ticket-scrubber, greece-integration-smoke-test-builder]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Greek Locale Copy Checker | ValidoHub"
  description:
    en: "Check UI labels for local identifier names, date/currency wording, and support-safe explanations."
