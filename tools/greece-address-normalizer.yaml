schemaVersion: 1
id: greece-address-normalizer
kind: tool
module: validohub
country: GR
category: address
status: active
name:
  en: "Greek Address Normalizer"
summary:
  en: "Normalize street, postal code, locality, region, and country lines for local address forms."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.greece-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ADDR input"
        required: true
        placeholder:
          en: "Ermou 1, 105 57 Athens"
    actions: [validate, parse, format, explain]
related:
  explicit: [greece-address-transliteration-normalizer, greece-region-code-mapper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Greek Address Normalizer | ValidoHub"
  description:
    en: "Normalize street, postal code, locality, region, and country lines for local address forms."
