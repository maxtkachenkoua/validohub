schemaVersion: 1
id: greece-amka-validator
kind: tool
module: validohub
country: GR
category: national-identifiers
status: active
name:
  en: "Greek AMKA Validator"
summary:
  en: "Validate AMKA shape, split date/control/body evidence, and prepare privacy-safe debugging output."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.greece-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ID input"
        required: true
        placeholder:
          en: "01018012345"
    actions: [validate, parse, format, explain]
related:
  explicit: [greece-gemi-number-validator, greece-vat-id-validator]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Greek AMKA Validator | ValidoHub"
  description:
    en: "Validate AMKA shape, split date/control/body evidence, and prepare privacy-safe debugging output."
