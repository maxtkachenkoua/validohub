schemaVersion: 1
id: greece-gemi-number-validator
kind: tool
module: validohub
country: GR
category: national-identifiers
status: active
name:
  en: "Greek GEMI number Validator"
summary:
  en: "Inspect GEMI number structure, registry-style prefixes, control digits, and official lookup boundaries."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.greece-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ORG input"
        required: true
        placeholder:
          en: "123456789000"
    actions: [validate, parse, format, explain]
related:
  explicit: [greece-vat-id-validator, greece-eori-validator]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Greek GEMI number Validator | ValidoHub"
  description:
    en: "Inspect GEMI number structure, registry-style prefixes, control digits, and official lookup boundaries."
