schemaVersion: 1
id: guinea-bissau-currency-decimal-formatter
kind: tool
module: validohub
country: GW
category: finance
status: active
name:
  en: "Bissau-Guinean XOF Decimal Currency Formatter"
summary:
  en: "Normalize XOF amount strings, decimal separators, grouping, and API-safe numeric previews."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.guinea-bissau-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "CUR input"
        required: true
        placeholder:
          en: "1,234.56 XOF"
    actions: [validate, parse, format, explain]
related:
  explicit: [guinea-bissau-tax-rate-sanity-helper, guinea-bissau-tax-return-field-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Bissau-Guinean XOF Decimal Currency Formatter | ValidoHub"
  description:
    en: "Normalize XOF amount strings, decimal separators, grouping, and API-safe numeric previews."
