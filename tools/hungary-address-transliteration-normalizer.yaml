schemaVersion: 1
id: hungary-address-transliteration-normalizer
kind: tool
module: validohub
country: HU
category: address
status: active
name:
  en: "Hungarian Address Transliteration Normalizer"
summary:
  en: "Prepare ASCII-safe address keys while preserving local display text and search/debug evidence."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.hungary-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ASCII input"
        required: true
        placeholder:
          en: "Andrassy ut 1, 1051 Budapest"
    actions: [validate, parse, format, explain]
related:
  explicit: [hungary-region-code-mapper, hungary-municipality-code-inspector]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Hungarian Address Transliteration Normalizer | ValidoHub"
  description:
    en: "Prepare ASCII-safe address keys while preserving local display text and search/debug evidence."
