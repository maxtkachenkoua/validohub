schemaVersion: 1
id: iceland-calendar-week-helper
kind: tool
module: validohub
country: IS
category: localization
status: active
name:
  en: "Icelandic Calendar Week Helper"
summary:
  en: "Inspect local date strings, week-start assumptions, fiscal-period labels, and ISO handoff values."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.iceland-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "CAL input"
        required: true
        placeholder:
          en: "21.07.2026 week 30"
    actions: [validate, parse, format, explain]
related:
  explicit: [iceland-company-suffix-normalizer, iceland-procurement-identifier-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Icelandic Calendar Week Helper | ValidoHub"
  description:
    en: "Inspect local date strings, week-start assumptions, fiscal-period labels, and ISO handoff values."
