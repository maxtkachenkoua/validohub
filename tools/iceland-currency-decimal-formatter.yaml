schemaVersion: 1
id: iceland-currency-decimal-formatter
kind: tool
module: validohub
country: IS
category: finance
status: active
name:
  en: "Icelandic ISK Decimal Currency Formatter"
summary:
  en: "Normalize ISK amount strings, decimal separators, grouping, and API-safe numeric previews."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.iceland-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "CUR input"
        required: true
        placeholder:
          en: "1.234,56 ISK"
    actions: [validate, parse, format, explain]
related:
  explicit: [iceland-vat-rate-sanity-helper, iceland-vat-return-field-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Icelandic ISK Decimal Currency Formatter | ValidoHub"
  description:
    en: "Normalize ISK amount strings, decimal separators, grouping, and API-safe numeric previews."
