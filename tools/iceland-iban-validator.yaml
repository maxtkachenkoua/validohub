schemaVersion: 1
id: iceland-iban-validator
kind: tool
module: validohub
country: IS
category: finance
status: active
name:
  en: "Iceland IBAN Validator"
summary:
  en: "Validate IS IBAN shape, replay MOD-97 evidence, and split bank/account blocks for debugging."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.iceland-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "IBAN input"
        required: true
        placeholder:
          en: "IS140159260076545510730339"
    actions: [validate, parse, format, explain]
related:
  explicit: [iceland-iban-generator, iceland-bank-account-inspector]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Iceland IBAN Validator | ValidoHub"
  description:
    en: "Validate IS IBAN shape, replay MOD-97 evidence, and split bank/account blocks for debugging."
