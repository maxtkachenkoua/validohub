schemaVersion: 1
id: iceland-vat-id-validator
kind: tool
module: validohub
country: IS
category: national-identifiers
status: active
name:
  en: "Icelandic VAT ID / IS Prefix Validator"
summary:
  en: "Normalize IS VAT identifiers, inspect local tax body evidence, and prepare VIES handoff diagnostics."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.iceland-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "VAT input"
        required: true
        placeholder:
          en: "IS5501692829"
    actions: [validate, parse, format, explain]
related:
  explicit: [iceland-eori-validator, iceland-kennitala-social-insurance-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Icelandic VAT ID / IS Prefix Validator | ValidoHub"
  description:
    en: "Normalize IS VAT identifiers, inspect local tax body evidence, and prepare VIES handoff diagnostics."
