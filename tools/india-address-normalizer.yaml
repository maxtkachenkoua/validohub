schemaVersion: 1
id: india-address-normalizer
kind: tool
module: validohub
country: IN
category: address
status: active
name:
  en: "Indian Address Normalizer"
summary:
  en: "Normalize street, postal code, locality, region, and country lines for local address forms."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.india-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ADDR input"
        required: true
        placeholder:
          en: "Connaught Place, New Delhi 110001"
    actions: [validate, parse, format, explain]
related:
  explicit: [india-address-transliteration-normalizer, india-region-code-mapper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Indian Address Normalizer | ValidoHub"
  description:
    en: "Normalize street, postal code, locality, region, and country lines for local address forms."
