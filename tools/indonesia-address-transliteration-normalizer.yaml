schemaVersion: 1
id: indonesia-address-transliteration-normalizer
kind: tool
module: validohub
country: ID
category: address
status: active
name:
  en: "Indonesian Address Transliteration Normalizer"
summary:
  en: "Prepare ASCII-safe address keys while preserving local display text and search/debug evidence."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.indonesia-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ASCII input"
        required: true
        placeholder:
          en: "Jl. Thamrin 1, Jakarta 10110"
    actions: [validate, parse, format, explain]
related:
  explicit: [indonesia-region-code-mapper, indonesia-municipality-code-inspector]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Indonesian Address Transliteration Normalizer | ValidoHub"
  description:
    en: "Prepare ASCII-safe address keys while preserving local display text and search/debug evidence."
