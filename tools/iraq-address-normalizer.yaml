schemaVersion: 1
id: iraq-address-normalizer
kind: tool
module: validohub
country: IQ
category: address
status: active
name:
  en: "Iraqi Address Normalizer"
summary:
  en: "Normalize street, postal code, locality, region, and country lines for local address forms."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.iraq-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ADDR input"
        required: true
        placeholder:
          en: "Karrada 1, Baghdad 10001"
    actions: [validate, parse, format, explain]
related:
  explicit: [iraq-address-transliteration-normalizer, iraq-region-code-mapper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Iraqi Address Normalizer | ValidoHub"
  description:
    en: "Normalize street, postal code, locality, region, and country lines for local address forms."
