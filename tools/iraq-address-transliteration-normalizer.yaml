schemaVersion: 1
id: iraq-address-transliteration-normalizer
kind: tool
module: validohub
country: IQ
category: address
status: active
name:
  en: "Iraqi Address Transliteration Normalizer"
summary:
  en: "Prepare ASCII-safe address keys while preserving local display text and search/debug evidence."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.iraq-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ASCII input"
        required: true
        placeholder:
          en: "Karrada 1, Baghdad 10001"
    actions: [validate, parse, format, explain]
related:
  explicit: [iraq-region-code-mapper, iraq-municipality-code-inspector]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Iraqi Address Transliteration Normalizer | ValidoHub"
  description:
    en: "Prepare ASCII-safe address keys while preserving local display text and search/debug evidence."
