schemaVersion: 1
id: iraq-currency-decimal-formatter
kind: tool
module: validohub
country: IQ
category: finance
status: active
name:
  en: "Iraqi IQD Decimal Currency Formatter"
summary:
  en: "Normalize IQD amount strings, decimal separators, grouping, and API-safe numeric previews."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.iraq-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "CUR input"
        required: true
        placeholder:
          en: "1,234,567 IQD"
    actions: [validate, parse, format, explain]
related:
  explicit: [iraq-tax-rate-sanity-helper, iraq-tax-return-field-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Iraqi IQD Decimal Currency Formatter | ValidoHub"
  description:
    en: "Normalize IQD amount strings, decimal separators, grouping, and API-safe numeric previews."
