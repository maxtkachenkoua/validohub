schemaVersion: 1
id: iraq-national-card-boundary-validator
kind: tool
module: validohub
country: IQ
category: national-identifiers
status: active
name:
  en: "Iraqi national card boundary Validator"
summary:
  en: "Validate national card boundary shape, split date/control/body evidence, and prepare privacy-safe debugging output."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.iraq-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ID input"
        required: true
        placeholder:
          en: "123456789012"
    actions: [validate, parse, format, explain]
related:
  explicit: [iraq-company-registration-number-validator, iraq-tax-id-validator]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Iraqi national card boundary Validator | ValidoHub"
  description:
    en: "Validate national card boundary shape, split date/control/body evidence, and prepare privacy-safe debugging output."
