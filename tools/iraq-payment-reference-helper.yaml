schemaVersion: 1
id: iraq-payment-reference-helper
kind: tool
module: validohub
country: IQ
category: finance
status: active
name:
  en: "Iraqi IBAN / RTGS reference Reference Helper"
summary:
  en: "Inspect local payment references, invoice links, amount evidence, and reconciliation-safe formatting."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.iraq-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "PAY input"
        required: true
        placeholder:
          en: "IBAN / RTGS reference REF 2026-001 1,234,567 IQD"
    actions: [validate, parse, format, explain]
related:
  explicit: [iraq-remittance-text-builder, iraq-payment-reconciliation-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Iraqi IBAN / RTGS reference Reference Helper | ValidoHub"
  description:
    en: "Inspect local payment references, invoice links, amount evidence, and reconciliation-safe formatting."
