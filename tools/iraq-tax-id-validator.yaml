schemaVersion: 1
id: iraq-tax-id-validator
kind: tool
module: validohub
country: IQ
category: national-identifiers
status: active
name:
  en: "Iraqi tax number Validator"
summary:
  en: "Normalize tax number identifiers, inspect local tax body evidence, and prepare authority handoff diagnostics."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.iraq-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "TAX input"
        required: true
        placeholder:
          en: "TAX123456"
    actions: [validate, parse, format, explain]
related:
  explicit: [iraq-customs-importer-code-helper, iraq-social-security-boundary-social-insurance-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Iraqi tax number Validator | ValidoHub"
  description:
    en: "Normalize tax number identifiers, inspect local tax body evidence, and prepare authority handoff diagnostics."
