schemaVersion: 1
id: ireland-address-normalizer
kind: tool
module: validohub
country: IE
category: address
status: active
name:
  en: "Irish Address Normalizer"
summary:
  en: "Normalize street, postal code, locality, region, and country lines for local address forms."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.ireland-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ADDR input"
        required: true
        placeholder:
          en: "1 Grafton Street, Dublin, D02 X285"
    actions: [validate, parse, format, explain]
related:
  explicit: [ireland-address-transliteration-normalizer, ireland-region-code-mapper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Irish Address Normalizer | ValidoHub"
  description:
    en: "Normalize street, postal code, locality, region, and country lines for local address forms."
