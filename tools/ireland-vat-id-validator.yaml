schemaVersion: 1
id: ireland-vat-id-validator
kind: tool
module: validohub
country: IE
category: national-identifiers
status: active
name:
  en: "Irish VAT ID / IE Prefix Validator"
summary:
  en: "Normalize IE VAT identifiers, inspect local tax body evidence, and prepare VIES handoff diagnostics."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.ireland-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "VAT input"
        required: true
        placeholder:
          en: "IE6388047V"
    actions: [validate, parse, format, explain]
related:
  explicit: [ireland-eori-validator, ireland-ppsn-social-insurance-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Irish VAT ID / IE Prefix Validator | ValidoHub"
  description:
    en: "Normalize IE VAT identifiers, inspect local tax body evidence, and prepare VIES handoff diagnostics."
