schemaVersion: 1
id: israel-address-normalizer
kind: tool
module: validohub
country: IL
category: address
status: active
name:
  en: "Israeli Address Normalizer"
summary:
  en: "Normalize street, postal code, locality, region, and country lines for local address forms."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.israel-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ADDR input"
        required: true
        placeholder:
          en: "Jaffa Road 1, Jerusalem 9100001"
    actions: [validate, parse, format, explain]
related:
  explicit: [israel-address-transliteration-normalizer, israel-region-code-mapper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Israeli Address Normalizer | ValidoHub"
  description:
    en: "Normalize street, postal code, locality, region, and country lines for local address forms."
