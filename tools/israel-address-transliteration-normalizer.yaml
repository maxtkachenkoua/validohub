schemaVersion: 1
id: israel-address-transliteration-normalizer
kind: tool
module: validohub
country: IL
category: address
status: active
name:
  en: "Israeli Address Transliteration Normalizer"
summary:
  en: "Prepare ASCII-safe address keys while preserving local display text and search/debug evidence."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.israel-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ASCII input"
        required: true
        placeholder:
          en: "Jaffa Road 1, Jerusalem 9100001"
    actions: [validate, parse, format, explain]
related:
  explicit: [israel-region-code-mapper, israel-municipality-code-inspector]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Israeli Address Transliteration Normalizer | ValidoHub"
  description:
    en: "Prepare ASCII-safe address keys while preserving local display text and search/debug evidence."
