schemaVersion: 1
id: italy-masked-iban-formatter
kind: tool
module: validohub
country: IT
category: finance
status: active
name:
  en: "Italian Masked IBAN Formatter"
summary:
  en: "Format and mask Italian IBANs for logs, support previews, and show which account slices remain sensitive."
capabilities:
  - validate
  - parse
  - format
  - explain
  - calculate
algorithm:
  algorithmId: validohub.italy-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "MASK input"
        required: true
        placeholder:
          en: "IT60X0542811101000000123456"
    actions: [validate, parse, format, explain]
related:
  explicit: [italy-iban-validator, italy-abi-cab-bank-code-inspector]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Italian Masked IBAN Formatter | ValidoHub"
  description:
    en: "Format and mask Italian IBANs for logs, support previews, and show which account slices remain sensitive."
