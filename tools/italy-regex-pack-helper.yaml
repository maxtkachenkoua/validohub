schemaVersion: 1
id: italy-regex-pack-helper
kind: tool
module: validohub
country: IT
category: developer-tools
status: active
name:
  en: "Italian Regex Pack Helper"
summary:
  en: "Prepare regex snippets for Italian CF, Partita IVA, IBAN, CAP, phone, dates, and local evidence labels."
capabilities:
  - validate
  - parse
  - format
  - explain
  - calculate
algorithm:
  algorithmId: validohub.italy-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "REGEX input"
        required: true
        placeholder:
          en: "codice fiscale, partita iva, CAP, telefono +39, IBAN IT"
    actions: [validate, parse, format, explain]
related:
  explicit: [italy-postal-code-validator, italy-address-normalizer]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Italian Regex Pack Helper | ValidoHub"
  description:
    en: "Prepare regex snippets for Italian CF, Partita IVA, IBAN, CAP, phone, dates, and local evidence labels."
