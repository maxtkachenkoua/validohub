schemaVersion: 1
id: jordan-address-normalizer
kind: tool
module: validohub
country: JO
category: address
status: active
name:
  en: "Jordanian Address Normalizer"
summary:
  en: "Normalize street, postal code, locality, region, and country lines for local address forms."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.jordan-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ADDR input"
        required: true
        placeholder:
          en: "Zahran Street 1, Amman 11118"
    actions: [validate, parse, format, explain]
related:
  explicit: [jordan-address-transliteration-normalizer, jordan-region-code-mapper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Jordanian Address Normalizer | ValidoHub"
  description:
    en: "Normalize street, postal code, locality, region, and country lines for local address forms."
