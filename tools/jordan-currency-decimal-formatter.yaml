schemaVersion: 1
id: jordan-currency-decimal-formatter
kind: tool
module: validohub
country: JO
category: finance
status: active
name:
  en: "Jordanian JOD Decimal Currency Formatter"
summary:
  en: "Normalize JOD amount strings, decimal separators, grouping, and API-safe numeric previews."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.jordan-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "CUR input"
        required: true
        placeholder:
          en: "1,234.560 JOD"
    actions: [validate, parse, format, explain]
related:
  explicit: [jordan-tax-rate-sanity-helper, jordan-tax-return-field-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Jordanian JOD Decimal Currency Formatter | ValidoHub"
  description:
    en: "Normalize JOD amount strings, decimal separators, grouping, and API-safe numeric previews."
