schemaVersion: 1
id: kazakhstan-address-normalizer
kind: tool
module: validohub
country: KZ
category: address
status: active
name:
  en: "Kazakh Address Normalizer"
summary:
  en: "Normalize street, postal code, locality, region, and country lines for local address forms."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.kazakhstan-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ADDR input"
        required: true
        placeholder:
          en: "Mangilik El Avenue 1, Astana 010000"
    actions: [validate, parse, format, explain]
related:
  explicit: [kazakhstan-address-transliteration-normalizer, kazakhstan-region-code-mapper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Kazakh Address Normalizer | ValidoHub"
  description:
    en: "Normalize street, postal code, locality, region, and country lines for local address forms."
