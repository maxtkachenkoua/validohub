schemaVersion: 1
id: kazakhstan-bin-validator
kind: tool
module: validohub
country: KZ
category: national-identifiers
status: active
name:
  en: "Kazakh BIN Validator"
summary:
  en: "Inspect BIN structure, registry-style prefixes, control digits, and official lookup boundaries."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.kazakhstan-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ORG input"
        required: true
        placeholder:
          en: "120940012345"
    actions: [validate, parse, format, explain]
related:
  explicit: [kazakhstan-tax-id-validator, kazakhstan-customs-importer-code-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Kazakh BIN Validator | ValidoHub"
  description:
    en: "Inspect BIN structure, registry-style prefixes, control digits, and official lookup boundaries."
