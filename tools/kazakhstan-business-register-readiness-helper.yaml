schemaVersion: 1
id: kazakhstan-business-register-readiness-helper
kind: tool
module: validohub
country: KZ
category: government
status: active
name:
  en: "Kazakh eGov / business registry Readiness Helper"
summary:
  en: "Prepare browser-only evidence before a regulated eGov / business registry lookup or company registry workflow."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.kazakhstan-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "REG input"
        required: true
        placeholder:
          en: "120940012345 120940012345 Mangilik El Avenue 1, Astana 010000"
    actions: [validate, parse, format, explain]
related:
  explicit: [kazakhstan-id-card-format-helper, kazakhstan-passport-number-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Kazakh eGov / business registry Readiness Helper | ValidoHub"
  description:
    en: "Prepare browser-only evidence before a regulated eGov / business registry lookup or company registry workflow."
