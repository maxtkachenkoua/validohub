schemaVersion: 1
id: kazakhstan-currency-decimal-formatter
kind: tool
module: validohub
country: KZ
category: finance
status: active
name:
  en: "Kazakh KZT Decimal Currency Formatter"
summary:
  en: "Normalize KZT amount strings, decimal separators, grouping, and API-safe numeric previews."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.kazakhstan-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "CUR input"
        required: true
        placeholder:
          en: "1 234,56 KZT"
    actions: [validate, parse, format, explain]
related:
  explicit: [kazakhstan-tax-rate-sanity-helper, kazakhstan-tax-return-field-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Kazakh KZT Decimal Currency Formatter | ValidoHub"
  description:
    en: "Normalize KZT amount strings, decimal separators, grouping, and API-safe numeric previews."
