schemaVersion: 1
id: kazakhstan-iin-boundary-validator
kind: tool
module: validohub
country: KZ
category: national-identifiers
status: active
name:
  en: "Kazakh IIN boundary Validator"
summary:
  en: "Validate IIN boundary shape, split date/control/body evidence, and prepare privacy-safe debugging output."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.kazakhstan-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ID input"
        required: true
        placeholder:
          en: "900101300123"
    actions: [validate, parse, format, explain]
related:
  explicit: [kazakhstan-bin-validator, kazakhstan-tax-id-validator]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Kazakh IIN boundary Validator | ValidoHub"
  description:
    en: "Validate IIN boundary shape, split date/control/body evidence, and prepare privacy-safe debugging output."
