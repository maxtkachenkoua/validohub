schemaVersion: 1
id: kazakhstan-tax-id-validator
kind: tool
module: validohub
country: KZ
category: national-identifiers
status: active
name:
  en: "Kazakh taxpayer number / VAT Validator"
summary:
  en: "Normalize taxpayer number / VAT identifiers, inspect local tax body evidence, and prepare authority handoff diagnostics."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.kazakhstan-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "TAX input"
        required: true
        placeholder:
          en: "120940012345"
    actions: [validate, parse, format, explain]
related:
  explicit: [kazakhstan-customs-importer-code-helper, kazakhstan-social-insurance-boundary-social-insurance-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Kazakh taxpayer number / VAT Validator | ValidoHub"
  description:
    en: "Normalize taxpayer number / VAT identifiers, inspect local tax body evidence, and prepare authority handoff diagnostics."
