schemaVersion: 1
id: kenya-company-registration-number-validator
kind: tool
module: validohub
country: KE
category: national-identifiers
status: active
name:
  en: "Kenyan company registration number Validator"
summary:
  en: "Inspect company registration number structure, registry-style prefixes, control digits, and official lookup boundaries."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.kenya-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ORG input"
        required: true
        placeholder:
          en: "KE-REG-123456"
    actions: [validate, parse, format, explain]
related:
  explicit: [kenya-tax-id-validator, kenya-customs-importer-code-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Kenyan company registration number Validator | ValidoHub"
  description:
    en: "Inspect company registration number structure, registry-style prefixes, control digits, and official lookup boundaries."
