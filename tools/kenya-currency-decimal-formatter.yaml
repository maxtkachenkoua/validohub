schemaVersion: 1
id: kenya-currency-decimal-formatter
kind: tool
module: validohub
country: KE
category: finance
status: active
name:
  en: "Kenyan KES Decimal Currency Formatter"
summary:
  en: "Normalize KES amount strings, decimal separators, grouping, and API-safe numeric previews."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.kenya-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "CUR input"
        required: true
        placeholder:
          en: "1,234.56 KES"
    actions: [validate, parse, format, explain]
related:
  explicit: [kenya-tax-rate-sanity-helper, kenya-tax-return-field-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Kenyan KES Decimal Currency Formatter | ValidoHub"
  description:
    en: "Normalize KES amount strings, decimal separators, grouping, and API-safe numeric previews."
