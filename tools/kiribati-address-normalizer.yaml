schemaVersion: 1
id: kiribati-address-normalizer
kind: tool
module: validohub
country: KI
category: address
status: active
name:
  en: "I-Kiribati Address Normalizer"
summary:
  en: "Normalize street, postal code, locality, region, and country lines for local address forms."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.kiribati-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ADDR input"
        required: true
        placeholder:
          en: "Bairiki, South Tarawa"
    actions: [validate, parse, format, explain]
related:
  explicit: [kiribati-address-transliteration-normalizer, kiribati-region-code-mapper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "I-Kiribati Address Normalizer | ValidoHub"
  description:
    en: "Normalize street, postal code, locality, region, and country lines for local address forms."
