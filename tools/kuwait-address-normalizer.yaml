schemaVersion: 1
id: kuwait-address-normalizer
kind: tool
module: validohub
country: KW
category: address
status: active
name:
  en: "Kuwaiti Address Normalizer"
summary:
  en: "Normalize street, postal code, locality, region, and country lines for local address forms."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.kuwait-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ADDR input"
        required: true
        placeholder:
          en: "Arabian Gulf Street 1, Kuwait City 13001"
    actions: [validate, parse, format, explain]
related:
  explicit: [kuwait-address-transliteration-normalizer, kuwait-region-code-mapper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Kuwaiti Address Normalizer | ValidoHub"
  description:
    en: "Normalize street, postal code, locality, region, and country lines for local address forms."
