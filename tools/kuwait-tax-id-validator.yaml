schemaVersion: 1
id: kuwait-tax-id-validator
kind: tool
module: validohub
country: KW
category: national-identifiers
status: active
name:
  en: "Kuwaiti tax file number Validator"
summary:
  en: "Normalize tax file number identifiers, inspect local tax body evidence, and prepare authority handoff diagnostics."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.kuwait-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "TAX input"
        required: true
        placeholder:
          en: "TF123456"
    actions: [validate, parse, format, explain]
related:
  explicit: [kuwait-customs-importer-code-helper, kuwait-pifss-boundary-social-insurance-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Kuwaiti tax file number Validator | ValidoHub"
  description:
    en: "Normalize tax file number identifiers, inspect local tax body evidence, and prepare authority handoff diagnostics."
