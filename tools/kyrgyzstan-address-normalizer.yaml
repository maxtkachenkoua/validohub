schemaVersion: 1
id: kyrgyzstan-address-normalizer
kind: tool
module: validohub
country: KG
category: address
status: active
name:
  en: "Kyrgyz Address Normalizer"
summary:
  en: "Normalize street, postal code, locality, region, and country lines for local address forms."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.kyrgyzstan-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ADDR input"
        required: true
        placeholder:
          en: "Chuy Avenue 1, Bishkek 720000"
    actions: [validate, parse, format, explain]
related:
  explicit: [kyrgyzstan-address-transliteration-normalizer, kyrgyzstan-region-code-mapper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Kyrgyz Address Normalizer | ValidoHub"
  description:
    en: "Normalize street, postal code, locality, region, and country lines for local address forms."
