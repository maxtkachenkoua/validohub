schemaVersion: 1
id: kyrgyzstan-bank-account-validator
kind: tool
module: validohub
country: KG
category: finance
status: active
name:
  en: "Kyrgyzstan Bank Account Validator"
summary:
  en: "Validate local bank-account or routing shape, split bank/account blocks, and keep ownership lookup outside the browser."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.kyrgyzstan-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "BANK input"
        required: true
        placeholder:
          en: "109 1234567890123456"
    actions: [validate, parse, format, explain]
related:
  explicit: [kyrgyzstan-bank-account-fixture-generator, kyrgyzstan-bank-account-inspector]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Kyrgyzstan Bank Account Validator | ValidoHub"
  description:
    en: "Validate local bank-account or routing shape, split bank/account blocks, and keep ownership lookup outside the browser."
