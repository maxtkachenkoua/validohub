schemaVersion: 1
id: kyrgyzstan-currency-decimal-formatter
kind: tool
module: validohub
country: KG
category: finance
status: active
name:
  en: "Kyrgyz KGS Decimal Currency Formatter"
summary:
  en: "Normalize KGS amount strings, decimal separators, grouping, and API-safe numeric previews."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.kyrgyzstan-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "CUR input"
        required: true
        placeholder:
          en: "1 234,56 KGS"
    actions: [validate, parse, format, explain]
related:
  explicit: [kyrgyzstan-tax-rate-sanity-helper, kyrgyzstan-tax-return-field-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Kyrgyz KGS Decimal Currency Formatter | ValidoHub"
  description:
    en: "Normalize KGS amount strings, decimal separators, grouping, and API-safe numeric previews."
