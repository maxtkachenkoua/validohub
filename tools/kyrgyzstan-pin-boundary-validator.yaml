schemaVersion: 1
id: kyrgyzstan-pin-boundary-validator
kind: tool
module: validohub
country: KG
category: national-identifiers
status: active
name:
  en: "Kyrgyz PIN boundary Validator"
summary:
  en: "Validate PIN boundary shape, split date/control/body evidence, and prepare privacy-safe debugging output."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.kyrgyzstan-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ID input"
        required: true
        placeholder:
          en: "20101200123456"
    actions: [validate, parse, format, explain]
related:
  explicit: [kyrgyzstan-okpo-company-code-validator, kyrgyzstan-tax-id-validator]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Kyrgyz PIN boundary Validator | ValidoHub"
  description:
    en: "Validate PIN boundary shape, split date/control/body evidence, and prepare privacy-safe debugging output."
