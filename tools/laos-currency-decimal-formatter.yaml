schemaVersion: 1
id: laos-currency-decimal-formatter
kind: tool
module: validohub
country: LA
category: finance
status: active
name:
  en: "Lao LAK Decimal Currency Formatter"
summary:
  en: "Normalize LAK amount strings, decimal separators, grouping, and API-safe numeric previews."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.laos-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "CUR input"
        required: true
        placeholder:
          en: "1,234,567 LAK"
    actions: [validate, parse, format, explain]
related:
  explicit: [laos-tax-rate-sanity-helper, laos-tax-return-field-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Lao LAK Decimal Currency Formatter | ValidoHub"
  description:
    en: "Normalize LAK amount strings, decimal separators, grouping, and API-safe numeric previews."
