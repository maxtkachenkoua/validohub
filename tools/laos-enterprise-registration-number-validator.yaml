schemaVersion: 1
id: laos-enterprise-registration-number-validator
kind: tool
module: validohub
country: LA
category: national-identifiers
status: active
name:
  en: "Lao enterprise registration number Validator"
summary:
  en: "Inspect enterprise registration number structure, registry-style prefixes, control digits, and official lookup boundaries."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.laos-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ORG input"
        required: true
        placeholder:
          en: "ER-123456"
    actions: [validate, parse, format, explain]
related:
  explicit: [laos-tax-id-validator, laos-customs-importer-code-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Lao enterprise registration number Validator | ValidoHub"
  description:
    en: "Inspect enterprise registration number structure, registry-style prefixes, control digits, and official lookup boundaries."
