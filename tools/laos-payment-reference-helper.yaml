schemaVersion: 1
id: laos-payment-reference-helper
kind: tool
module: validohub
country: LA
category: finance
status: active
name:
  en: "Lao LAPNet / BCEL One reference Reference Helper"
summary:
  en: "Inspect local payment references, invoice links, amount evidence, and reconciliation-safe formatting."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.laos-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "PAY input"
        required: true
        placeholder:
          en: "LAPNet / BCEL One reference REF 2026-001 1,234,567 LAK"
    actions: [validate, parse, format, explain]
related:
  explicit: [laos-remittance-text-builder, laos-payment-reconciliation-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Lao LAPNet / BCEL One reference Reference Helper | ValidoHub"
  description:
    en: "Inspect local payment references, invoice links, amount evidence, and reconciliation-safe formatting."
