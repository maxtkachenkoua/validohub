schemaVersion: 1
id: latvia-personal-code-social-insurance-helper
kind: tool
module: validohub
country: LV
category: national-identifiers
status: active
name:
  en: "Latvian personal code Helper"
summary:
  en: "Split personal code evidence into local body, date hints, checksum notes, and privacy-safe diagnostics."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.latvia-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "SOC input"
        required: true
        placeholder:
          en: "010180-12345"
    actions: [validate, parse, format, explain]
related:
  explicit: [latvia-company-onboarding-auditor, latvia-business-register-readiness-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Latvian personal code Helper | ValidoHub"
  description:
    en: "Split personal code evidence into local body, date hints, checksum notes, and privacy-safe diagnostics."
