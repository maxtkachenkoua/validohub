schemaVersion: 1
id: latvia-personal-data-fixture-generator
kind: tool
module: validohub
country: LV
category: privacy
status: active
name:
  en: "Latvian Personal Data Fixture Helper"
summary:
  en: "Generate fixture-safe local identity/address/payment snippets with clear non-official boundaries."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.latvia-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "FIX input"
        required: true
        placeholder:
          en: "010180-12345\\nBrivibas iela 1, LV-1050 Riga\\n+371 29 123 456"
    actions: [validate, parse, format, explain]
related:
  explicit: [latvia-driving-licence-format-helper, latvia-residence-permit-format-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Latvian Personal Data Fixture Helper | ValidoHub"
  description:
    en: "Generate fixture-safe local identity/address/payment snippets with clear non-official boundaries."
