schemaVersion: 1
id: latvia-sepa-direct-debit-mandate-helper
kind: tool
module: validohub
country: LV
category: finance
status: active
name:
  en: "Latvian SEPA Direct Debit Mandate Helper"
summary:
  en: "Inspect mandate references, creditor data, debtor IBAN, and browser-only direct-debit readiness."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.latvia-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "SDD input"
        required: true
        placeholder:
          en: "MANDATE-2026-001 LV80BANK0000435195001"
    actions: [validate, parse, format, explain]
related:
  explicit: [latvia-payment-reference-helper, latvia-remittance-text-builder]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Latvian SEPA Direct Debit Mandate Helper | ValidoHub"
  description:
    en: "Inspect mandate references, creditor data, debtor IBAN, and browser-only direct-debit readiness."
