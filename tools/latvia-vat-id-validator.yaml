schemaVersion: 1
id: latvia-vat-id-validator
kind: tool
module: validohub
country: LV
category: national-identifiers
status: active
name:
  en: "Latvian VAT ID / LV Prefix Validator"
summary:
  en: "Normalize LV VAT identifiers, inspect local tax body evidence, and prepare VIES handoff diagnostics."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.latvia-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "VAT input"
        required: true
        placeholder:
          en: "LV40003000000"
    actions: [validate, parse, format, explain]
related:
  explicit: [latvia-eori-validator, latvia-personal-code-social-insurance-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Latvian VAT ID / LV Prefix Validator | ValidoHub"
  description:
    en: "Normalize LV VAT identifiers, inspect local tax body evidence, and prepare VIES handoff diagnostics."
