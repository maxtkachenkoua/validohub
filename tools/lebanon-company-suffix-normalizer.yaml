schemaVersion: 1
id: lebanon-company-suffix-normalizer
kind: tool
module: validohub
country: LB
category: developer-tools
status: active
name:
  en: "Lebanese Company Suffix Normalizer"
summary:
  en: "Normalize local company suffixes, registry labels, and search-key variants for onboarding forms."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.lebanon-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "SUFFIX input"
        required: true
        placeholder:
          en: "Lebanon Sample Holding Ltd CR-123456"
    actions: [validate, parse, format, explain]
related:
  explicit: [lebanon-procurement-identifier-helper, lebanon-accessibility-locale-copy-checker]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Lebanese Company Suffix Normalizer | ValidoHub"
  description:
    en: "Normalize local company suffixes, registry labels, and search-key variants for onboarding forms."
