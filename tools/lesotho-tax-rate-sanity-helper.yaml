schemaVersion: 1
id: lesotho-tax-rate-sanity-helper
kind: tool
module: validohub
country: LS
category: tax
status: active
name:
  en: "Basotho Tax Rate Sanity Helper"
summary:
  en: "Inspect tax-rate snippets, local tax labels, amount bases, and official rate lookup boundaries."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.lesotho-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "RATE input"
        required: true
        placeholder:
          en: "tax number / VAT boundary 20% base 1,234.56 LSL"
    actions: [validate, parse, format, explain]
related:
  explicit: [lesotho-tax-return-field-helper, lesotho-invoice-number-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Basotho Tax Rate Sanity Helper | ValidoHub"
  description:
    en: "Inspect tax-rate snippets, local tax labels, amount bases, and official rate lookup boundaries."
