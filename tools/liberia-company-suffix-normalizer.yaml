schemaVersion: 1
id: liberia-company-suffix-normalizer
kind: tool
module: validohub
country: LR
category: developer-tools
status: active
name:
  en: "Liberian Company Suffix Normalizer"
summary:
  en: "Normalize local company suffixes, registry labels, and search-key variants for onboarding forms."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.liberia-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "SUFFIX input"
        required: true
        placeholder:
          en: "Liberia Sample Holding Ltd LR-REG-123456"
    actions: [validate, parse, format, explain]
related:
  explicit: [liberia-procurement-identifier-helper, liberia-accessibility-locale-copy-checker]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Liberian Company Suffix Normalizer | ValidoHub"
  description:
    en: "Normalize local company suffixes, registry labels, and search-key variants for onboarding forms."
