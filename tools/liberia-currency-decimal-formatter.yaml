schemaVersion: 1
id: liberia-currency-decimal-formatter
kind: tool
module: validohub
country: LR
category: finance
status: active
name:
  en: "Liberian LRD Decimal Currency Formatter"
summary:
  en: "Normalize LRD amount strings, decimal separators, grouping, and API-safe numeric previews."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.liberia-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "CUR input"
        required: true
        placeholder:
          en: "1,234.56 LRD"
    actions: [validate, parse, format, explain]
related:
  explicit: [liberia-tax-rate-sanity-helper, liberia-tax-return-field-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Liberian LRD Decimal Currency Formatter | ValidoHub"
  description:
    en: "Normalize LRD amount strings, decimal separators, grouping, and API-safe numeric previews."
