schemaVersion: 1
id: libya-company-suffix-normalizer
kind: tool
module: validohub
country: LY
category: developer-tools
status: active
name:
  en: "Libyan Company Suffix Normalizer"
summary:
  en: "Normalize local company suffixes, registry labels, and search-key variants for onboarding forms."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.libya-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "SUFFIX input"
        required: true
        placeholder:
          en: "Libya Sample Holding Ltd LY-REG-123456"
    actions: [validate, parse, format, explain]
related:
  explicit: [libya-procurement-identifier-helper, libya-accessibility-locale-copy-checker]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Libyan Company Suffix Normalizer | ValidoHub"
  description:
    en: "Normalize local company suffixes, registry labels, and search-key variants for onboarding forms."
