schemaVersion: 1
id: libya-pii-masker
kind: tool
module: validohub
country: LY
category: privacy
status: active
name:
  en: "Libyan PII Masker"
summary:
  en: "Detect and mask local identifier, phone, address, bank, and tax evidence in plain text."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.libya-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "PII input"
        required: true
        placeholder:
          en: "LY-ID-123456 +218 123 456 789 001 1234567890"
    actions: [validate, parse, format, explain]
related:
  explicit: [libya-personal-data-fixture-generator, libya-driving-licence-format-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Libyan PII Masker | ValidoHub"
  description:
    en: "Detect and mask local identifier, phone, address, bank, and tax evidence in plain text."
