schemaVersion: 1
id: liechtenstein-accessibility-locale-copy-checker
kind: tool
module: validohub
country: LI
category: localization
status: active
name:
  en: "Liechtenstein Locale Copy Checker"
summary:
  en: "Check UI labels for local identifier names, date/currency wording, and support-safe explanations."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.liechtenstein-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "COPY input"
        required: true
        placeholder:
          en: "PEID input, Postleitzahl input, amount 1'234.56 CHF"
    actions: [validate, parse, format, explain]
related:
  explicit: [liechtenstein-support-ticket-scrubber, liechtenstein-integration-smoke-test-builder]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Liechtenstein Locale Copy Checker | ValidoHub"
  description:
    en: "Check UI labels for local identifier names, date/currency wording, and support-safe explanations."
