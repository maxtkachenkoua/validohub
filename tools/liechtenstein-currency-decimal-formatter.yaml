schemaVersion: 1
id: liechtenstein-currency-decimal-formatter
kind: tool
module: validohub
country: LI
category: finance
status: active
name:
  en: "Liechtenstein CHF Decimal Currency Formatter"
summary:
  en: "Normalize CHF amount strings, decimal separators, grouping, and API-safe numeric previews."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.liechtenstein-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "CUR input"
        required: true
        placeholder:
          en: "1'234.56 CHF"
    actions: [validate, parse, format, explain]
related:
  explicit: [liechtenstein-vat-rate-sanity-helper, liechtenstein-vat-return-field-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Liechtenstein CHF Decimal Currency Formatter | ValidoHub"
  description:
    en: "Normalize CHF amount strings, decimal separators, grouping, and API-safe numeric previews."
