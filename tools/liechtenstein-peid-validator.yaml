schemaVersion: 1
id: liechtenstein-peid-validator
kind: tool
module: validohub
country: LI
category: national-identifiers
status: active
name:
  en: "Liechtenstein PEID Validator"
summary:
  en: "Validate PEID shape, split date/control/body evidence, and prepare privacy-safe debugging output."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.liechtenstein-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ID input"
        required: true
        placeholder:
          en: "756.1234.5678.97"
    actions: [validate, parse, format, explain]
related:
  explicit: [liechtenstein-company-register-number-validator, liechtenstein-vat-id-validator]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Liechtenstein PEID Validator | ValidoHub"
  description:
    en: "Validate PEID shape, split date/control/body evidence, and prepare privacy-safe debugging output."
