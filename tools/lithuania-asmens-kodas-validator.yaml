schemaVersion: 1
id: lithuania-asmens-kodas-validator
kind: tool
module: validohub
country: LT
category: national-identifiers
status: active
name:
  en: "Lithuanian asmens kodas Validator"
summary:
  en: "Validate asmens kodas shape, split date/control/body evidence, and prepare privacy-safe debugging output."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.lithuania-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ID input"
        required: true
        placeholder:
          en: "38001010017"
    actions: [validate, parse, format, explain]
related:
  explicit: [lithuania-company-code-validator, lithuania-vat-id-validator]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Lithuanian asmens kodas Validator | ValidoHub"
  description:
    en: "Validate asmens kodas shape, split date/control/body evidence, and prepare privacy-safe debugging output."
