schemaVersion: 1
id: lithuania-vat-id-validator
kind: tool
module: validohub
country: LT
category: national-identifiers
status: active
name:
  en: "Lithuanian VAT ID / LT Prefix Validator"
summary:
  en: "Normalize LT VAT identifiers, inspect local tax body evidence, and prepare VIES handoff diagnostics."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.lithuania-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "VAT input"
        required: true
        placeholder:
          en: "LT123456789"
    actions: [validate, parse, format, explain]
related:
  explicit: [lithuania-eori-validator, lithuania-asmens-kodas-social-insurance-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Lithuanian VAT ID / LT Prefix Validator | ValidoHub"
  description:
    en: "Normalize LT VAT identifiers, inspect local tax body evidence, and prepare VIES handoff diagnostics."
