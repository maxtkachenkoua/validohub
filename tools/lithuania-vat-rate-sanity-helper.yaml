schemaVersion: 1
id: lithuania-vat-rate-sanity-helper
kind: tool
module: validohub
country: LT
category: tax
status: active
name:
  en: "Lithuanian VAT Rate Sanity Helper"
summary:
  en: "Inspect VAT-rate snippets, local tax labels, amount bases, and official rate lookup boundaries."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.lithuania-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "RATE input"
        required: true
        placeholder:
          en: "PVM / VAT 20% base 1 234,56 EUR"
    actions: [validate, parse, format, explain]
related:
  explicit: [lithuania-vat-return-field-helper, lithuania-invoice-number-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Lithuanian VAT Rate Sanity Helper | ValidoHub"
  description:
    en: "Inspect VAT-rate snippets, local tax labels, amount bases, and official rate lookup boundaries."
