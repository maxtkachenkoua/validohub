schemaVersion: 1
id: luxembourg-address-normalizer
kind: tool
module: validohub
country: LU
category: address
status: active
name:
  en: "Luxembourgish Address Normalizer"
summary:
  en: "Normalize street, postal code, locality, region, and country lines for local address forms."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.luxembourg-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ADDR input"
        required: true
        placeholder:
          en: "Grand-Rue 1, L-1661 Luxembourg"
    actions: [validate, parse, format, explain]
related:
  explicit: [luxembourg-address-transliteration-normalizer, luxembourg-region-code-mapper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Luxembourgish Address Normalizer | ValidoHub"
  description:
    en: "Normalize street, postal code, locality, region, and country lines for local address forms."
