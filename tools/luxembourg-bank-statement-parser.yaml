schemaVersion: 1
id: luxembourg-bank-statement-parser
kind: tool
module: validohub
country: LU
category: finance
status: active
name:
  en: "Luxembourgish Bank Statement Parser"
summary:
  en: "Parse statement rows for date, amount, IBAN, counterparty, reference, and local decimal conventions."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.luxembourg-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "STMT input"
        required: true
        placeholder:
          en: "21/07/2026; 1 234,56 EUR; LU280019400644750000; sample counterparty"
    actions: [validate, parse, format, explain]
related:
  explicit: [luxembourg-masked-iban-formatter, luxembourg-currency-decimal-formatter]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Luxembourgish Bank Statement Parser | ValidoHub"
  description:
    en: "Parse statement rows for date, amount, IBAN, counterparty, reference, and local decimal conventions."
