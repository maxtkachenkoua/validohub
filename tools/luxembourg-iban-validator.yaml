schemaVersion: 1
id: luxembourg-iban-validator
kind: tool
module: validohub
country: LU
category: finance
status: active
name:
  en: "Luxembourg IBAN Validator"
summary:
  en: "Validate LU IBAN shape, replay MOD-97 evidence, and split bank/account blocks for debugging."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.luxembourg-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "IBAN input"
        required: true
        placeholder:
          en: "LU280019400644750000"
    actions: [validate, parse, format, explain]
related:
  explicit: [luxembourg-iban-generator, luxembourg-bank-account-inspector]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Luxembourg IBAN Validator | ValidoHub"
  description:
    en: "Validate LU IBAN shape, replay MOD-97 evidence, and split bank/account blocks for debugging."
