schemaVersion: 1
id: luxembourg-rcs-number-validator
kind: tool
module: validohub
country: LU
category: national-identifiers
status: active
name:
  en: "Luxembourgish RCS number Validator"
summary:
  en: "Inspect RCS number structure, registry-style prefixes, control digits, and official lookup boundaries."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.luxembourg-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ORG input"
        required: true
        placeholder:
          en: "B123456"
    actions: [validate, parse, format, explain]
related:
  explicit: [luxembourg-vat-id-validator, luxembourg-eori-validator]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Luxembourgish RCS number Validator | ValidoHub"
  description:
    en: "Inspect RCS number structure, registry-style prefixes, control digits, and official lookup boundaries."
