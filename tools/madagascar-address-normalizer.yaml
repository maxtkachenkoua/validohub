schemaVersion: 1
id: madagascar-address-normalizer
kind: tool
module: validohub
country: MG
category: address
status: active
name:
  en: "Malagasy Address Normalizer"
summary:
  en: "Normalize street, postal code, locality, region, and country lines for local address forms."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.madagascar-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ADDR input"
        required: true
        placeholder:
          en: "Central business district, Antananarivo 1000"
    actions: [validate, parse, format, explain]
related:
  explicit: [madagascar-address-transliteration-normalizer, madagascar-region-code-mapper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Malagasy Address Normalizer | ValidoHub"
  description:
    en: "Normalize street, postal code, locality, region, and country lines for local address forms."
