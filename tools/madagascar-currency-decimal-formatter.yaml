schemaVersion: 1
id: madagascar-currency-decimal-formatter
kind: tool
module: validohub
country: MG
category: finance
status: active
name:
  en: "Malagasy MGA Decimal Currency Formatter"
summary:
  en: "Normalize MGA amount strings, decimal separators, grouping, and API-safe numeric previews."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.madagascar-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "CUR input"
        required: true
        placeholder:
          en: "1,234.56 MGA"
    actions: [validate, parse, format, explain]
related:
  explicit: [madagascar-tax-rate-sanity-helper, madagascar-tax-return-field-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Malagasy MGA Decimal Currency Formatter | ValidoHub"
  description:
    en: "Normalize MGA amount strings, decimal separators, grouping, and API-safe numeric previews."
