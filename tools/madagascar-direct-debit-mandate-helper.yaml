schemaVersion: 1
id: madagascar-direct-debit-mandate-helper
kind: tool
module: validohub
country: MG
category: finance
status: active
name:
  en: "Malagasy Direct Debit / Auto-Debit Mandate Helper"
summary:
  en: "Inspect mandate references, creditor data, debtor account evidence, and browser-only debit readiness."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.madagascar-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "DD input"
        required: true
        placeholder:
          en: "MANDATE-2026-001 001 1234567890"
    actions: [validate, parse, format, explain]
related:
  explicit: [madagascar-payment-reference-helper, madagascar-remittance-text-builder]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Malagasy Direct Debit / Auto-Debit Mandate Helper | ValidoHub"
  description:
    en: "Inspect mandate references, creditor data, debtor account evidence, and browser-only debit readiness."
