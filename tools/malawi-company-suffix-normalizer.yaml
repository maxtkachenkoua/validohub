schemaVersion: 1
id: malawi-company-suffix-normalizer
kind: tool
module: validohub
country: MW
category: developer-tools
status: active
name:
  en: "Malawian Company Suffix Normalizer"
summary:
  en: "Normalize local company suffixes, registry labels, and search-key variants for onboarding forms."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.malawi-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "SUFFIX input"
        required: true
        placeholder:
          en: "Malawi Sample Holding Ltd MW-REG-123456"
    actions: [validate, parse, format, explain]
related:
  explicit: [malawi-procurement-identifier-helper, malawi-accessibility-locale-copy-checker]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Malawian Company Suffix Normalizer | ValidoHub"
  description:
    en: "Normalize local company suffixes, registry labels, and search-key variants for onboarding forms."
