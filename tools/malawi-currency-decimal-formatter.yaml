schemaVersion: 1
id: malawi-currency-decimal-formatter
kind: tool
module: validohub
country: MW
category: finance
status: active
name:
  en: "Malawian MWK Decimal Currency Formatter"
summary:
  en: "Normalize MWK amount strings, decimal separators, grouping, and API-safe numeric previews."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.malawi-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "CUR input"
        required: true
        placeholder:
          en: "1,234.56 MWK"
    actions: [validate, parse, format, explain]
related:
  explicit: [malawi-tax-rate-sanity-helper, malawi-tax-return-field-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Malawian MWK Decimal Currency Formatter | ValidoHub"
  description:
    en: "Normalize MWK amount strings, decimal separators, grouping, and API-safe numeric previews."
