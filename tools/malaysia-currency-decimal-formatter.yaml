schemaVersion: 1
id: malaysia-currency-decimal-formatter
kind: tool
module: validohub
country: MY
category: finance
status: active
name:
  en: "Malaysian MYR Decimal Currency Formatter"
summary:
  en: "Normalize MYR amount strings, decimal separators, grouping, and API-safe numeric previews."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.malaysia-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "CUR input"
        required: true
        placeholder:
          en: "1,234.56 MYR"
    actions: [validate, parse, format, explain]
related:
  explicit: [malaysia-tax-rate-sanity-helper, malaysia-tax-return-field-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Malaysian MYR Decimal Currency Formatter | ValidoHub"
  description:
    en: "Normalize MYR amount strings, decimal separators, grouping, and API-safe numeric previews."
