schemaVersion: 1
id: malaysia-vin-validator
kind: tool
module: validohub
country: MY
category: transport
status: active
name:
  en: "Malaysian VIN Validator"
summary:
  en: "Validate VIN shape, split WMI/VDS/VIS evidence, and prepare vehicle-intake diagnostics."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.malaysia-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "VIN input"
        required: true
        placeholder:
          en: "WVWZZZ1JZXW000001"
    actions: [validate, parse, format, explain]
related:
  explicit: [malaysia-vehicle-data-redaction-helper, malaysia-customs-declaration-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Malaysian VIN Validator | ValidoHub"
  description:
    en: "Validate VIN shape, split WMI/VDS/VIS evidence, and prepare vehicle-intake diagnostics."
