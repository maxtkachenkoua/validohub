schemaVersion: 1
id: maldives-address-normalizer
kind: tool
module: validohub
country: MV
category: address
status: active
name:
  en: "Maldivian Address Normalizer"
summary:
  en: "Normalize street, postal code, locality, region, and country lines for local address forms."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.maldives-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ADDR input"
        required: true
        placeholder:
          en: "Majeedhee Magu 1, Male 20026"
    actions: [validate, parse, format, explain]
related:
  explicit: [maldives-address-transliteration-normalizer, maldives-region-code-mapper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Maldivian Address Normalizer | ValidoHub"
  description:
    en: "Normalize street, postal code, locality, region, and country lines for local address forms."
