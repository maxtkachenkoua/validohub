schemaVersion: 1
id: mali-business-register-readiness-helper
kind: tool
module: validohub
country: ML
category: government
status: active
name:
  en: "Malian business registry Readiness Helper"
summary:
  en: "Prepare browser-only evidence before a regulated business registry lookup or company registry workflow."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.mali-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "REG input"
        required: true
        placeholder:
          en: "ML-REG-123456 ML123456789 Central business district, Bamako 1000"
    actions: [validate, parse, format, explain]
related:
  explicit: [mali-id-card-format-helper, mali-passport-number-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Malian business registry Readiness Helper | ValidoHub"
  description:
    en: "Prepare browser-only evidence before a regulated business registry lookup or company registry workflow."
