schemaVersion: 1
id: mali-company-suffix-normalizer
kind: tool
module: validohub
country: ML
category: developer-tools
status: active
name:
  en: "Malian Company Suffix Normalizer"
summary:
  en: "Normalize local company suffixes, registry labels, and search-key variants for onboarding forms."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.mali-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "SUFFIX input"
        required: true
        placeholder:
          en: "Mali Sample Holding Ltd ML-REG-123456"
    actions: [validate, parse, format, explain]
related:
  explicit: [mali-procurement-identifier-helper, mali-accessibility-locale-copy-checker]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Malian Company Suffix Normalizer | ValidoHub"
  description:
    en: "Normalize local company suffixes, registry labels, and search-key variants for onboarding forms."
