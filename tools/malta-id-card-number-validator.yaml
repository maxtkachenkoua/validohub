schemaVersion: 1
id: malta-id-card-number-validator
kind: tool
module: validohub
country: MT
category: national-identifiers
status: active
name:
  en: "Maltese ID card number Validator"
summary:
  en: "Validate ID card number shape, split date/control/body evidence, and prepare privacy-safe debugging output."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.malta-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ID input"
        required: true
        placeholder:
          en: "1234567M"
    actions: [validate, parse, format, explain]
related:
  explicit: [malta-company-c-number-validator, malta-vat-id-validator]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Maltese ID card number Validator | ValidoHub"
  description:
    en: "Validate ID card number shape, split date/control/body evidence, and prepare privacy-safe debugging output."
