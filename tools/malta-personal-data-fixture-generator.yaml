schemaVersion: 1
id: malta-personal-data-fixture-generator
kind: tool
module: validohub
country: MT
category: privacy
status: active
name:
  en: "Maltese Personal Data Fixture Helper"
summary:
  en: "Generate fixture-safe local identity/address/payment snippets with clear non-official boundaries."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.malta-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "FIX input"
        required: true
        placeholder:
          en: "1234567M\\nRepublic Street 1, VLT 1111 Valletta\\n+356 2123 4567"
    actions: [validate, parse, format, explain]
related:
  explicit: [malta-driving-licence-format-helper, malta-residence-permit-format-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Maltese Personal Data Fixture Helper | ValidoHub"
  description:
    en: "Generate fixture-safe local identity/address/payment snippets with clear non-official boundaries."
