schemaVersion: 1
id: marshall-islands-address-transliteration-normalizer
kind: tool
module: validohub
country: MH
category: address
status: active
name:
  en: "Marshallese Address Transliteration Normalizer"
summary:
  en: "Prepare ASCII-safe address keys while preserving local display text and search/debug evidence."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.marshall-islands-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ASCII input"
        required: true
        placeholder:
          en: "Delap, Majuro MH 96960"
    actions: [validate, parse, format, explain]
related:
  explicit: [marshall-islands-region-code-mapper, marshall-islands-municipality-code-inspector]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Marshallese Address Transliteration Normalizer | ValidoHub"
  description:
    en: "Prepare ASCII-safe address keys while preserving local display text and search/debug evidence."
