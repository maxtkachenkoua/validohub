schemaVersion: 1
id: mauritania-address-normalizer
kind: tool
module: validohub
country: MR
category: address
status: active
name:
  en: "Mauritanian Address Normalizer"
summary:
  en: "Normalize street, postal code, locality, region, and country lines for local address forms."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.mauritania-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ADDR input"
        required: true
        placeholder:
          en: "Central business district, Nouakchott 1000"
    actions: [validate, parse, format, explain]
related:
  explicit: [mauritania-address-transliteration-normalizer, mauritania-region-code-mapper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Mauritanian Address Normalizer | ValidoHub"
  description:
    en: "Normalize street, postal code, locality, region, and country lines for local address forms."
