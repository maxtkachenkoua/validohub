schemaVersion: 1
id: mauritania-id-card-format-helper
kind: tool
module: validohub
country: MR
category: national-identifiers
status: active
name:
  en: "Mauritanian ID Card Format Helper"
summary:
  en: "Inspect local identity-card snippets, document numbers, dates, and privacy boundaries without proving identity."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.mauritania-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "CARD input"
        required: true
        placeholder:
          en: "MR-ID-123456"
    actions: [validate, parse, format, explain]
related:
  explicit: [mauritania-passport-number-helper, mauritania-mrz-passport-parser]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Mauritanian ID Card Format Helper | ValidoHub"
  description:
    en: "Inspect local identity-card snippets, document numbers, dates, and privacy boundaries without proving identity."
