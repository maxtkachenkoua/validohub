schemaVersion: 1
id: mauritius-regex-pack-helper
kind: tool
module: validohub
country: MU
category: developer-tools
status: active
name:
  en: "Mauritian Regex Pack Helper"
summary:
  en: "Prepare regex snippets for local identifiers, tax IDs, bank accounts, postal, phone, dates, and debug labels."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.mauritius-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "REGEX input"
        required: true
        placeholder:
          en: "national ID boundary company registration number postal/address code  001 1234567890"
    actions: [validate, parse, format, explain]
related:
  explicit: [mauritius-api-payload-auditor, mauritius-form-field-auditor]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Mauritian Regex Pack Helper | ValidoHub"
  description:
    en: "Prepare regex snippets for local identifiers, tax IDs, bank accounts, postal, phone, dates, and debug labels."
