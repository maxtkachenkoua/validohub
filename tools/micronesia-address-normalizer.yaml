schemaVersion: 1
id: micronesia-address-normalizer
kind: tool
module: validohub
country: FM
category: address
status: active
name:
  en: "Micronesian Address Normalizer"
summary:
  en: "Normalize street, postal code, locality, region, and country lines for local address forms."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.micronesia-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ADDR input"
        required: true
        placeholder:
          en: "Palikir, Pohnpei State"
    actions: [validate, parse, format, explain]
related:
  explicit: [micronesia-address-transliteration-normalizer, micronesia-region-code-mapper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Micronesian Address Normalizer | ValidoHub"
  description:
    en: "Normalize street, postal code, locality, region, and country lines for local address forms."
