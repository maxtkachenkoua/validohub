schemaVersion: 1
id: micronesia-currency-decimal-formatter
kind: tool
module: validohub
country: FM
category: finance
status: active
name:
  en: "Micronesian USD Decimal Currency Formatter"
summary:
  en: "Normalize USD amount strings, decimal separators, grouping, and API-safe numeric previews."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.micronesia-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "CUR input"
        required: true
        placeholder:
          en: "1,234.56 USD"
    actions: [validate, parse, format, explain]
related:
  explicit: [micronesia-tax-rate-sanity-helper, micronesia-tax-return-field-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Micronesian USD Decimal Currency Formatter | ValidoHub"
  description:
    en: "Normalize USD amount strings, decimal separators, grouping, and API-safe numeric previews."
