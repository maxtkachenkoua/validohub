schemaVersion: 1
id: monaco-iban-validator
kind: tool
module: validohub
country: MC
category: finance
status: active
name:
  en: "Monaco IBAN Validator"
summary:
  en: "Validate MC IBAN shape, replay MOD-97 evidence, and split bank/account blocks for debugging."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.monaco-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "IBAN input"
        required: true
        placeholder:
          en: "MC5811222000010123456789030"
    actions: [validate, parse, format, explain]
related:
  explicit: [monaco-iban-generator, monaco-bank-account-inspector]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Monaco IBAN Validator | ValidoHub"
  description:
    en: "Validate MC IBAN shape, replay MOD-97 evidence, and split bank/account blocks for debugging."
