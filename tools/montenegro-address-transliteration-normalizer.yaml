schemaVersion: 1
id: montenegro-address-transliteration-normalizer
kind: tool
module: validohub
country: ME
category: address
status: active
name:
  en: "Montenegrin Address Transliteration Normalizer"
summary:
  en: "Prepare ASCII-safe address keys while preserving local display text and search/debug evidence."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.montenegro-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ASCII input"
        required: true
        placeholder:
          en: "Bulevar Svetog Petra 1, 81000 Podgorica"
    actions: [validate, parse, format, explain]
related:
  explicit: [montenegro-region-code-mapper, montenegro-municipality-code-inspector]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Montenegrin Address Transliteration Normalizer | ValidoHub"
  description:
    en: "Prepare ASCII-safe address keys while preserving local display text and search/debug evidence."
