schemaVersion: 1
id: montenegro-iban-validator
kind: tool
module: validohub
country: ME
category: finance
status: active
name:
  en: "Montenegro IBAN Validator"
summary:
  en: "Validate ME IBAN shape, replay MOD-97 evidence, and split bank/account blocks for debugging."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.montenegro-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "IBAN input"
        required: true
        placeholder:
          en: "ME25505000012345678951"
    actions: [validate, parse, format, explain]
related:
  explicit: [montenegro-iban-generator, montenegro-bank-account-inspector]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Montenegro IBAN Validator | ValidoHub"
  description:
    en: "Validate ME IBAN shape, replay MOD-97 evidence, and split bank/account blocks for debugging."
