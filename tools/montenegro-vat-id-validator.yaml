schemaVersion: 1
id: montenegro-vat-id-validator
kind: tool
module: validohub
country: ME
category: national-identifiers
status: active
name:
  en: "Montenegrin VAT ID / ME Prefix Validator"
summary:
  en: "Normalize ME VAT identifiers, inspect local tax body evidence, and prepare VIES handoff diagnostics."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.montenegro-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "VAT input"
        required: true
        placeholder:
          en: "ME02712345"
    actions: [validate, parse, format, explain]
related:
  explicit: [montenegro-eori-validator, montenegro-jmbg-social-insurance-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Montenegrin VAT ID / ME Prefix Validator | ValidoHub"
  description:
    en: "Normalize ME VAT identifiers, inspect local tax body evidence, and prepare VIES handoff diagnostics."
