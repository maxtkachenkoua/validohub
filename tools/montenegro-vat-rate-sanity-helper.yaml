schemaVersion: 1
id: montenegro-vat-rate-sanity-helper
kind: tool
module: validohub
country: ME
category: tax
status: active
name:
  en: "Montenegrin VAT Rate Sanity Helper"
summary:
  en: "Inspect VAT-rate snippets, local tax labels, amount bases, and official rate lookup boundaries."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.montenegro-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "RATE input"
        required: true
        placeholder:
          en: "PDV / VAT 20% base 1.234,56 EUR"
    actions: [validate, parse, format, explain]
related:
  explicit: [montenegro-vat-return-field-helper, montenegro-invoice-number-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Montenegrin VAT Rate Sanity Helper | ValidoHub"
  description:
    en: "Inspect VAT-rate snippets, local tax labels, amount bases, and official rate lookup boundaries."
