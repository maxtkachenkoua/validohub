schemaVersion: 1
id: morocco-bank-account-fixture-generator
kind: tool
module: validohub
country: MA
category: finance
status: active
name:
  en: "Morocco Bank Account Fixture Generator"
summary:
  en: "Generate local bank-account fixture payloads, split routing/account blocks, and prepare payment QA data."
capabilities:
  - generate
  - validate
  - parse
  - explain
algorithm:
  algorithmId: validohub.morocco-suite
forms:
  generate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "BGEN input"
        required: true
        placeholder:
          en: "001 1234567890"
    actions: [generate, validate, explain]
related:
  explicit: [morocco-bank-account-inspector, morocco-bic-swift-inspector]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Morocco Bank Account Fixture Generator | ValidoHub"
  description:
    en: "Generate local bank-account fixture payloads, split routing/account blocks, and prepare payment QA data."
