schemaVersion: 1
id: morocco-calendar-week-helper
kind: tool
module: validohub
country: MA
category: localization
status: active
name:
  en: "Moroccan Calendar Week Helper"
summary:
  en: "Inspect local date strings, week-start assumptions, fiscal-period labels, and ISO handoff values."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.morocco-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "CAL input"
        required: true
        placeholder:
          en: "24/07/2026 week 30"
    actions: [validate, parse, format, explain]
related:
  explicit: [morocco-company-suffix-normalizer, morocco-procurement-identifier-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Moroccan Calendar Week Helper | ValidoHub"
  description:
    en: "Inspect local date strings, week-start assumptions, fiscal-period labels, and ISO handoff values."
