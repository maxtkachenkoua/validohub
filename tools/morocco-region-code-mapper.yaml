schemaVersion: 1
id: morocco-region-code-mapper
kind: tool
module: validohub
country: MA
category: address
status: active
name:
  en: "Moroccan Region / Province Code Mapper"
summary:
  en: "Inspect regional abbreviations, locality evidence, postal zones, and official geography lookup boundaries."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.morocco-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "REGION input"
        required: true
        placeholder:
          en: "1000 Rabat"
    actions: [validate, parse, format, explain]
related:
  explicit: [morocco-municipality-code-inspector, morocco-phone-number-validator]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Moroccan Region / Province Code Mapper | ValidoHub"
  description:
    en: "Inspect regional abbreviations, locality evidence, postal zones, and official geography lookup boundaries."
