schemaVersion: 1
id: mozambique-address-normalizer
kind: tool
module: validohub
country: MZ
category: address
status: active
name:
  en: "Mozambican Address Normalizer"
summary:
  en: "Normalize street, postal code, locality, region, and country lines for local address forms."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.mozambique-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ADDR input"
        required: true
        placeholder:
          en: "Central business district, Maputo 1000"
    actions: [validate, parse, format, explain]
related:
  explicit: [mozambique-address-transliteration-normalizer, mozambique-region-code-mapper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Mozambican Address Normalizer | ValidoHub"
  description:
    en: "Normalize street, postal code, locality, region, and country lines for local address forms."
