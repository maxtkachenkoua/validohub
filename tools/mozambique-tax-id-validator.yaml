schemaVersion: 1
id: mozambique-tax-id-validator
kind: tool
module: validohub
country: MZ
category: national-identifiers
status: active
name:
  en: "Mozambican tax number / VAT boundary Validator"
summary:
  en: "Normalize tax number / VAT boundary identifiers, inspect local tax body evidence, and prepare authority handoff diagnostics."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.mozambique-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "TAX input"
        required: true
        placeholder:
          en: "MZ123456789"
    actions: [validate, parse, format, explain]
related:
  explicit: [mozambique-customs-importer-code-helper, mozambique-social-security-boundary-social-insurance-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Mozambican tax number / VAT boundary Validator | ValidoHub"
  description:
    en: "Normalize tax number / VAT boundary identifiers, inspect local tax body evidence, and prepare authority handoff diagnostics."
