schemaVersion: 1
id: myanmar-accessibility-locale-copy-checker
kind: tool
module: validohub
country: MM
category: localization
status: active
name:
  en: "Myanmar Locale Copy Checker"
summary:
  en: "Check UI labels for local identifier names, date/currency wording, and support-safe explanations."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.myanmar-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "COPY input"
        required: true
        placeholder:
          en: "NRC boundary input, postal code input, amount 1,234,567 MMK"
    actions: [validate, parse, format, explain]
related:
  explicit: [myanmar-support-ticket-scrubber, myanmar-integration-smoke-test-builder]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Myanmar Locale Copy Checker | ValidoHub"
  description:
    en: "Check UI labels for local identifier names, date/currency wording, and support-safe explanations."
