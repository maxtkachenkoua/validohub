schemaVersion: 1
id: myanmar-address-normalizer
kind: tool
module: validohub
country: MM
category: address
status: active
name:
  en: "Myanmar Address Normalizer"
summary:
  en: "Normalize street, postal code, locality, region, and country lines for local address forms."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.myanmar-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ADDR input"
        required: true
        placeholder:
          en: "Yaza Htarni Road 1, Naypyidaw 15011"
    actions: [validate, parse, format, explain]
related:
  explicit: [myanmar-address-transliteration-normalizer, myanmar-region-code-mapper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Myanmar Address Normalizer | ValidoHub"
  description:
    en: "Normalize street, postal code, locality, region, and country lines for local address forms."
