schemaVersion: 1
id: myanmar-currency-decimal-formatter
kind: tool
module: validohub
country: MM
category: finance
status: active
name:
  en: "Myanmar MMK Decimal Currency Formatter"
summary:
  en: "Normalize MMK amount strings, decimal separators, grouping, and API-safe numeric previews."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.myanmar-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "CUR input"
        required: true
        placeholder:
          en: "1,234,567 MMK"
    actions: [validate, parse, format, explain]
related:
  explicit: [myanmar-tax-rate-sanity-helper, myanmar-tax-return-field-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Myanmar MMK Decimal Currency Formatter | ValidoHub"
  description:
    en: "Normalize MMK amount strings, decimal separators, grouping, and API-safe numeric previews."
