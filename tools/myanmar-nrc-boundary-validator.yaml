schemaVersion: 1
id: myanmar-nrc-boundary-validator
kind: tool
module: validohub
country: MM
category: national-identifiers
status: active
name:
  en: "Myanmar NRC boundary Validator"
summary:
  en: "Validate NRC boundary shape, split date/control/body evidence, and prepare privacy-safe debugging output."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.myanmar-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ID input"
        required: true
        placeholder:
          en: "12/LaMaNa(N)123456"
    actions: [validate, parse, format, explain]
related:
  explicit: [myanmar-dica-company-number-validator, myanmar-tax-id-validator]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Myanmar NRC boundary Validator | ValidoHub"
  description:
    en: "Validate NRC boundary shape, split date/control/body evidence, and prepare privacy-safe debugging output."
