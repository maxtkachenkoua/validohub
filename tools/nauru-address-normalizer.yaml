schemaVersion: 1
id: nauru-address-normalizer
kind: tool
module: validohub
country: NR
category: address
status: active
name:
  en: "Nauruan Address Normalizer"
summary:
  en: "Normalize street, postal code, locality, region, and country lines for local address forms."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.nauru-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ADDR input"
        required: true
        placeholder:
          en: "Yaren District, Nauru"
    actions: [validate, parse, format, explain]
related:
  explicit: [nauru-address-transliteration-normalizer, nauru-region-code-mapper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Nauruan Address Normalizer | ValidoHub"
  description:
    en: "Normalize street, postal code, locality, region, and country lines for local address forms."
