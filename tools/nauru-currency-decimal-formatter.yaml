schemaVersion: 1
id: nauru-currency-decimal-formatter
kind: tool
module: validohub
country: NR
category: finance
status: active
name:
  en: "Nauruan AUD Decimal Currency Formatter"
summary:
  en: "Normalize AUD amount strings, decimal separators, grouping, and API-safe numeric previews."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.nauru-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "CUR input"
        required: true
        placeholder:
          en: "1,234.56 AUD"
    actions: [validate, parse, format, explain]
related:
  explicit: [nauru-tax-rate-sanity-helper, nauru-tax-return-field-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Nauruan AUD Decimal Currency Formatter | ValidoHub"
  description:
    en: "Normalize AUD amount strings, decimal separators, grouping, and API-safe numeric previews."
