schemaVersion: 1
id: nepal-address-normalizer
kind: tool
module: validohub
country: NP
category: address
status: active
name:
  en: "Nepali Address Normalizer"
summary:
  en: "Normalize street, postal code, locality, region, and country lines for local address forms."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.nepal-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ADDR input"
        required: true
        placeholder:
          en: "Durbar Marg 1, Kathmandu 44600"
    actions: [validate, parse, format, explain]
related:
  explicit: [nepal-address-transliteration-normalizer, nepal-region-code-mapper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Nepali Address Normalizer | ValidoHub"
  description:
    en: "Normalize street, postal code, locality, region, and country lines for local address forms."
