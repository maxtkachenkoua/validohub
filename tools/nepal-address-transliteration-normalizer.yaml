schemaVersion: 1
id: nepal-address-transliteration-normalizer
kind: tool
module: validohub
country: NP
category: address
status: active
name:
  en: "Nepali Address Transliteration Normalizer"
summary:
  en: "Prepare ASCII-safe address keys while preserving local display text and search/debug evidence."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.nepal-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ASCII input"
        required: true
        placeholder:
          en: "Durbar Marg 1, Kathmandu 44600"
    actions: [validate, parse, format, explain]
related:
  explicit: [nepal-region-code-mapper, nepal-municipality-code-inspector]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Nepali Address Transliteration Normalizer | ValidoHub"
  description:
    en: "Prepare ASCII-safe address keys while preserving local display text and search/debug evidence."
