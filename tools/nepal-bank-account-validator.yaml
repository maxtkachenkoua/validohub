schemaVersion: 1
id: nepal-bank-account-validator
kind: tool
module: validohub
country: NP
category: finance
status: active
name:
  en: "Nepal Bank Account Validator"
summary:
  en: "Validate local bank-account or routing shape, split bank/account blocks, and keep ownership lookup outside the browser."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.nepal-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "BANK input"
        required: true
        placeholder:
          en: "001 1234567890123"
    actions: [validate, parse, format, explain]
related:
  explicit: [nepal-bank-account-fixture-generator, nepal-bank-account-inspector]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Nepal Bank Account Validator | ValidoHub"
  description:
    en: "Validate local bank-account or routing shape, split bank/account blocks, and keep ownership lookup outside the browser."
