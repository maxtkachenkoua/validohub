schemaVersion: 1
id: nepal-tax-id-validator
kind: tool
module: validohub
country: NP
category: national-identifiers
status: active
name:
  en: "Nepali PAN / VAT Validator"
summary:
  en: "Normalize PAN / VAT identifiers, inspect local tax body evidence, and prepare authority handoff diagnostics."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.nepal-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "TAX input"
        required: true
        placeholder:
          en: "500123456"
    actions: [validate, parse, format, explain]
related:
  explicit: [nepal-customs-importer-code-helper, nepal-ssf-boundary-social-insurance-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Nepali PAN / VAT Validator | ValidoHub"
  description:
    en: "Normalize PAN / VAT identifiers, inspect local tax body evidence, and prepare authority handoff diagnostics."
