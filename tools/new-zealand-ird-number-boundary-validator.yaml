schemaVersion: 1
id: new-zealand-ird-number-boundary-validator
kind: tool
module: validohub
country: NZ
category: national-identifiers
status: active
name:
  en: "New Zealand IRD number boundary Validator"
summary:
  en: "Validate IRD number boundary shape, split date/control/body evidence, and prepare privacy-safe debugging output."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.new-zealand-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ID input"
        required: true
        placeholder:
          en: "123-456-789"
    actions: [validate, parse, format, explain]
related:
  explicit: [new-zealand-nzbn-validator, new-zealand-tax-id-validator]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "New Zealand IRD number boundary Validator | ValidoHub"
  description:
    en: "Validate IRD number boundary shape, split date/control/body evidence, and prepare privacy-safe debugging output."
