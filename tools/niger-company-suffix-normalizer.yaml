schemaVersion: 1
id: niger-company-suffix-normalizer
kind: tool
module: validohub
country: NE
category: developer-tools
status: active
name:
  en: "Nigerien Company Suffix Normalizer"
summary:
  en: "Normalize local company suffixes, registry labels, and search-key variants for onboarding forms."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.niger-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "SUFFIX input"
        required: true
        placeholder:
          en: "Niger Sample Holding Ltd NE-REG-123456"
    actions: [validate, parse, format, explain]
related:
  explicit: [niger-procurement-identifier-helper, niger-accessibility-locale-copy-checker]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Nigerien Company Suffix Normalizer | ValidoHub"
  description:
    en: "Normalize local company suffixes, registry labels, and search-key variants for onboarding forms."
