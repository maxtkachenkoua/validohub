schemaVersion: 1
id: niger-currency-decimal-formatter
kind: tool
module: validohub
country: NE
category: finance
status: active
name:
  en: "Nigerien XOF Decimal Currency Formatter"
summary:
  en: "Normalize XOF amount strings, decimal separators, grouping, and API-safe numeric previews."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.niger-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "CUR input"
        required: true
        placeholder:
          en: "1,234.56 XOF"
    actions: [validate, parse, format, explain]
related:
  explicit: [niger-tax-rate-sanity-helper, niger-tax-return-field-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Nigerien XOF Decimal Currency Formatter | ValidoHub"
  description:
    en: "Normalize XOF amount strings, decimal separators, grouping, and API-safe numeric previews."
