schemaVersion: 1
id: niger-tax-id-validator
kind: tool
module: validohub
country: NE
category: national-identifiers
status: active
name:
  en: "Nigerien tax number / VAT boundary Validator"
summary:
  en: "Normalize tax number / VAT boundary identifiers, inspect local tax body evidence, and prepare authority handoff diagnostics."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.niger-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "TAX input"
        required: true
        placeholder:
          en: "NE123456789"
    actions: [validate, parse, format, explain]
related:
  explicit: [niger-customs-importer-code-helper, niger-social-security-boundary-social-insurance-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Nigerien tax number / VAT boundary Validator | ValidoHub"
  description:
    en: "Normalize tax number / VAT boundary identifiers, inspect local tax body evidence, and prepare authority handoff diagnostics."
