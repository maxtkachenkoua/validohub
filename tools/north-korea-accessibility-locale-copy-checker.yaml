schemaVersion: 1
id: north-korea-accessibility-locale-copy-checker
kind: tool
module: validohub
country: KP
category: localization
status: active
name:
  en: "North Korean Locale Copy Checker"
summary:
  en: "Check UI labels for local identifier names, date/currency wording, and support-safe explanations."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.north-korea-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "COPY input"
        required: true
        placeholder:
          en: "resident certificate boundary input, postal code input, amount 1,234 KPW"
    actions: [validate, parse, format, explain]
related:
  explicit: [north-korea-support-ticket-scrubber, north-korea-integration-smoke-test-builder]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "North Korean Locale Copy Checker | ValidoHub"
  description:
    en: "Check UI labels for local identifier names, date/currency wording, and support-safe explanations."
