schemaVersion: 1
id: north-korea-address-transliteration-normalizer
kind: tool
module: validohub
country: KP
category: address
status: active
name:
  en: "North Korean Address Transliteration Normalizer"
summary:
  en: "Prepare ASCII-safe address keys while preserving local display text and search/debug evidence."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.north-korea-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ASCII input"
        required: true
        placeholder:
          en: "Central District 1, Pyongyang 999093"
    actions: [validate, parse, format, explain]
related:
  explicit: [north-korea-region-code-mapper, north-korea-municipality-code-inspector]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "North Korean Address Transliteration Normalizer | ValidoHub"
  description:
    en: "Prepare ASCII-safe address keys while preserving local display text and search/debug evidence."
