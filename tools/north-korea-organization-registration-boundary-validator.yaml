schemaVersion: 1
id: north-korea-organization-registration-boundary-validator
kind: tool
module: validohub
country: KP
category: national-identifiers
status: active
name:
  en: "North Korean organization registration boundary Validator"
summary:
  en: "Inspect organization registration boundary structure, registry-style prefixes, control digits, and official lookup boundaries."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.north-korea-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ORG input"
        required: true
        placeholder:
          en: "ORG-123456"
    actions: [validate, parse, format, explain]
related:
  explicit: [north-korea-tax-id-validator, north-korea-customs-importer-code-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "North Korean organization registration boundary Validator | ValidoHub"
  description:
    en: "Inspect organization registration boundary structure, registry-style prefixes, control digits, and official lookup boundaries."
