schemaVersion: 1
id: north-macedonia-address-transliteration-normalizer
kind: tool
module: validohub
country: MK
category: address
status: active
name:
  en: "Macedonian Address Transliteration Normalizer"
summary:
  en: "Prepare ASCII-safe address keys while preserving local display text and search/debug evidence."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.north-macedonia-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ASCII input"
        required: true
        placeholder:
          en: "Macedonia Square 1, 1000 Skopje"
    actions: [validate, parse, format, explain]
related:
  explicit: [north-macedonia-region-code-mapper, north-macedonia-municipality-code-inspector]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Macedonian Address Transliteration Normalizer | ValidoHub"
  description:
    en: "Prepare ASCII-safe address keys while preserving local display text and search/debug evidence."
