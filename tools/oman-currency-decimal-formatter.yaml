schemaVersion: 1
id: oman-currency-decimal-formatter
kind: tool
module: validohub
country: OM
category: finance
status: active
name:
  en: "Omani OMR Decimal Currency Formatter"
summary:
  en: "Normalize OMR amount strings, decimal separators, grouping, and API-safe numeric previews."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.oman-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "CUR input"
        required: true
        placeholder:
          en: "1,234.560 OMR"
    actions: [validate, parse, format, explain]
related:
  explicit: [oman-tax-rate-sanity-helper, oman-tax-return-field-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Omani OMR Decimal Currency Formatter | ValidoHub"
  description:
    en: "Normalize OMR amount strings, decimal separators, grouping, and API-safe numeric previews."
