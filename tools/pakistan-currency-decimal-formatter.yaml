schemaVersion: 1
id: pakistan-currency-decimal-formatter
kind: tool
module: validohub
country: PK
category: finance
status: active
name:
  en: "Pakistani PKR Decimal Currency Formatter"
summary:
  en: "Normalize PKR amount strings, decimal separators, grouping, and API-safe numeric previews."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.pakistan-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "CUR input"
        required: true
        placeholder:
          en: "1,234.56 PKR"
    actions: [validate, parse, format, explain]
related:
  explicit: [pakistan-tax-rate-sanity-helper, pakistan-tax-return-field-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Pakistani PKR Decimal Currency Formatter | ValidoHub"
  description:
    en: "Normalize PKR amount strings, decimal separators, grouping, and API-safe numeric previews."
