schemaVersion: 1
id: pakistan-vin-validator
kind: tool
module: validohub
country: PK
category: transport
status: active
name:
  en: "Pakistani VIN Validator"
summary:
  en: "Validate VIN shape, split WMI/VDS/VIS evidence, and prepare vehicle-intake diagnostics."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.pakistan-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "VIN input"
        required: true
        placeholder:
          en: "WVWZZZ1JZXW000001"
    actions: [validate, parse, format, explain]
related:
  explicit: [pakistan-vehicle-data-redaction-helper, pakistan-customs-declaration-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Pakistani VIN Validator | ValidoHub"
  description:
    en: "Validate VIN shape, split WMI/VDS/VIS evidence, and prepare vehicle-intake diagnostics."
