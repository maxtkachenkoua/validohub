schemaVersion: 1
id: palau-address-normalizer
kind: tool
module: validohub
country: PW
category: address
status: active
name:
  en: "Palauan Address Normalizer"
summary:
  en: "Normalize street, postal code, locality, region, and country lines for local address forms."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.palau-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ADDR input"
        required: true
        placeholder:
          en: "Ngerulmud, Melekeok State"
    actions: [validate, parse, format, explain]
related:
  explicit: [palau-address-transliteration-normalizer, palau-region-code-mapper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Palauan Address Normalizer | ValidoHub"
  description:
    en: "Normalize street, postal code, locality, region, and country lines for local address forms."
