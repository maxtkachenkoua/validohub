schemaVersion: 1
id: palau-business-register-readiness-helper
kind: tool
module: validohub
country: PW
category: government
status: active
name:
  en: "Palauan business registry Readiness Helper"
summary:
  en: "Prepare browser-only evidence before a regulated business registry lookup or company registry workflow."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.palau-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "REG input"
        required: true
        placeholder:
          en: "PW-REG-123456 PW123456789 Ngerulmud, Melekeok State"
    actions: [validate, parse, format, explain]
related:
  explicit: [palau-id-card-format-helper, palau-passport-number-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Palauan business registry Readiness Helper | ValidoHub"
  description:
    en: "Prepare browser-only evidence before a regulated business registry lookup or company registry workflow."
