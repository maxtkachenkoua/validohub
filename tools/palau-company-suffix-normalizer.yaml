schemaVersion: 1
id: palau-company-suffix-normalizer
kind: tool
module: validohub
country: PW
category: developer-tools
status: active
name:
  en: "Palauan Company Suffix Normalizer"
summary:
  en: "Normalize local company suffixes, registry labels, and search-key variants for onboarding forms."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.palau-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "SUFFIX input"
        required: true
        placeholder:
          en: "Palau Sample Holding Ltd PW-REG-123456"
    actions: [validate, parse, format, explain]
related:
  explicit: [palau-procurement-identifier-helper, palau-accessibility-locale-copy-checker]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Palauan Company Suffix Normalizer | ValidoHub"
  description:
    en: "Normalize local company suffixes, registry labels, and search-key variants for onboarding forms."
