schemaVersion: 1
id: palestine-accessibility-locale-copy-checker
kind: tool
module: validohub
country: PS
category: localization
status: active
name:
  en: "Palestinian Locale Copy Checker"
summary:
  en: "Check UI labels for local identifier names, date/currency wording, and support-safe explanations."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.palestine-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "COPY input"
        required: true
        placeholder:
          en: "ID number boundary input, postal code input, amount 1,234.56 ILS"
    actions: [validate, parse, format, explain]
related:
  explicit: [palestine-support-ticket-scrubber, palestine-integration-smoke-test-builder]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Palestinian Locale Copy Checker | ValidoHub"
  description:
    en: "Check UI labels for local identifier names, date/currency wording, and support-safe explanations."
