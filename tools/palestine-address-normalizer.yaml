schemaVersion: 1
id: palestine-address-normalizer
kind: tool
module: validohub
country: PS
category: address
status: active
name:
  en: "Palestinian Address Normalizer"
summary:
  en: "Normalize street, postal code, locality, region, and country lines for local address forms."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.palestine-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ADDR input"
        required: true
        placeholder:
          en: "Al-Manara 1, Ramallah P600"
    actions: [validate, parse, format, explain]
related:
  explicit: [palestine-address-transliteration-normalizer, palestine-region-code-mapper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Palestinian Address Normalizer | ValidoHub"
  description:
    en: "Normalize street, postal code, locality, region, and country lines for local address forms."
