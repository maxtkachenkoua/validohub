schemaVersion: 1
id: palestine-id-number-boundary-validator
kind: tool
module: validohub
country: PS
category: national-identifiers
status: active
name:
  en: "Palestinian ID number boundary Validator"
summary:
  en: "Validate ID number boundary shape, split date/control/body evidence, and prepare privacy-safe debugging output."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.palestine-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ID input"
        required: true
        placeholder:
          en: "401234567"
    actions: [validate, parse, format, explain]
related:
  explicit: [palestine-company-registration-number-validator, palestine-tax-id-validator]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Palestinian ID number boundary Validator | ValidoHub"
  description:
    en: "Validate ID number boundary shape, split date/control/body evidence, and prepare privacy-safe debugging output."
