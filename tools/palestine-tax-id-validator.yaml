schemaVersion: 1
id: palestine-tax-id-validator
kind: tool
module: validohub
country: PS
category: national-identifiers
status: active
name:
  en: "Palestinian tax number / VAT Validator"
summary:
  en: "Normalize tax number / VAT identifiers, inspect local tax body evidence, and prepare authority handoff diagnostics."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.palestine-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "TAX input"
        required: true
        placeholder:
          en: "562345678"
    actions: [validate, parse, format, explain]
related:
  explicit: [palestine-customs-importer-code-helper, palestine-social-insurance-boundary-social-insurance-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Palestinian tax number / VAT Validator | ValidoHub"
  description:
    en: "Normalize tax number / VAT identifiers, inspect local tax body evidence, and prepare authority handoff diagnostics."
