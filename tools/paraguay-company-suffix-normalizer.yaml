schemaVersion: 1
id: paraguay-company-suffix-normalizer
kind: tool
module: validohub
country: PY
category: developer-tools
status: active
name:
  en: "Paraguayan Company Suffix Normalizer"
summary:
  en: "Normalize local company suffixes, registry labels, and search-key variants for onboarding forms."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.paraguay-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "SUFFIX input"
        required: true
        placeholder:
          en: "Paraguay Sample Holding Ltd 80012345-6"
    actions: [validate, parse, format, explain]
related:
  explicit: [paraguay-procurement-identifier-helper, paraguay-accessibility-locale-copy-checker]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Paraguayan Company Suffix Normalizer | ValidoHub"
  description:
    en: "Normalize local company suffixes, registry labels, and search-key variants for onboarding forms."
