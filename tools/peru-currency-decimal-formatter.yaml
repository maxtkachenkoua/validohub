schemaVersion: 1
id: peru-currency-decimal-formatter
kind: tool
module: validohub
country: PE
category: finance
status: active
name:
  en: "Peruvian PEN Decimal Currency Formatter"
summary:
  en: "Normalize PEN amount strings, decimal separators, grouping, and API-safe numeric previews."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.peru-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "CUR input"
        required: true
        placeholder:
          en: "1,234.56 PEN"
    actions: [validate, parse, format, explain]
related:
  explicit: [peru-vat-rate-sanity-helper, peru-vat-return-field-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Peruvian PEN Decimal Currency Formatter | ValidoHub"
  description:
    en: "Normalize PEN amount strings, decimal separators, grouping, and API-safe numeric previews."
