schemaVersion: 1
id: pesel-validator
kind: tool
module: validohub
country: PL
category: national-identifiers
status: active
name:
  en: PESEL Validator & Explainer
summary:
  en: Validate, parse, generate, and explain Polish PESEL numbers.
capabilities:
  - validate
  - parse
  - generate
  - explain
algorithm:
  algorithmId: validohub.pesel
forms:
  validate:
    inputs:
      - type: text
        name: pesel
        label:
          en: PESEL
        required: true
        pattern: "^[0-9]{11}$"
    actions: [validate, parse, explain]
  generate:
    inputs:
      - type: select
        name: gender
        label:
          en: Gender
        options: [female, male, any]
        default: any
      - type: text
        name: birthDate
        label:
          en: Birth date
        placeholder:
          en: YYYY-MM-DD
    actions: [generate]
related:
  explicit: []
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
    sameModule: true
seo:
  title:
    en: PESEL Validator & Explainer
  description:
    en: Validate and explain Polish PESEL numbers with checksum and metadata.
