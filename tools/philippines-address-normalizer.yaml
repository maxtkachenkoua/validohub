schemaVersion: 1
id: philippines-address-normalizer
kind: tool
module: validohub
country: PH
category: address
status: active
name:
  en: "Philippine Address Normalizer"
summary:
  en: "Normalize street, postal code, locality, region, and country lines for local address forms."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.philippines-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ADDR input"
        required: true
        placeholder:
          en: "Ayala Avenue 1, Makati 1226"
    actions: [validate, parse, format, explain]
related:
  explicit: [philippines-address-transliteration-normalizer, philippines-region-code-mapper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Philippine Address Normalizer | ValidoHub"
  description:
    en: "Normalize street, postal code, locality, region, and country lines for local address forms."
