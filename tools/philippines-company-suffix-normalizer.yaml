schemaVersion: 1
id: philippines-company-suffix-normalizer
kind: tool
module: validohub
country: PH
category: developer-tools
status: active
name:
  en: "Philippine Company Suffix Normalizer"
summary:
  en: "Normalize local company suffixes, registry labels, and search-key variants for onboarding forms."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.philippines-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "SUFFIX input"
        required: true
        placeholder:
          en: "Philippines Sample Holding Ltd CS202012345"
    actions: [validate, parse, format, explain]
related:
  explicit: [philippines-procurement-identifier-helper, philippines-accessibility-locale-copy-checker]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Philippine Company Suffix Normalizer | ValidoHub"
  description:
    en: "Normalize local company suffixes, registry labels, and search-key variants for onboarding forms."
