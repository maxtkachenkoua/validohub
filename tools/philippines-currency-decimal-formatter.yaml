schemaVersion: 1
id: philippines-currency-decimal-formatter
kind: tool
module: validohub
country: PH
category: finance
status: active
name:
  en: "Philippine PHP Decimal Currency Formatter"
summary:
  en: "Normalize PHP amount strings, decimal separators, grouping, and API-safe numeric previews."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.philippines-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "CUR input"
        required: true
        placeholder:
          en: "1,234.56 PHP"
    actions: [validate, parse, format, explain]
related:
  explicit: [philippines-tax-rate-sanity-helper, philippines-tax-return-field-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Philippine PHP Decimal Currency Formatter | ValidoHub"
  description:
    en: "Normalize PHP amount strings, decimal separators, grouping, and API-safe numeric previews."
