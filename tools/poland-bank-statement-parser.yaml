schemaVersion: 1
id: poland-bank-statement-parser
kind: tool
module: validohub
country: PL
category: finance
status: active
name:
  en: "Polish Bank Statement Parser"
summary:
  en: "Parse pasted Polish bank statement rows locally, detect dates, amounts, IBANs, references, and export an audit summary."
capabilities:
  - validate
  - parse
  - generate
  - explain
algorithm:
  algorithmId: validohub.poland-baseline
forms:
  validate:
    inputs:
      - name: input
        type: text
        required: true
    actions:
      - validate
      - parse
      - generate
      - explain
related:
  explicit: [pesel-validator, poland-nip-validator, poland-regon-validator, poland-iban-nrb-validator]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
    sameModule: true
seo:
  title:
    en: "Polish Bank Statement Parser | ValidoHub"
  description:
    en: "Parse pasted Polish bank statement rows locally, detect dates, amounts, IBANs, references, and export an audit summary."
