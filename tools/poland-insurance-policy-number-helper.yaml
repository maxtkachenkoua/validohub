schemaVersion: 1
id: poland-insurance-policy-number-helper
kind: tool
module: validohub
country: PL
category: finance
status: active
name:
  en: "Polish Insurance / Policy Number Helper"
summary:
  en: "Audit Polish insurance policy references locally, detect dates, vehicle or person identifiers, and produce masked support-safe summaries."
capabilities:
  - validate
  - parse
  - generate
  - explain
algorithm:
  algorithmId: validohub.poland-baseline
forms:
  validate:
    inputs:
      - name: input
        type: text
        required: true
    actions:
      - validate
      - parse
      - generate
      - explain
related:
  explicit: [pesel-validator, poland-nip-validator, poland-regon-validator, poland-iban-nrb-validator]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
    sameModule: true
seo:
  title:
    en: "Polish Insurance / Policy Number Helper | ValidoHub"
  description:
    en: "Audit Polish insurance policy references locally, detect dates, vehicle or person identifiers, and produce masked support-safe summaries."
