schemaVersion: 1
id: poland-mrz-passport-id-parser
kind: tool
module: validohub
country: PL
category: national-identifiers
status: active
name:
  en: "Polish MRZ Passport / ID Parser"
summary:
  en: "Parse Polish passport or ID MRZ lines locally, verify MRZ check digits where possible, and expose travel-document fields safely."
capabilities:
  - validate
  - parse
  - generate
  - explain
algorithm:
  algorithmId: validohub.poland-baseline
forms:
  validate:
    inputs:
      - name: input
        type: text
        required: true
    actions:
      - validate
      - parse
      - generate
      - explain
related:
  explicit: [pesel-validator, poland-nip-validator, poland-regon-validator, poland-iban-nrb-validator]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
    sameModule: true
seo:
  title:
    en: "Polish MRZ Passport / ID Parser | ValidoHub"
  description:
    en: "Parse Polish passport or ID MRZ lines locally, verify MRZ check digits where possible, and expose travel-document fields safely."
