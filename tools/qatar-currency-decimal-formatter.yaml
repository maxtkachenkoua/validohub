schemaVersion: 1
id: qatar-currency-decimal-formatter
kind: tool
module: validohub
country: QA
category: finance
status: active
name:
  en: "Qatari QAR Decimal Currency Formatter"
summary:
  en: "Normalize QAR amount strings, decimal separators, grouping, and API-safe numeric previews."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.qatar-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "CUR input"
        required: true
        placeholder:
          en: "1,234.56 QAR"
    actions: [validate, parse, format, explain]
related:
  explicit: [qatar-tax-rate-sanity-helper, qatar-tax-return-field-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Qatari QAR Decimal Currency Formatter | ValidoHub"
  description:
    en: "Normalize QAR amount strings, decimal separators, grouping, and API-safe numeric previews."
