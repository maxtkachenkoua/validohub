schemaVersion: 1
id: rwanda-address-normalizer
kind: tool
module: validohub
country: RW
category: address
status: active
name:
  en: "Rwandan Address Normalizer"
summary:
  en: "Normalize street, postal code, locality, region, and country lines for local address forms."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.rwanda-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ADDR input"
        required: true
        placeholder:
          en: "Central business district, Kigali 1000"
    actions: [validate, parse, format, explain]
related:
  explicit: [rwanda-address-transliteration-normalizer, rwanda-region-code-mapper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Rwandan Address Normalizer | ValidoHub"
  description:
    en: "Normalize street, postal code, locality, region, and country lines for local address forms."
