schemaVersion: 1
id: rwanda-currency-decimal-formatter
kind: tool
module: validohub
country: RW
category: finance
status: active
name:
  en: "Rwandan RWF Decimal Currency Formatter"
summary:
  en: "Normalize RWF amount strings, decimal separators, grouping, and API-safe numeric previews."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.rwanda-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "CUR input"
        required: true
        placeholder:
          en: "1,234.56 RWF"
    actions: [validate, parse, format, explain]
related:
  explicit: [rwanda-tax-rate-sanity-helper, rwanda-tax-return-field-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Rwandan RWF Decimal Currency Formatter | ValidoHub"
  description:
    en: "Normalize RWF amount strings, decimal separators, grouping, and API-safe numeric previews."
