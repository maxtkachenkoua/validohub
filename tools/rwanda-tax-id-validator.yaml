schemaVersion: 1
id: rwanda-tax-id-validator
kind: tool
module: validohub
country: RW
category: national-identifiers
status: active
name:
  en: "Rwandan tax number / VAT boundary Validator"
summary:
  en: "Normalize tax number / VAT boundary identifiers, inspect local tax body evidence, and prepare authority handoff diagnostics."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.rwanda-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "TAX input"
        required: true
        placeholder:
          en: "RW123456789"
    actions: [validate, parse, format, explain]
related:
  explicit: [rwanda-customs-importer-code-helper, rwanda-social-security-boundary-social-insurance-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Rwandan tax number / VAT boundary Validator | ValidoHub"
  description:
    en: "Normalize tax number / VAT boundary identifiers, inspect local tax body evidence, and prepare authority handoff diagnostics."
