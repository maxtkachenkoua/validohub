schemaVersion: 1
id: samoa-customs-declaration-helper
kind: tool
module: validohub
country: WS
category: government
status: active
name:
  en: "Samoan Customs Declaration Helper"
summary:
  en: "Inspect importer code, tax, invoice, amount, HS-code, and border handoff evidence without official filing."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.samoa-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "CUSTOMS input"
        required: true
        placeholder:
          en: "WS123456789 HS 8471 1,234.56 WST"
    actions: [validate, parse, format, explain]
related:
  explicit: [samoa-postal-tracking-helper, samoa-data-quality-workbench]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Samoan Customs Declaration Helper | ValidoHub"
  description:
    en: "Inspect importer code, tax, invoice, amount, HS-code, and border handoff evidence without official filing."
