schemaVersion: 1
id: samoa-tax-id-boundary-validator
kind: tool
module: validohub
country: WS
category: national-identifiers
status: active
name:
  en: "Samoan tax ID boundary Validator"
summary:
  en: "Validate tax ID boundary shape, split date/control/body evidence, and prepare privacy-safe debugging output."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.samoa-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ID input"
        required: true
        placeholder:
          en: "WS-TIN-123456"
    actions: [validate, parse, format, explain]
related:
  explicit: [samoa-company-registration-number-validator, samoa-tax-id-validator]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Samoan tax ID boundary Validator | ValidoHub"
  description:
    en: "Validate tax ID boundary shape, split date/control/body evidence, and prepare privacy-safe debugging output."
