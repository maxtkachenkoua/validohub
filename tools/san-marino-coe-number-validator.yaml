schemaVersion: 1
id: san-marino-coe-number-validator
kind: tool
module: validohub
country: SM
category: national-identifiers
status: active
name:
  en: "Sammarinese COE number Validator"
summary:
  en: "Inspect COE number structure, registry-style prefixes, control digits, and official lookup boundaries."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.san-marino-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ORG input"
        required: true
        placeholder:
          en: "SM12345"
    actions: [validate, parse, format, explain]
related:
  explicit: [san-marino-vat-id-validator, san-marino-eori-validator]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Sammarinese COE number Validator | ValidoHub"
  description:
    en: "Inspect COE number structure, registry-style prefixes, control digits, and official lookup boundaries."
