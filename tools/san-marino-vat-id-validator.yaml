schemaVersion: 1
id: san-marino-vat-id-validator
kind: tool
module: validohub
country: SM
category: national-identifiers
status: active
name:
  en: "Sammarinese VAT ID / SM Prefix Validator"
summary:
  en: "Normalize SM VAT identifiers, inspect local tax body evidence, and prepare VIES handoff diagnostics."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.san-marino-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "VAT input"
        required: true
        placeholder:
          en: "SM12345"
    actions: [validate, parse, format, explain]
related:
  explicit: [san-marino-eori-validator, san-marino-iss-code-social-insurance-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Sammarinese VAT ID / SM Prefix Validator | ValidoHub"
  description:
    en: "Normalize SM VAT identifiers, inspect local tax body evidence, and prepare VIES handoff diagnostics."
