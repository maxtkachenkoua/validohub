schemaVersion: 1
id: sao-tome-and-principe-address-transliteration-normalizer
kind: tool
module: validohub
country: ST
category: address
status: active
name:
  en: "Sao Tomean Address Transliteration Normalizer"
summary:
  en: "Prepare ASCII-safe address keys while preserving local display text and search/debug evidence."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.sao-tome-and-principe-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ASCII input"
        required: true
        placeholder:
          en: "Central business district, Sao Tome 1000"
    actions: [validate, parse, format, explain]
related:
  explicit: [sao-tome-and-principe-region-code-mapper, sao-tome-and-principe-municipality-code-inspector]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Sao Tomean Address Transliteration Normalizer | ValidoHub"
  description:
    en: "Prepare ASCII-safe address keys while preserving local display text and search/debug evidence."
