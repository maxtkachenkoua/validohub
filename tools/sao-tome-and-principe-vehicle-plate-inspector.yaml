schemaVersion: 1
id: sao-tome-and-principe-vehicle-plate-inspector
kind: tool
module: validohub
country: ST
category: transport
status: active
name:
  en: "Sao Tomean Vehicle Plate Inspector"
summary:
  en: "Inspect vehicle plate shape, regional hints, serial blocks, and official vehicle registry boundaries."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.sao-tome-and-principe-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "PLATE input"
        required: true
        placeholder:
          en: "ST 1234"
    actions: [validate, parse, format, explain]
related:
  explicit: [sao-tome-and-principe-vin-validator, sao-tome-and-principe-vehicle-data-redaction-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Sao Tomean Vehicle Plate Inspector | ValidoHub"
  description:
    en: "Inspect vehicle plate shape, regional hints, serial blocks, and official vehicle registry boundaries."
