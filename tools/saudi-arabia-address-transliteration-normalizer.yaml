schemaVersion: 1
id: saudi-arabia-address-transliteration-normalizer
kind: tool
module: validohub
country: SA
category: address
status: active
name:
  en: "Saudi Address Transliteration Normalizer"
summary:
  en: "Prepare ASCII-safe address keys while preserving local display text and search/debug evidence."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.saudi-arabia-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ASCII input"
        required: true
        placeholder:
          en: "King Fahd Road 1, Riyadh 12211"
    actions: [validate, parse, format, explain]
related:
  explicit: [saudi-arabia-region-code-mapper, saudi-arabia-municipality-code-inspector]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Saudi Address Transliteration Normalizer | ValidoHub"
  description:
    en: "Prepare ASCII-safe address keys while preserving local display text and search/debug evidence."
