schemaVersion: 1
id: saudi-arabia-bank-account-validator
kind: tool
module: validohub
country: SA
category: finance
status: active
name:
  en: "Saudi Arabia Bank Account Validator"
summary:
  en: "Validate local bank-account or routing shape, split bank/account blocks, and keep ownership lookup outside the browser."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.saudi-arabia-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "BANK input"
        required: true
        placeholder:
          en: "80 000000608010167519"
    actions: [validate, parse, format, explain]
related:
  explicit: [saudi-arabia-bank-account-fixture-generator, saudi-arabia-bank-account-inspector]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Saudi Arabia Bank Account Validator | ValidoHub"
  description:
    en: "Validate local bank-account or routing shape, split bank/account blocks, and keep ownership lookup outside the browser."
