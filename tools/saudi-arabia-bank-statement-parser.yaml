schemaVersion: 1
id: saudi-arabia-bank-statement-parser
kind: tool
module: validohub
country: SA
category: finance
status: active
name:
  en: "Saudi Bank Statement Parser"
summary:
  en: "Parse statement rows for date, amount, account/routing evidence, counterparty, reference, and local decimal conventions."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.saudi-arabia-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "STMT input"
        required: true
        placeholder:
          en: "21/07/2026; 1,234.56 SAR; 80 000000608010167519; sample counterparty"
    actions: [validate, parse, format, explain]
related:
  explicit: [saudi-arabia-masked-bank-account-formatter, saudi-arabia-currency-decimal-formatter]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Saudi Bank Statement Parser | ValidoHub"
  description:
    en: "Parse statement rows for date, amount, account/routing evidence, counterparty, reference, and local decimal conventions."
