schemaVersion: 1
id: saudi-arabia-currency-decimal-formatter
kind: tool
module: validohub
country: SA
category: finance
status: active
name:
  en: "Saudi SAR Decimal Currency Formatter"
summary:
  en: "Normalize SAR amount strings, decimal separators, grouping, and API-safe numeric previews."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.saudi-arabia-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "CUR input"
        required: true
        placeholder:
          en: "1,234.56 SAR"
    actions: [validate, parse, format, explain]
related:
  explicit: [saudi-arabia-tax-rate-sanity-helper, saudi-arabia-tax-return-field-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Saudi SAR Decimal Currency Formatter | ValidoHub"
  description:
    en: "Normalize SAR amount strings, decimal separators, grouping, and API-safe numeric previews."
