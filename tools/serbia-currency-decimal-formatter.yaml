schemaVersion: 1
id: serbia-currency-decimal-formatter
kind: tool
module: validohub
country: RS
category: finance
status: active
name:
  en: "Serbian RSD Decimal Currency Formatter"
summary:
  en: "Normalize RSD amount strings, decimal separators, grouping, and API-safe numeric previews."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.serbia-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "CUR input"
        required: true
        placeholder:
          en: "1.234,56 RSD"
    actions: [validate, parse, format, explain]
related:
  explicit: [serbia-vat-rate-sanity-helper, serbia-vat-return-field-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Serbian RSD Decimal Currency Formatter | ValidoHub"
  description:
    en: "Normalize RSD amount strings, decimal separators, grouping, and API-safe numeric previews."
