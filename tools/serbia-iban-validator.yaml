schemaVersion: 1
id: serbia-iban-validator
kind: tool
module: validohub
country: RS
category: finance
status: active
name:
  en: "Serbia IBAN Validator"
summary:
  en: "Validate RS IBAN shape, replay MOD-97 evidence, and split bank/account blocks for debugging."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.serbia-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "IBAN input"
        required: true
        placeholder:
          en: "RS35260005601001611379"
    actions: [validate, parse, format, explain]
related:
  explicit: [serbia-iban-generator, serbia-bank-account-inspector]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Serbia IBAN Validator | ValidoHub"
  description:
    en: "Validate RS IBAN shape, replay MOD-97 evidence, and split bank/account blocks for debugging."
