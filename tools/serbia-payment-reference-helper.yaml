schemaVersion: 1
id: serbia-payment-reference-helper
kind: tool
module: validohub
country: RS
category: finance
status: active
name:
  en: "Serbian model i poziv na broj Reference Helper"
summary:
  en: "Inspect local payment references, invoice links, amount evidence, and reconciliation-safe formatting."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.serbia-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "PAY input"
        required: true
        placeholder:
          en: "model i poziv na broj REF 2026-001 1.234,56 RSD"
    actions: [validate, parse, format, explain]
related:
  explicit: [serbia-remittance-text-builder, serbia-payment-reconciliation-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Serbian model i poziv na broj Reference Helper | ValidoHub"
  description:
    en: "Inspect local payment references, invoice links, amount evidence, and reconciliation-safe formatting."
