schemaVersion: 1
id: serbia-vat-id-validator
kind: tool
module: validohub
country: RS
category: national-identifiers
status: active
name:
  en: "Serbian VAT ID / RS Prefix Validator"
summary:
  en: "Normalize RS VAT identifiers, inspect local tax body evidence, and prepare VIES handoff diagnostics."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.serbia-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "VAT input"
        required: true
        placeholder:
          en: "RS100123456"
    actions: [validate, parse, format, explain]
related:
  explicit: [serbia-eori-validator, serbia-jmbg-social-insurance-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Serbian VAT ID / RS Prefix Validator | ValidoHub"
  description:
    en: "Normalize RS VAT identifiers, inspect local tax body evidence, and prepare VIES handoff diagnostics."
