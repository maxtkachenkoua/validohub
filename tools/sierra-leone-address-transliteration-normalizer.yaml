schemaVersion: 1
id: sierra-leone-address-transliteration-normalizer
kind: tool
module: validohub
country: SL
category: address
status: active
name:
  en: "Sierra Leonean Address Transliteration Normalizer"
summary:
  en: "Prepare ASCII-safe address keys while preserving local display text and search/debug evidence."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.sierra-leone-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ASCII input"
        required: true
        placeholder:
          en: "Central business district, Freetown 1000"
    actions: [validate, parse, format, explain]
related:
  explicit: [sierra-leone-region-code-mapper, sierra-leone-municipality-code-inspector]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Sierra Leonean Address Transliteration Normalizer | ValidoHub"
  description:
    en: "Prepare ASCII-safe address keys while preserving local display text and search/debug evidence."
