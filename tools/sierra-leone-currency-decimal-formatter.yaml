schemaVersion: 1
id: sierra-leone-currency-decimal-formatter
kind: tool
module: validohub
country: SL
category: finance
status: active
name:
  en: "Sierra Leonean SLE Decimal Currency Formatter"
summary:
  en: "Normalize SLE amount strings, decimal separators, grouping, and API-safe numeric previews."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.sierra-leone-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "CUR input"
        required: true
        placeholder:
          en: "1,234.56 SLE"
    actions: [validate, parse, format, explain]
related:
  explicit: [sierra-leone-tax-rate-sanity-helper, sierra-leone-tax-return-field-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Sierra Leonean SLE Decimal Currency Formatter | ValidoHub"
  description:
    en: "Normalize SLE amount strings, decimal separators, grouping, and API-safe numeric previews."
