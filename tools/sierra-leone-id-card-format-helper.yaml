schemaVersion: 1
id: sierra-leone-id-card-format-helper
kind: tool
module: validohub
country: SL
category: national-identifiers
status: active
name:
  en: "Sierra Leonean ID Card Format Helper"
summary:
  en: "Inspect local identity-card snippets, document numbers, dates, and privacy boundaries without proving identity."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.sierra-leone-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "CARD input"
        required: true
        placeholder:
          en: "SL-ID-123456"
    actions: [validate, parse, format, explain]
related:
  explicit: [sierra-leone-passport-number-helper, sierra-leone-mrz-passport-parser]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Sierra Leonean ID Card Format Helper | ValidoHub"
  description:
    en: "Inspect local identity-card snippets, document numbers, dates, and privacy boundaries without proving identity."
