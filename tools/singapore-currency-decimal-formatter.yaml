schemaVersion: 1
id: singapore-currency-decimal-formatter
kind: tool
module: validohub
country: SG
category: finance
status: active
name:
  en: "Singapore SGD Decimal Currency Formatter"
summary:
  en: "Normalize SGD amount strings, decimal separators, grouping, and API-safe numeric previews."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.singapore-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "CUR input"
        required: true
        placeholder:
          en: "1,234.56 SGD"
    actions: [validate, parse, format, explain]
related:
  explicit: [singapore-tax-rate-sanity-helper, singapore-tax-return-field-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Singapore SGD Decimal Currency Formatter | ValidoHub"
  description:
    en: "Normalize SGD amount strings, decimal separators, grouping, and API-safe numeric previews."
