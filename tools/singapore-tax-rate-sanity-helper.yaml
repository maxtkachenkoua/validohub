schemaVersion: 1
id: singapore-tax-rate-sanity-helper
kind: tool
module: validohub
country: SG
category: tax
status: active
name:
  en: "Singapore Tax Rate Sanity Helper"
summary:
  en: "Inspect tax-rate snippets, local tax labels, amount bases, and official rate lookup boundaries."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.singapore-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "RATE input"
        required: true
        placeholder:
          en: "GST 20% base 1,234.56 SGD"
    actions: [validate, parse, format, explain]
related:
  explicit: [singapore-tax-return-field-helper, singapore-invoice-number-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Singapore Tax Rate Sanity Helper | ValidoHub"
  description:
    en: "Inspect tax-rate snippets, local tax labels, amount bases, and official rate lookup boundaries."
