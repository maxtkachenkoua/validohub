schemaVersion: 1
id: slovakia-address-normalizer
kind: tool
module: validohub
country: SK
category: address
status: active
name:
  en: "Slovak Address Normalizer"
summary:
  en: "Normalize street, postal code, locality, region, and country lines for local address forms."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.slovakia-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ADDR input"
        required: true
        placeholder:
          en: "Hlavne namestie 1, 811 01 Bratislava"
    actions: [validate, parse, format, explain]
related:
  explicit: [slovakia-address-transliteration-normalizer, slovakia-region-code-mapper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Slovak Address Normalizer | ValidoHub"
  description:
    en: "Normalize street, postal code, locality, region, and country lines for local address forms."
