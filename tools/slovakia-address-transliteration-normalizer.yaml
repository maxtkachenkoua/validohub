schemaVersion: 1
id: slovakia-address-transliteration-normalizer
kind: tool
module: validohub
country: SK
category: address
status: active
name:
  en: "Slovak Address Transliteration Normalizer"
summary:
  en: "Prepare ASCII-safe address keys while preserving local display text and search/debug evidence."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.slovakia-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ASCII input"
        required: true
        placeholder:
          en: "Hlavne namestie 1, 811 01 Bratislava"
    actions: [validate, parse, format, explain]
related:
  explicit: [slovakia-region-code-mapper, slovakia-municipality-code-inspector]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Slovak Address Transliteration Normalizer | ValidoHub"
  description:
    en: "Prepare ASCII-safe address keys while preserving local display text and search/debug evidence."
