schemaVersion: 1
id: slovakia-masked-iban-formatter
kind: tool
module: validohub
country: SK
category: finance
status: active
name:
  en: "Slovak Masked IBAN Formatter"
summary:
  en: "Create log-safe IBAN previews while preserving country, check digits, and account-tail evidence."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.slovakia-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "MASK input"
        required: true
        placeholder:
          en: "SK3112000000198742637541"
    actions: [validate, parse, format, explain]
related:
  explicit: [slovakia-currency-decimal-formatter, slovakia-vat-rate-sanity-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Slovak Masked IBAN Formatter | ValidoHub"
  description:
    en: "Create log-safe IBAN previews while preserving country, check digits, and account-tail evidence."
