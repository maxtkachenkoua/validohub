schemaVersion: 1
id: slovenia-emso-validator
kind: tool
module: validohub
country: SI
category: national-identifiers
status: active
name:
  en: "Slovenian EMSO Validator"
summary:
  en: "Validate EMSO shape, split date/control/body evidence, and prepare privacy-safe debugging output."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.slovenia-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ID input"
        required: true
        placeholder:
          en: "0101980500123"
    actions: [validate, parse, format, explain]
related:
  explicit: [slovenia-maticna-stevilka-validator, slovenia-vat-id-validator]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Slovenian EMSO Validator | ValidoHub"
  description:
    en: "Validate EMSO shape, split date/control/body evidence, and prepare privacy-safe debugging output."
