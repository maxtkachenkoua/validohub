schemaVersion: 1
id: solomon-islands-address-normalizer
kind: tool
module: validohub
country: SB
category: address
status: active
name:
  en: "Solomon Islands Address Normalizer"
summary:
  en: "Normalize street, postal code, locality, region, and country lines for local address forms."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.solomon-islands-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ADDR input"
        required: true
        placeholder:
          en: "Mendana Avenue, Honiara"
    actions: [validate, parse, format, explain]
related:
  explicit: [solomon-islands-address-transliteration-normalizer, solomon-islands-region-code-mapper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Solomon Islands Address Normalizer | ValidoHub"
  description:
    en: "Normalize street, postal code, locality, region, and country lines for local address forms."
