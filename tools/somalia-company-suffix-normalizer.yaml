schemaVersion: 1
id: somalia-company-suffix-normalizer
kind: tool
module: validohub
country: SO
category: developer-tools
status: active
name:
  en: "Somali Company Suffix Normalizer"
summary:
  en: "Normalize local company suffixes, registry labels, and search-key variants for onboarding forms."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.somalia-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "SUFFIX input"
        required: true
        placeholder:
          en: "Somalia Sample Holding Ltd SO-REG-123456"
    actions: [validate, parse, format, explain]
related:
  explicit: [somalia-procurement-identifier-helper, somalia-accessibility-locale-copy-checker]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Somali Company Suffix Normalizer | ValidoHub"
  description:
    en: "Normalize local company suffixes, registry labels, and search-key variants for onboarding forms."
