schemaVersion: 1
id: south-sudan-currency-decimal-formatter
kind: tool
module: validohub
country: SS
category: finance
status: active
name:
  en: "South Sudanese SSP Decimal Currency Formatter"
summary:
  en: "Normalize SSP amount strings, decimal separators, grouping, and API-safe numeric previews."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.south-sudan-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "CUR input"
        required: true
        placeholder:
          en: "1,234.56 SSP"
    actions: [validate, parse, format, explain]
related:
  explicit: [south-sudan-tax-rate-sanity-helper, south-sudan-tax-return-field-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "South Sudanese SSP Decimal Currency Formatter | ValidoHub"
  description:
    en: "Normalize SSP amount strings, decimal separators, grouping, and API-safe numeric previews."
