schemaVersion: 1
id: sri-lanka-address-normalizer
kind: tool
module: validohub
country: LK
category: address
status: active
name:
  en: "Sri Lankan Address Normalizer"
summary:
  en: "Normalize street, postal code, locality, region, and country lines for local address forms."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.sri-lanka-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ADDR input"
        required: true
        placeholder:
          en: "Galle Road 1, Colombo 00100"
    actions: [validate, parse, format, explain]
related:
  explicit: [sri-lanka-address-transliteration-normalizer, sri-lanka-region-code-mapper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Sri Lankan Address Normalizer | ValidoHub"
  description:
    en: "Normalize street, postal code, locality, region, and country lines for local address forms."
