schemaVersion: 1
id: sri-lanka-bank-account-validator
kind: tool
module: validohub
country: LK
category: finance
status: active
name:
  en: "Sri Lanka Bank Account Validator"
summary:
  en: "Validate local bank-account or routing shape, split bank/account blocks, and keep ownership lookup outside the browser."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.sri-lanka-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "BANK input"
        required: true
        placeholder:
          en: "7010 1234567890"
    actions: [validate, parse, format, explain]
related:
  explicit: [sri-lanka-bank-account-fixture-generator, sri-lanka-bank-account-inspector]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Sri Lanka Bank Account Validator | ValidoHub"
  description:
    en: "Validate local bank-account or routing shape, split bank/account blocks, and keep ownership lookup outside the browser."
