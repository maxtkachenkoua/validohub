schemaVersion: 1
id: sri-lanka-company-registration-number-validator
kind: tool
module: validohub
country: LK
category: national-identifiers
status: active
name:
  en: "Sri Lankan company registration number Validator"
summary:
  en: "Inspect company registration number structure, registry-style prefixes, control digits, and official lookup boundaries."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.sri-lanka-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ORG input"
        required: true
        placeholder:
          en: "PV123456"
    actions: [validate, parse, format, explain]
related:
  explicit: [sri-lanka-tax-id-validator, sri-lanka-customs-importer-code-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Sri Lankan company registration number Validator | ValidoHub"
  description:
    en: "Inspect company registration number structure, registry-style prefixes, control digits, and official lookup boundaries."
