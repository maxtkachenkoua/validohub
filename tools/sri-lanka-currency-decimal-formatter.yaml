schemaVersion: 1
id: sri-lanka-currency-decimal-formatter
kind: tool
module: validohub
country: LK
category: finance
status: active
name:
  en: "Sri Lankan LKR Decimal Currency Formatter"
summary:
  en: "Normalize LKR amount strings, decimal separators, grouping, and API-safe numeric previews."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.sri-lanka-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "CUR input"
        required: true
        placeholder:
          en: "1,234.56 LKR"
    actions: [validate, parse, format, explain]
related:
  explicit: [sri-lanka-tax-rate-sanity-helper, sri-lanka-tax-return-field-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Sri Lankan LKR Decimal Currency Formatter | ValidoHub"
  description:
    en: "Normalize LKR amount strings, decimal separators, grouping, and API-safe numeric previews."
