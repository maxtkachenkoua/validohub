schemaVersion: 1
id: sri-lanka-nic-boundary-validator
kind: tool
module: validohub
country: LK
category: national-identifiers
status: active
name:
  en: "Sri Lankan NIC boundary Validator"
summary:
  en: "Validate NIC boundary shape, split date/control/body evidence, and prepare privacy-safe debugging output."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.sri-lanka-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ID input"
        required: true
        placeholder:
          en: "199012345678"
    actions: [validate, parse, format, explain]
related:
  explicit: [sri-lanka-company-registration-number-validator, sri-lanka-tax-id-validator]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Sri Lankan NIC boundary Validator | ValidoHub"
  description:
    en: "Validate NIC boundary shape, split date/control/body evidence, and prepare privacy-safe debugging output."
