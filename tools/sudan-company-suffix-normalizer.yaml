schemaVersion: 1
id: sudan-company-suffix-normalizer
kind: tool
module: validohub
country: SD
category: developer-tools
status: active
name:
  en: "Sudanese Company Suffix Normalizer"
summary:
  en: "Normalize local company suffixes, registry labels, and search-key variants for onboarding forms."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.sudan-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "SUFFIX input"
        required: true
        placeholder:
          en: "Sudan Sample Holding Ltd SD-REG-123456"
    actions: [validate, parse, format, explain]
related:
  explicit: [sudan-procurement-identifier-helper, sudan-accessibility-locale-copy-checker]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Sudanese Company Suffix Normalizer | ValidoHub"
  description:
    en: "Normalize local company suffixes, registry labels, and search-key variants for onboarding forms."
