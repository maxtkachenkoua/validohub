schemaVersion: 1
id: sudan-regex-pack-helper
kind: tool
module: validohub
country: SD
category: developer-tools
status: active
name:
  en: "Sudanese Regex Pack Helper"
summary:
  en: "Prepare regex snippets for local identifiers, tax IDs, bank accounts, postal, phone, dates, and debug labels."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.sudan-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "REGEX input"
        required: true
        placeholder:
          en: "national ID boundary company registration number postal/address code  001 1234567890"
    actions: [validate, parse, format, explain]
related:
  explicit: [sudan-api-payload-auditor, sudan-form-field-auditor]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Sudanese Regex Pack Helper | ValidoHub"
  description:
    en: "Prepare regex snippets for local identifiers, tax IDs, bank accounts, postal, phone, dates, and debug labels."
