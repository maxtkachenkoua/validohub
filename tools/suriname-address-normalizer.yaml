schemaVersion: 1
id: suriname-address-normalizer
kind: tool
module: validohub
country: SR
category: address
status: active
name:
  en: "Surinamese Address Normalizer"
summary:
  en: "Normalize street, postal code, locality, region, and country lines for local address forms."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.suriname-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ADDR input"
        required: true
        placeholder:
          en: "Waterkant 1, Paramaribo"
    actions: [validate, parse, format, explain]
related:
  explicit: [suriname-address-transliteration-normalizer, suriname-region-code-mapper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Surinamese Address Normalizer | ValidoHub"
  description:
    en: "Normalize street, postal code, locality, region, and country lines for local address forms."
