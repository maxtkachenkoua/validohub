schemaVersion: 1
id: suriname-masked-bank-account-formatter
kind: tool
module: validohub
country: SR
category: finance
status: active
name:
  en: "Surinamese Masked Bank Account Formatter"
summary:
  en: "Create log-safe domestic account previews while preserving bank, branch, and account-tail evidence."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.suriname-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "MASK input"
        required: true
        placeholder:
          en: "001-123456789"
    actions: [validate, parse, format, explain]
related:
  explicit: [suriname-currency-decimal-formatter, suriname-vat-rate-sanity-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Surinamese Masked Bank Account Formatter | ValidoHub"
  description:
    en: "Create log-safe domestic account previews while preserving bank, branch, and account-tail evidence."
