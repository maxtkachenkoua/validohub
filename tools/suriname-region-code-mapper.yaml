schemaVersion: 1
id: suriname-region-code-mapper
kind: tool
module: validohub
country: SR
category: address
status: active
name:
  en: "Surinamese Region / Province Code Mapper"
summary:
  en: "Inspect regional abbreviations, locality evidence, postal zones, and official geography lookup boundaries."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.suriname-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "REGION input"
        required: true
        placeholder:
          en: "Paramaribo"
    actions: [validate, parse, format, explain]
related:
  explicit: [suriname-municipality-code-inspector, suriname-phone-number-validator]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Surinamese Region / Province Code Mapper | ValidoHub"
  description:
    en: "Inspect regional abbreviations, locality evidence, postal zones, and official geography lookup boundaries."
