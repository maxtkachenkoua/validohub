schemaVersion: 1
id: suriname-vat-rate-sanity-helper
kind: tool
module: validohub
country: SR
category: tax
status: active
name:
  en: "Surinamese VAT Rate Sanity Helper"
summary:
  en: "Inspect VAT-rate snippets, local tax labels, amount bases, and official rate lookup boundaries."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.suriname-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "RATE input"
        required: true
        placeholder:
          en: "BTW / tax number 20% base 1.234,56 SRD"
    actions: [validate, parse, format, explain]
related:
  explicit: [suriname-vat-return-field-helper, suriname-invoice-number-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Surinamese VAT Rate Sanity Helper | ValidoHub"
  description:
    en: "Inspect VAT-rate snippets, local tax labels, amount bases, and official rate lookup boundaries."
