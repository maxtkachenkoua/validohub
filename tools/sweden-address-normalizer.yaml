schemaVersion: 1
id: sweden-address-normalizer
kind: tool
module: validohub
country: SE
category: address
status: active
name:
  en: "Swedish Address Normalizer"
summary:
  en: "Normalize street, postal code, locality, region, and country lines for local address forms."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.sweden-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ADDR input"
        required: true
        placeholder:
          en: "Drottninggatan 1, 111 20 Stockholm"
    actions: [validate, parse, format, explain]
related:
  explicit: [sweden-address-transliteration-normalizer, sweden-region-code-mapper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Swedish Address Normalizer | ValidoHub"
  description:
    en: "Normalize street, postal code, locality, region, and country lines for local address forms."
