schemaVersion: 1
id: sweden-address-transliteration-normalizer
kind: tool
module: validohub
country: SE
category: address
status: active
name:
  en: "Swedish Address Transliteration Normalizer"
summary:
  en: "Prepare ASCII-safe address keys while preserving local display text and search/debug evidence."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.sweden-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ASCII input"
        required: true
        placeholder:
          en: "Drottninggatan 1, 111 20 Stockholm"
    actions: [validate, parse, format, explain]
related:
  explicit: [sweden-region-code-mapper, sweden-municipality-code-inspector]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Swedish Address Transliteration Normalizer | ValidoHub"
  description:
    en: "Prepare ASCII-safe address keys while preserving local display text and search/debug evidence."
