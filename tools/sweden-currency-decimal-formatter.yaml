schemaVersion: 1
id: sweden-currency-decimal-formatter
kind: tool
module: validohub
country: SE
category: finance
status: active
name:
  en: "Swedish SEK Decimal Currency Formatter"
summary:
  en: "Normalize SEK amount strings, decimal separators, grouping, and API-safe numeric previews."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.sweden-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "CUR input"
        required: true
        placeholder:
          en: "1 234,56 SEK"
    actions: [validate, parse, format, explain]
related:
  explicit: [sweden-vat-rate-sanity-helper, sweden-vat-return-field-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Swedish SEK Decimal Currency Formatter | ValidoHub"
  description:
    en: "Normalize SEK amount strings, decimal separators, grouping, and API-safe numeric previews."
