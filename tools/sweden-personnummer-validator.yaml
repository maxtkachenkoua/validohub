schemaVersion: 1
id: sweden-personnummer-validator
kind: tool
module: validohub
country: SE
category: national-identifiers
status: active
name:
  en: "Swedish Personnummer Validator"
summary:
  en: "Validate Personnummer shape, split date/control/body evidence, and prepare privacy-safe debugging output."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.sweden-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ID input"
        required: true
        placeholder:
          en: "850101-1236"
    actions: [validate, parse, format, explain]
related:
  explicit: [sweden-organisationsnummer-validator, sweden-vat-id-validator]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Swedish Personnummer Validator | ValidoHub"
  description:
    en: "Validate Personnummer shape, split date/control/body evidence, and prepare privacy-safe debugging output."
