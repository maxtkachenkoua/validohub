schemaVersion: 1
id: sweden-vat-id-validator
kind: tool
module: validohub
country: SE
category: national-identifiers
status: active
name:
  en: "Swedish VAT ID / SE Prefix Validator"
summary:
  en: "Normalize SE VAT identifiers, inspect local tax body evidence, and prepare VIES handoff diagnostics."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.sweden-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "VAT input"
        required: true
        placeholder:
          en: "SE556016068001"
    actions: [validate, parse, format, explain]
related:
  explicit: [sweden-eori-validator, sweden-samordningsnummer-social-insurance-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Swedish VAT ID / SE Prefix Validator | ValidoHub"
  description:
    en: "Normalize SE VAT identifiers, inspect local tax body evidence, and prepare VIES handoff diagnostics."
