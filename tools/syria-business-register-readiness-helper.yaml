schemaVersion: 1
id: syria-business-register-readiness-helper
kind: tool
module: validohub
country: SY
category: government
status: active
name:
  en: "Syrian commercial registry boundary Readiness Helper"
summary:
  en: "Prepare browser-only evidence before a regulated commercial registry boundary lookup or company registry workflow."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.syria-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "REG input"
        required: true
        placeholder:
          en: "SY-123456 TAX123456 Malki 1, Damascus 0100"
    actions: [validate, parse, format, explain]
related:
  explicit: [syria-id-card-format-helper, syria-passport-number-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Syrian commercial registry boundary Readiness Helper | ValidoHub"
  description:
    en: "Prepare browser-only evidence before a regulated commercial registry boundary lookup or company registry workflow."
