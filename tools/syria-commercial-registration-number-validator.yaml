schemaVersion: 1
id: syria-commercial-registration-number-validator
kind: tool
module: validohub
country: SY
category: national-identifiers
status: active
name:
  en: "Syrian commercial registration number Validator"
summary:
  en: "Inspect commercial registration number structure, registry-style prefixes, control digits, and official lookup boundaries."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.syria-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ORG input"
        required: true
        placeholder:
          en: "SY-123456"
    actions: [validate, parse, format, explain]
related:
  explicit: [syria-tax-id-validator, syria-customs-importer-code-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Syrian commercial registration number Validator | ValidoHub"
  description:
    en: "Inspect commercial registration number structure, registry-style prefixes, control digits, and official lookup boundaries."
