schemaVersion: 1
id: taiwan-accessibility-locale-copy-checker
kind: tool
module: validohub
country: TW
category: localization
status: active
name:
  en: "Taiwanese Locale Copy Checker"
summary:
  en: "Check UI labels for local identifier names, date/currency wording, and support-safe explanations."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.taiwan-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "COPY input"
        required: true
        placeholder:
          en: "National ID boundary input, postal code input, amount 1,234 TWD"
    actions: [validate, parse, format, explain]
related:
  explicit: [taiwan-support-ticket-scrubber, taiwan-integration-smoke-test-builder]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Taiwanese Locale Copy Checker | ValidoHub"
  description:
    en: "Check UI labels for local identifier names, date/currency wording, and support-safe explanations."
