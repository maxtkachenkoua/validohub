schemaVersion: 1
id: taiwan-address-normalizer
kind: tool
module: validohub
country: TW
category: address
status: active
name:
  en: "Taiwanese Address Normalizer"
summary:
  en: "Normalize street, postal code, locality, region, and country lines for local address forms."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.taiwan-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ADDR input"
        required: true
        placeholder:
          en: "Zhongxiao East Road 1, Taipei 100"
    actions: [validate, parse, format, explain]
related:
  explicit: [taiwan-address-transliteration-normalizer, taiwan-region-code-mapper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Taiwanese Address Normalizer | ValidoHub"
  description:
    en: "Normalize street, postal code, locality, region, and country lines for local address forms."
