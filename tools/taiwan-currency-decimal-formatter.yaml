schemaVersion: 1
id: taiwan-currency-decimal-formatter
kind: tool
module: validohub
country: TW
category: finance
status: active
name:
  en: "Taiwanese TWD Decimal Currency Formatter"
summary:
  en: "Normalize TWD amount strings, decimal separators, grouping, and API-safe numeric previews."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.taiwan-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "CUR input"
        required: true
        placeholder:
          en: "1,234 TWD"
    actions: [validate, parse, format, explain]
related:
  explicit: [taiwan-tax-rate-sanity-helper, taiwan-tax-return-field-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Taiwanese TWD Decimal Currency Formatter | ValidoHub"
  description:
    en: "Normalize TWD amount strings, decimal separators, grouping, and API-safe numeric previews."
