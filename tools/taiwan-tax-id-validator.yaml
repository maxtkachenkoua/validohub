schemaVersion: 1
id: taiwan-tax-id-validator
kind: tool
module: validohub
country: TW
category: national-identifiers
status: active
name:
  en: "Taiwanese VAT / business tax number Validator"
summary:
  en: "Normalize VAT / business tax number identifiers, inspect local tax body evidence, and prepare authority handoff diagnostics."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.taiwan-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "TAX input"
        required: true
        placeholder:
          en: "12345675"
    actions: [validate, parse, format, explain]
related:
  explicit: [taiwan-customs-importer-code-helper, taiwan-labor-insurance-boundary-social-insurance-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Taiwanese VAT / business tax number Validator | ValidoHub"
  description:
    en: "Normalize VAT / business tax number identifiers, inspect local tax body evidence, and prepare authority handoff diagnostics."
