schemaVersion: 1
id: taiwan-tax-rate-sanity-helper
kind: tool
module: validohub
country: TW
category: tax
status: active
name:
  en: "Taiwanese Tax Rate Sanity Helper"
summary:
  en: "Inspect tax-rate snippets, local tax labels, amount bases, and official rate lookup boundaries."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.taiwan-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "RATE input"
        required: true
        placeholder:
          en: "VAT / business tax number 20% base 1,234 TWD"
    actions: [validate, parse, format, explain]
related:
  explicit: [taiwan-tax-return-field-helper, taiwan-invoice-number-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Taiwanese Tax Rate Sanity Helper | ValidoHub"
  description:
    en: "Inspect tax-rate snippets, local tax labels, amount bases, and official rate lookup boundaries."
