schemaVersion: 1
id: tajikistan-bank-statement-parser
kind: tool
module: validohub
country: TJ
category: finance
status: active
name:
  en: "Tajik Bank Statement Parser"
summary:
  en: "Parse statement rows for date, amount, account/routing evidence, counterparty, reference, and local decimal conventions."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.tajikistan-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "STMT input"
        required: true
        placeholder:
          en: "21.07.2026; 1 234,56 TJS; 3501012345678901; sample counterparty"
    actions: [validate, parse, format, explain]
related:
  explicit: [tajikistan-masked-bank-account-formatter, tajikistan-currency-decimal-formatter]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Tajik Bank Statement Parser | ValidoHub"
  description:
    en: "Parse statement rows for date, amount, account/routing evidence, counterparty, reference, and local decimal conventions."
