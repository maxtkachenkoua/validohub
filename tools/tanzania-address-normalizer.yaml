schemaVersion: 1
id: tanzania-address-normalizer
kind: tool
module: validohub
country: TZ
category: address
status: active
name:
  en: "Tanzanian Address Normalizer"
summary:
  en: "Normalize street, postal code, locality, region, and country lines for local address forms."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.tanzania-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ADDR input"
        required: true
        placeholder:
          en: "Central business district, Dodoma 1000"
    actions: [validate, parse, format, explain]
related:
  explicit: [tanzania-address-transliteration-normalizer, tanzania-region-code-mapper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Tanzanian Address Normalizer | ValidoHub"
  description:
    en: "Normalize street, postal code, locality, region, and country lines for local address forms."
