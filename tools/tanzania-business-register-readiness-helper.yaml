schemaVersion: 1
id: tanzania-business-register-readiness-helper
kind: tool
module: validohub
country: TZ
category: government
status: active
name:
  en: "Tanzanian business registry Readiness Helper"
summary:
  en: "Prepare browser-only evidence before a regulated business registry lookup or company registry workflow."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.tanzania-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "REG input"
        required: true
        placeholder:
          en: "TZ-REG-123456 TZ123456789 Central business district, Dodoma 1000"
    actions: [validate, parse, format, explain]
related:
  explicit: [tanzania-id-card-format-helper, tanzania-passport-number-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Tanzanian business registry Readiness Helper | ValidoHub"
  description:
    en: "Prepare browser-only evidence before a regulated business registry lookup or company registry workflow."
