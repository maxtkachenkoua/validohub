schemaVersion: 1
id: tanzania-company-registration-number-validator
kind: tool
module: validohub
country: TZ
category: national-identifiers
status: active
name:
  en: "Tanzanian company registration number Validator"
summary:
  en: "Inspect company registration number structure, registry-style prefixes, control digits, and official lookup boundaries."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.tanzania-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ORG input"
        required: true
        placeholder:
          en: "TZ-REG-123456"
    actions: [validate, parse, format, explain]
related:
  explicit: [tanzania-tax-id-validator, tanzania-customs-importer-code-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Tanzanian company registration number Validator | ValidoHub"
  description:
    en: "Inspect company registration number structure, registry-style prefixes, control digits, and official lookup boundaries."
