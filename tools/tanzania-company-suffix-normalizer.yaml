schemaVersion: 1
id: tanzania-company-suffix-normalizer
kind: tool
module: validohub
country: TZ
category: developer-tools
status: active
name:
  en: "Tanzanian Company Suffix Normalizer"
summary:
  en: "Normalize local company suffixes, registry labels, and search-key variants for onboarding forms."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.tanzania-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "SUFFIX input"
        required: true
        placeholder:
          en: "Tanzania Sample Holding Ltd TZ-REG-123456"
    actions: [validate, parse, format, explain]
related:
  explicit: [tanzania-procurement-identifier-helper, tanzania-accessibility-locale-copy-checker]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Tanzanian Company Suffix Normalizer | ValidoHub"
  description:
    en: "Normalize local company suffixes, registry labels, and search-key variants for onboarding forms."
