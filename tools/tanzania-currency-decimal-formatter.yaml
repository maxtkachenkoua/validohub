schemaVersion: 1
id: tanzania-currency-decimal-formatter
kind: tool
module: validohub
country: TZ
category: finance
status: active
name:
  en: "Tanzanian TZS Decimal Currency Formatter"
summary:
  en: "Normalize TZS amount strings, decimal separators, grouping, and API-safe numeric previews."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.tanzania-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "CUR input"
        required: true
        placeholder:
          en: "1,234.56 TZS"
    actions: [validate, parse, format, explain]
related:
  explicit: [tanzania-tax-rate-sanity-helper, tanzania-tax-return-field-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Tanzanian TZS Decimal Currency Formatter | ValidoHub"
  description:
    en: "Normalize TZS amount strings, decimal separators, grouping, and API-safe numeric previews."
