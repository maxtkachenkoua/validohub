schemaVersion: 1
id: tanzania-id-card-format-helper
kind: tool
module: validohub
country: TZ
category: national-identifiers
status: active
name:
  en: "Tanzanian ID Card Format Helper"
summary:
  en: "Inspect local identity-card snippets, document numbers, dates, and privacy boundaries without proving identity."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.tanzania-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "CARD input"
        required: true
        placeholder:
          en: "TZ-ID-123456"
    actions: [validate, parse, format, explain]
related:
  explicit: [tanzania-passport-number-helper, tanzania-mrz-passport-parser]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Tanzanian ID Card Format Helper | ValidoHub"
  description:
    en: "Inspect local identity-card snippets, document numbers, dates, and privacy boundaries without proving identity."
