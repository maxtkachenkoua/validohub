schemaVersion: 1
id: thailand-address-transliteration-normalizer
kind: tool
module: validohub
country: TH
category: address
status: active
name:
  en: "Thai Address Transliteration Normalizer"
summary:
  en: "Prepare ASCII-safe address keys while preserving local display text and search/debug evidence."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.thailand-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ASCII input"
        required: true
        placeholder:
          en: "Sukhumvit Road 1, Bangkok 10110"
    actions: [validate, parse, format, explain]
related:
  explicit: [thailand-region-code-mapper, thailand-municipality-code-inspector]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Thai Address Transliteration Normalizer | ValidoHub"
  description:
    en: "Prepare ASCII-safe address keys while preserving local display text and search/debug evidence."
