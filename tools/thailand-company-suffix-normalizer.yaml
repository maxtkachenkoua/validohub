schemaVersion: 1
id: thailand-company-suffix-normalizer
kind: tool
module: validohub
country: TH
category: developer-tools
status: active
name:
  en: "Thai Company Suffix Normalizer"
summary:
  en: "Normalize local company suffixes, registry labels, and search-key variants for onboarding forms."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.thailand-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "SUFFIX input"
        required: true
        placeholder:
          en: "Thailand Sample Holding Ltd 0105559123456"
    actions: [validate, parse, format, explain]
related:
  explicit: [thailand-procurement-identifier-helper, thailand-accessibility-locale-copy-checker]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Thai Company Suffix Normalizer | ValidoHub"
  description:
    en: "Normalize local company suffixes, registry labels, and search-key variants for onboarding forms."
