schemaVersion: 1
id: timor-leste-id-boundary-validator
kind: tool
module: validohub
country: TL
category: national-identifiers
status: active
name:
  en: "Timorese ID boundary Validator"
summary:
  en: "Validate ID boundary shape, split date/control/body evidence, and prepare privacy-safe debugging output."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.timor-leste-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ID input"
        required: true
        placeholder:
          en: "TL-1234567"
    actions: [validate, parse, format, explain]
related:
  explicit: [timor-leste-company-registration-number-validator, timor-leste-tax-id-validator]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Timorese ID boundary Validator | ValidoHub"
  description:
    en: "Validate ID boundary shape, split date/control/body evidence, and prepare privacy-safe debugging output."
