schemaVersion: 1
id: tonga-address-normalizer
kind: tool
module: validohub
country: TO
category: address
status: active
name:
  en: "Tongan Address Normalizer"
summary:
  en: "Normalize street, postal code, locality, region, and country lines for local address forms."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.tonga-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ADDR input"
        required: true
        placeholder:
          en: "Taufaʻahau Road, Nukuʻalofa"
    actions: [validate, parse, format, explain]
related:
  explicit: [tonga-address-transliteration-normalizer, tonga-region-code-mapper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Tongan Address Normalizer | ValidoHub"
  description:
    en: "Normalize street, postal code, locality, region, and country lines for local address forms."
