schemaVersion: 1
id: tonga-currency-decimal-formatter
kind: tool
module: validohub
country: TO
category: finance
status: active
name:
  en: "Tongan TOP Decimal Currency Formatter"
summary:
  en: "Normalize TOP amount strings, decimal separators, grouping, and API-safe numeric previews."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.tonga-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "CUR input"
        required: true
        placeholder:
          en: "1,234.56 TOP"
    actions: [validate, parse, format, explain]
related:
  explicit: [tonga-tax-rate-sanity-helper, tonga-tax-return-field-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Tongan TOP Decimal Currency Formatter | ValidoHub"
  description:
    en: "Normalize TOP amount strings, decimal separators, grouping, and API-safe numeric previews."
