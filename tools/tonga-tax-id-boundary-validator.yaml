schemaVersion: 1
id: tonga-tax-id-boundary-validator
kind: tool
module: validohub
country: TO
category: national-identifiers
status: active
name:
  en: "Tongan tax ID boundary Validator"
summary:
  en: "Validate tax ID boundary shape, split date/control/body evidence, and prepare privacy-safe debugging output."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.tonga-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ID input"
        required: true
        placeholder:
          en: "TO-TIN-123456"
    actions: [validate, parse, format, explain]
related:
  explicit: [tonga-company-registration-number-validator, tonga-tax-id-validator]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Tongan tax ID boundary Validator | ValidoHub"
  description:
    en: "Validate tax ID boundary shape, split date/control/body evidence, and prepare privacy-safe debugging output."
