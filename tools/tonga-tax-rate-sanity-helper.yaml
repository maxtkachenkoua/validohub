schemaVersion: 1
id: tonga-tax-rate-sanity-helper
kind: tool
module: validohub
country: TO
category: tax
status: active
name:
  en: "Tongan Tax Rate Sanity Helper"
summary:
  en: "Inspect tax-rate snippets, local tax labels, amount bases, and official rate lookup boundaries."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.tonga-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "RATE input"
        required: true
        placeholder:
          en: "consumption tax boundary 20% base 1,234.56 TOP"
    actions: [validate, parse, format, explain]
related:
  explicit: [tonga-tax-return-field-helper, tonga-invoice-number-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Tongan Tax Rate Sanity Helper | ValidoHub"
  description:
    en: "Inspect tax-rate snippets, local tax labels, amount bases, and official rate lookup boundaries."
