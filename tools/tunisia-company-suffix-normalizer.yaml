schemaVersion: 1
id: tunisia-company-suffix-normalizer
kind: tool
module: validohub
country: TN
category: developer-tools
status: active
name:
  en: "Tunisian Company Suffix Normalizer"
summary:
  en: "Normalize local company suffixes, registry labels, and search-key variants for onboarding forms."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.tunisia-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "SUFFIX input"
        required: true
        placeholder:
          en: "Tunisia Sample Holding Ltd TN-REG-123456"
    actions: [validate, parse, format, explain]
related:
  explicit: [tunisia-procurement-identifier-helper, tunisia-accessibility-locale-copy-checker]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Tunisian Company Suffix Normalizer | ValidoHub"
  description:
    en: "Normalize local company suffixes, registry labels, and search-key variants for onboarding forms."
