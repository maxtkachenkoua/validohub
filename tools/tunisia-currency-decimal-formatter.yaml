schemaVersion: 1
id: tunisia-currency-decimal-formatter
kind: tool
module: validohub
country: TN
category: finance
status: active
name:
  en: "Tunisian TND Decimal Currency Formatter"
summary:
  en: "Normalize TND amount strings, decimal separators, grouping, and API-safe numeric previews."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.tunisia-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "CUR input"
        required: true
        placeholder:
          en: "1,234.56 TND"
    actions: [validate, parse, format, explain]
related:
  explicit: [tunisia-tax-rate-sanity-helper, tunisia-tax-return-field-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Tunisian TND Decimal Currency Formatter | ValidoHub"
  description:
    en: "Normalize TND amount strings, decimal separators, grouping, and API-safe numeric previews."
