schemaVersion: 1
id: tunisia-pii-masker
kind: tool
module: validohub
country: TN
category: privacy
status: active
name:
  en: "Tunisian PII Masker"
summary:
  en: "Detect and mask local identifier, phone, address, bank, and tax evidence in plain text."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.tunisia-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "PII input"
        required: true
        placeholder:
          en: "TN-ID-123456 +216 123 456 789 001 1234567890"
    actions: [validate, parse, format, explain]
related:
  explicit: [tunisia-personal-data-fixture-generator, tunisia-driving-licence-format-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Tunisian PII Masker | ValidoHub"
  description:
    en: "Detect and mask local identifier, phone, address, bank, and tax evidence in plain text."
