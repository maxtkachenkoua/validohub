schemaVersion: 1
id: turkmenistan-address-transliteration-normalizer
kind: tool
module: validohub
country: TM
category: address
status: active
name:
  en: "Turkmen Address Transliteration Normalizer"
summary:
  en: "Prepare ASCII-safe address keys while preserving local display text and search/debug evidence."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.turkmenistan-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ASCII input"
        required: true
        placeholder:
          en: "Magtymguly Avenue 1, Ashgabat 744000"
    actions: [validate, parse, format, explain]
related:
  explicit: [turkmenistan-region-code-mapper, turkmenistan-municipality-code-inspector]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Turkmen Address Transliteration Normalizer | ValidoHub"
  description:
    en: "Prepare ASCII-safe address keys while preserving local display text and search/debug evidence."
