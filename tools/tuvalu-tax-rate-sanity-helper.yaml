schemaVersion: 1
id: tuvalu-tax-rate-sanity-helper
kind: tool
module: validohub
country: TV
category: tax
status: active
name:
  en: "Tuvaluan Tax Rate Sanity Helper"
summary:
  en: "Inspect tax-rate snippets, local tax labels, amount bases, and official rate lookup boundaries."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.tuvalu-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "RATE input"
        required: true
        placeholder:
          en: "tax boundary 20% base 1,234.56 AUD"
    actions: [validate, parse, format, explain]
related:
  explicit: [tuvalu-tax-return-field-helper, tuvalu-invoice-number-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Tuvaluan Tax Rate Sanity Helper | ValidoHub"
  description:
    en: "Inspect tax-rate snippets, local tax labels, amount bases, and official rate lookup boundaries."
