schemaVersion: 1
id: uganda-company-registration-number-validator
kind: tool
module: validohub
country: UG
category: national-identifiers
status: active
name:
  en: "Ugandan company registration number Validator"
summary:
  en: "Inspect company registration number structure, registry-style prefixes, control digits, and official lookup boundaries."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.uganda-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ORG input"
        required: true
        placeholder:
          en: "UG-REG-123456"
    actions: [validate, parse, format, explain]
related:
  explicit: [uganda-tax-id-validator, uganda-customs-importer-code-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Ugandan company registration number Validator | ValidoHub"
  description:
    en: "Inspect company registration number structure, registry-style prefixes, control digits, and official lookup boundaries."
