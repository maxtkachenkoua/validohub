schemaVersion: 1
id: uganda-company-suffix-normalizer
kind: tool
module: validohub
country: UG
category: developer-tools
status: active
name:
  en: "Ugandan Company Suffix Normalizer"
summary:
  en: "Normalize local company suffixes, registry labels, and search-key variants for onboarding forms."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.uganda-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "SUFFIX input"
        required: true
        placeholder:
          en: "Uganda Sample Holding Ltd UG-REG-123456"
    actions: [validate, parse, format, explain]
related:
  explicit: [uganda-procurement-identifier-helper, uganda-accessibility-locale-copy-checker]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Ugandan Company Suffix Normalizer | ValidoHub"
  description:
    en: "Normalize local company suffixes, registry labels, and search-key variants for onboarding forms."
