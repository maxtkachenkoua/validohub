schemaVersion: 1
id: uganda-currency-decimal-formatter
kind: tool
module: validohub
country: UG
category: finance
status: active
name:
  en: "Ugandan UGX Decimal Currency Formatter"
summary:
  en: "Normalize UGX amount strings, decimal separators, grouping, and API-safe numeric previews."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.uganda-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "CUR input"
        required: true
        placeholder:
          en: "1,234.56 UGX"
    actions: [validate, parse, format, explain]
related:
  explicit: [uganda-tax-rate-sanity-helper, uganda-tax-return-field-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Ugandan UGX Decimal Currency Formatter | ValidoHub"
  description:
    en: "Normalize UGX amount strings, decimal separators, grouping, and API-safe numeric previews."
