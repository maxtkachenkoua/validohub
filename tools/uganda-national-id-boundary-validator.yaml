schemaVersion: 1
id: uganda-national-id-boundary-validator
kind: tool
module: validohub
country: UG
category: national-identifiers
status: active
name:
  en: "Ugandan national ID boundary Validator"
summary:
  en: "Validate national ID boundary shape, split date/control/body evidence, and prepare privacy-safe debugging output."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.uganda-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ID input"
        required: true
        placeholder:
          en: "UG-ID-123456"
    actions: [validate, parse, format, explain]
related:
  explicit: [uganda-company-registration-number-validator, uganda-tax-id-validator]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Ugandan national ID boundary Validator | ValidoHub"
  description:
    en: "Validate national ID boundary shape, split date/control/body evidence, and prepare privacy-safe debugging output."
