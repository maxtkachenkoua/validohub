schemaVersion: 1
id: ukraine-address-normalizer
kind: tool
module: validohub
country: UA
category: address
status: active
name:
  en: "Ukrainian Address Normalizer"
summary:
  en: "Normalize street, postal code, locality, region, and country lines for local address forms."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.ukraine-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ADDR input"
        required: true
        placeholder:
          en: "Khreshchatyk 1, 01001 Kyiv"
    actions: [validate, parse, format, explain]
related:
  explicit: [ukraine-address-transliteration-normalizer, ukraine-region-code-mapper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Ukrainian Address Normalizer | ValidoHub"
  description:
    en: "Normalize street, postal code, locality, region, and country lines for local address forms."
