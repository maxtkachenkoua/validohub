schemaVersion: 1
id: uruguay-cedula-de-identidad-validator
kind: tool
module: validohub
country: UY
category: national-identifiers
status: active
name:
  en: "Uruguayan Cedula de identidad Validator"
summary:
  en: "Validate Cedula de identidad shape, split date/control/body evidence, and prepare privacy-safe debugging output."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.uruguay-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ID input"
        required: true
        placeholder:
          en: "1.234.567-8"
    actions: [validate, parse, format, explain]
related:
  explicit: [uruguay-rut-validator, uruguay-vat-id-validator]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Uruguayan Cedula de identidad Validator | ValidoHub"
  description:
    en: "Validate Cedula de identidad shape, split date/control/body evidence, and prepare privacy-safe debugging output."
