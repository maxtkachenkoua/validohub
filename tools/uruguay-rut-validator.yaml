schemaVersion: 1
id: uruguay-rut-validator
kind: tool
module: validohub
country: UY
category: national-identifiers
status: active
name:
  en: "Uruguayan RUT Validator"
summary:
  en: "Inspect RUT structure, registry-style prefixes, control digits, and official lookup boundaries."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.uruguay-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ORG input"
        required: true
        placeholder:
          en: "210123456789"
    actions: [validate, parse, format, explain]
related:
  explicit: [uruguay-vat-id-validator, uruguay-customs-tax-identifier-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Uruguayan RUT Validator | ValidoHub"
  description:
    en: "Inspect RUT structure, registry-style prefixes, control digits, and official lookup boundaries."
