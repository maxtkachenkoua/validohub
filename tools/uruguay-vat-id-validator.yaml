schemaVersion: 1
id: uruguay-vat-id-validator
kind: tool
module: validohub
country: UY
category: national-identifiers
status: active
name:
  en: "Uruguayan IVA / RUT Tax ID Validator"
summary:
  en: "Normalize IVA / RUT identifiers, inspect local tax body evidence, and prepare tax-authority handoff diagnostics."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.uruguay-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "TAX input"
        required: true
        placeholder:
          en: "UY RUT 210123456789"
    actions: [validate, parse, format, explain]
related:
  explicit: [uruguay-customs-tax-identifier-helper, uruguay-bps-evidence-social-insurance-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Uruguayan IVA / RUT Tax ID Validator | ValidoHub"
  description:
    en: "Normalize IVA / RUT identifiers, inspect local tax body evidence, and prepare tax-authority handoff diagnostics."
