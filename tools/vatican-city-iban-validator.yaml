schemaVersion: 1
id: vatican-city-iban-validator
kind: tool
module: validohub
country: VA
category: finance
status: active
name:
  en: "Vatican City IBAN Validator"
summary:
  en: "Validate VA IBAN shape, replay MOD-97 evidence, and split bank/account blocks for debugging."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.vatican-city-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "IBAN input"
        required: true
        placeholder:
          en: "VA59001123000000012345678"
    actions: [validate, parse, format, explain]
related:
  explicit: [vatican-city-iban-generator, vatican-city-bank-account-inspector]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Vatican City IBAN Validator | ValidoHub"
  description:
    en: "Validate VA IBAN shape, replay MOD-97 evidence, and split bank/account blocks for debugging."
