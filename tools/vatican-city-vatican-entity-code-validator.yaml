schemaVersion: 1
id: vatican-city-vatican-entity-code-validator
kind: tool
module: validohub
country: VA
category: national-identifiers
status: active
name:
  en: "Vatican Vatican entity code Validator"
summary:
  en: "Inspect Vatican entity code structure, registry-style prefixes, control digits, and official lookup boundaries."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.vatican-city-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ORG input"
        required: true
        placeholder:
          en: "VA-ENTITY-001"
    actions: [validate, parse, format, explain]
related:
  explicit: [vatican-city-vat-id-validator, vatican-city-eori-validator]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Vatican Vatican entity code Validator | ValidoHub"
  description:
    en: "Inspect Vatican entity code structure, registry-style prefixes, control digits, and official lookup boundaries."
