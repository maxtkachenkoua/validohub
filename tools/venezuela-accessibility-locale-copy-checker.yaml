schemaVersion: 1
id: venezuela-accessibility-locale-copy-checker
kind: tool
module: validohub
country: VE
category: localization
status: active
name:
  en: "Venezuelan Locale Copy Checker"
summary:
  en: "Check UI labels for local identifier names, date/currency wording, and support-safe explanations."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.venezuela-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "COPY input"
        required: true
        placeholder:
          en: "Cedula input, codigo postal input, amount 1.234,56 VES"
    actions: [validate, parse, format, explain]
related:
  explicit: [venezuela-support-ticket-scrubber, venezuela-integration-smoke-test-builder]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Venezuelan Locale Copy Checker | ValidoHub"
  description:
    en: "Check UI labels for local identifier names, date/currency wording, and support-safe explanations."
