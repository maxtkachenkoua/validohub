schemaVersion: 1
id: venezuela-currency-decimal-formatter
kind: tool
module: validohub
country: VE
category: finance
status: active
name:
  en: "Venezuelan VES Decimal Currency Formatter"
summary:
  en: "Normalize VES amount strings, decimal separators, grouping, and API-safe numeric previews."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.venezuela-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "CUR input"
        required: true
        placeholder:
          en: "1.234,56 VES"
    actions: [validate, parse, format, explain]
related:
  explicit: [venezuela-vat-rate-sanity-helper, venezuela-vat-return-field-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Venezuelan VES Decimal Currency Formatter | ValidoHub"
  description:
    en: "Normalize VES amount strings, decimal separators, grouping, and API-safe numeric previews."
