schemaVersion: 1
id: venezuela-masked-bank-account-formatter
kind: tool
module: validohub
country: VE
category: finance
status: active
name:
  en: "Venezuelan Masked Bank Account Formatter"
summary:
  en: "Create log-safe domestic account previews while preserving bank, branch, and account-tail evidence."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.venezuela-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "MASK input"
        required: true
        placeholder:
          en: "0102-1234567890"
    actions: [validate, parse, format, explain]
related:
  explicit: [venezuela-currency-decimal-formatter, venezuela-vat-rate-sanity-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Venezuelan Masked Bank Account Formatter | ValidoHub"
  description:
    en: "Create log-safe domestic account previews while preserving bank, branch, and account-tail evidence."
