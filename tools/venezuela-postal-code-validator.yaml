schemaVersion: 1
id: venezuela-postal-code-validator
kind: tool
module: validohub
country: VE
category: address
status: active
name:
  en: "Venezuelan Postal Code Validator"
summary:
  en: "Validate codigo postal shape, split area/delivery hints, and preserve official postal lookup boundaries."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.venezuela-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "POST input"
        required: true
        placeholder:
          en: "1010 Caracas"
    actions: [validate, parse, format, explain]
related:
  explicit: [venezuela-address-normalizer, venezuela-address-transliteration-normalizer]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Venezuelan Postal Code Validator | ValidoHub"
  description:
    en: "Validate codigo postal shape, split area/delivery hints, and preserve official postal lookup boundaries."
