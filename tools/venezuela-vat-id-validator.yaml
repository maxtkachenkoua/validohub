schemaVersion: 1
id: venezuela-vat-id-validator
kind: tool
module: validohub
country: VE
category: national-identifiers
status: active
name:
  en: "Venezuelan IVA / RIF Tax ID Validator"
summary:
  en: "Normalize IVA / RIF identifiers, inspect local tax body evidence, and prepare tax-authority handoff diagnostics."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.venezuela-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "TAX input"
        required: true
        placeholder:
          en: "VE J-12345678-9"
    actions: [validate, parse, format, explain]
related:
  explicit: [venezuela-customs-tax-identifier-helper, venezuela-ivss-evidence-social-insurance-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Venezuelan IVA / RIF Tax ID Validator | ValidoHub"
  description:
    en: "Normalize IVA / RIF identifiers, inspect local tax body evidence, and prepare tax-authority handoff diagnostics."
