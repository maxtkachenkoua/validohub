schemaVersion: 1
id: venezuela-vat-rate-sanity-helper
kind: tool
module: validohub
country: VE
category: tax
status: active
name:
  en: "Venezuelan VAT Rate Sanity Helper"
summary:
  en: "Inspect VAT-rate snippets, local tax labels, amount bases, and official rate lookup boundaries."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.venezuela-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "RATE input"
        required: true
        placeholder:
          en: "IVA / RIF 20% base 1.234,56 VES"
    actions: [validate, parse, format, explain]
related:
  explicit: [venezuela-vat-return-field-helper, venezuela-invoice-number-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Venezuelan VAT Rate Sanity Helper | ValidoHub"
  description:
    en: "Inspect VAT-rate snippets, local tax labels, amount bases, and official rate lookup boundaries."
