schemaVersion: 1
id: vietnam-address-transliteration-normalizer
kind: tool
module: validohub
country: VN
category: address
status: active
name:
  en: "Vietnamese Address Transliteration Normalizer"
summary:
  en: "Prepare ASCII-safe address keys while preserving local display text and search/debug evidence."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.vietnam-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ASCII input"
        required: true
        placeholder:
          en: "1 Trang Tien, Hanoi 100000"
    actions: [validate, parse, format, explain]
related:
  explicit: [vietnam-region-code-mapper, vietnam-municipality-code-inspector]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Vietnamese Address Transliteration Normalizer | ValidoHub"
  description:
    en: "Prepare ASCII-safe address keys while preserving local display text and search/debug evidence."
