schemaVersion: 1
id: vietnam-currency-decimal-formatter
kind: tool
module: validohub
country: VN
category: finance
status: active
name:
  en: "Vietnamese VND Decimal Currency Formatter"
summary:
  en: "Normalize VND amount strings, decimal separators, grouping, and API-safe numeric previews."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.vietnam-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "CUR input"
        required: true
        placeholder:
          en: "1.234.567 VND"
    actions: [validate, parse, format, explain]
related:
  explicit: [vietnam-tax-rate-sanity-helper, vietnam-tax-return-field-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Vietnamese VND Decimal Currency Formatter | ValidoHub"
  description:
    en: "Normalize VND amount strings, decimal separators, grouping, and API-safe numeric previews."
