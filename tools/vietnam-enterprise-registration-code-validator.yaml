schemaVersion: 1
id: vietnam-enterprise-registration-code-validator
kind: tool
module: validohub
country: VN
category: national-identifiers
status: active
name:
  en: "Vietnamese enterprise registration code Validator"
summary:
  en: "Inspect enterprise registration code structure, registry-style prefixes, control digits, and official lookup boundaries."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.vietnam-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "ORG input"
        required: true
        placeholder:
          en: "0101234567"
    actions: [validate, parse, format, explain]
related:
  explicit: [vietnam-tax-id-validator, vietnam-customs-importer-code-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Vietnamese enterprise registration code Validator | ValidoHub"
  description:
    en: "Inspect enterprise registration code structure, registry-style prefixes, control digits, and official lookup boundaries."
