schemaVersion: 1
id: vietnam-tax-id-validator
kind: tool
module: validohub
country: VN
category: national-identifiers
status: active
name:
  en: "Vietnamese tax code Validator"
summary:
  en: "Normalize tax code identifiers, inspect local tax body evidence, and prepare authority handoff diagnostics."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.vietnam-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "TAX input"
        required: true
        placeholder:
          en: "0101234567"
    actions: [validate, parse, format, explain]
related:
  explicit: [vietnam-customs-importer-code-helper, vietnam-vssid-boundary-social-insurance-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Vietnamese tax code Validator | ValidoHub"
  description:
    en: "Normalize tax code identifiers, inspect local tax body evidence, and prepare authority handoff diagnostics."
