schemaVersion: 1
id: yemen-currency-decimal-formatter
kind: tool
module: validohub
country: YE
category: finance
status: active
name:
  en: "Yemeni YER Decimal Currency Formatter"
summary:
  en: "Normalize YER amount strings, decimal separators, grouping, and API-safe numeric previews."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.yemen-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "CUR input"
        required: true
        placeholder:
          en: "1,234,567 YER"
    actions: [validate, parse, format, explain]
related:
  explicit: [yemen-tax-rate-sanity-helper, yemen-tax-return-field-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Yemeni YER Decimal Currency Formatter | ValidoHub"
  description:
    en: "Normalize YER amount strings, decimal separators, grouping, and API-safe numeric previews."
