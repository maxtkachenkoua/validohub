schemaVersion: 1
id: zambia-accessibility-locale-copy-checker
kind: tool
module: validohub
country: ZM
category: localization
status: active
name:
  en: "Zambian Locale Copy Checker"
summary:
  en: "Check UI labels for local identifier names, date/currency wording, and support-safe explanations."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.zambia-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "COPY input"
        required: true
        placeholder:
          en: "national ID boundary input, postal/address code input, amount 1,234.56 ZMW"
    actions: [validate, parse, format, explain]
related:
  explicit: [zambia-support-ticket-scrubber, zambia-integration-smoke-test-builder]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Zambian Locale Copy Checker | ValidoHub"
  description:
    en: "Check UI labels for local identifier names, date/currency wording, and support-safe explanations."
