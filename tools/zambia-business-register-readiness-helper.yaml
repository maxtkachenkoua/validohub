schemaVersion: 1
id: zambia-business-register-readiness-helper
kind: tool
module: validohub
country: ZM
category: government
status: active
name:
  en: "Zambian business registry Readiness Helper"
summary:
  en: "Prepare browser-only evidence before a regulated business registry lookup or company registry workflow."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.zambia-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "REG input"
        required: true
        placeholder:
          en: "ZM-REG-123456 ZM123456789 Central business district, Lusaka 1000"
    actions: [validate, parse, format, explain]
related:
  explicit: [zambia-id-card-format-helper, zambia-passport-number-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Zambian business registry Readiness Helper | ValidoHub"
  description:
    en: "Prepare browser-only evidence before a regulated business registry lookup or company registry workflow."
