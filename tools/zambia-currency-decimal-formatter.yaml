schemaVersion: 1
id: zambia-currency-decimal-formatter
kind: tool
module: validohub
country: ZM
category: finance
status: active
name:
  en: "Zambian ZMW Decimal Currency Formatter"
summary:
  en: "Normalize ZMW amount strings, decimal separators, grouping, and API-safe numeric previews."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.zambia-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "CUR input"
        required: true
        placeholder:
          en: "1,234.56 ZMW"
    actions: [validate, parse, format, explain]
related:
  explicit: [zambia-tax-rate-sanity-helper, zambia-tax-return-field-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Zambian ZMW Decimal Currency Formatter | ValidoHub"
  description:
    en: "Normalize ZMW amount strings, decimal separators, grouping, and API-safe numeric previews."
