schemaVersion: 1
id: zambia-pii-masker
kind: tool
module: validohub
country: ZM
category: privacy
status: active
name:
  en: "Zambian PII Masker"
summary:
  en: "Detect and mask local identifier, phone, address, bank, and tax evidence in plain text."
capabilities:
  - validate
  - parse
  - format
  - explain
algorithm:
  algorithmId: validohub.zambia-suite
forms:
  validate:
    inputs:
      - type: textarea
        name: input
        label:
          en: "PII input"
        required: true
        placeholder:
          en: "ZM-ID-123456 +260 123 456 789 001 1234567890"
    actions: [validate, parse, format, explain]
related:
  explicit: [zambia-personal-data-fixture-generator, zambia-driving-licence-format-helper]
  auto:
    sameCountry: true
    sameCategory: true
    sameCapability: true
seo:
  title:
    en: "Zambian PII Masker | ValidoHub"
  description:
    en: "Detect and mask local identifier, phone, address, bank, and tax evidence in plain text."
